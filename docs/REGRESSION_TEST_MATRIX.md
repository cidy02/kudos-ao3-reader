# REGRESSION_TEST_MATRIX.md

What must keep working, where it's tested, and what is manual-only. Run everything via `KUDOS_CASEFOLD_SIMULATOR_UDID=<udid> Scripts/verify.sh` (or just the suite via `KUDOS_CASEFOLD_SIMULATOR_UDID=<udid> Scripts/test.sh`; parallel testing deliberately disabled). The full target uses the case-sensitive APFS harness, so raw `xcodebuild test` is not a valid complete-suite command. 724+ tests / 68+ suites green as of T-150 (reader redesign polish, on top of the T-148/T-149 reader chrome redesign).

| Area | Invariant | Automated coverage | Manual-only residue |
|---|---|---|---|
| Privacy — blur | M/E works blur in Blur mode on List rows AND carousel cards (Home, Library incl. macOS branch, Subscriptions via `CanonicalWorkCoverCard`); tap-to-reveal via `PrivacyGate` | None (SwiftUI rendering) — logic lives in `SensitiveWorkRow`/`SensitiveWorkCoverCard` | Simulator pass per surface; Hide mode removes cards entirely |
| Privacy — button | `MatureRevealToggle` appears iff the **visible filtered** set contains M/E (`PrivacyGate.hasVisibleMatureWorks`); all surfaces incl. Library select mode | None | Filter Library to zero M/E → button disappears |
| Cards/select — Home/Library/Browse | Select mode toggles selection not navigation; selection survives carousel↔list switch; canonical dedup: one card per AO3 work | `CanonicalWorkMergeTests` (8: ID/URL matching, remoteLed/remoteOnly, ordering, duplicate mentions) | Tap-through of select flows |
| Multi-select batch actions | Browse (fandom/tag), Search results, and Author works share one selection shell (`RemoteWorkSelectionController`/`RemoteWorkBulkActionBar`): bulk Save / Save-for-Later / Add-to-Collection / Add-to-Queue resolve sequentially, report partial failures, never open sheets with empty work lists; an in-flight batch (and its failure alert / Add-to sheet) survives leaving selection mode and cancels only when the surface exits | None directly; primitives covered by `ReadingQueueTests`, `PreservedWorkTests` (revive-on-reacquire) | Offline batch → error alert; leave screen mid-batch; selection/bulk/pagination tap-through on all 4 surfaces |
| Search/index | Case+diacritic-insensitive, AND-across-terms over title/author/series/user tags/AO3 tags/rating/language/summary (index v2); reindex on import/refresh/restore **and user-tag add/remove** (backup restore reindexes *after* tag linking); launch rebuild migrates older index versions once, **time-sliced** (≤5 ms main-actor slices, incremental saves) so it never blocks typing; index never in backups; Global Search renders from a debounced match snapshot (no per-keystroke scans); "Fandoms on AO3" suggestions come from the precomputed normalized catalog index, prefix matches first then work count; the other type-to-filter surfaces (Browse category fandom filter, Library filter-panel option pickers, add-works-to-collection) filter over precomputed normalized text, never `localizedCaseInsensitiveContains` per keystroke | `WorkSearchIndexTests` (9, incl. 1,200-work sweep), `FandomCatalogSearchTests` (4) | Typing stays smooth in Search/Browse-filter with a large library + warm fandom catalog, including during the first-launch index migration |
| Library filters | Facet semantics unchanged; empty facets build no per-work Sets | `SearchFiltersTests` (AO3 side); Library side untested *(gap)* | Filter panel spot-check |
| AO3 parsing | Search pages, subscriptions, bookmarks, tags, metadata parse; failures degrade without data loss | `AO3ClientTests`, `AO3SubscriptionsParseTests`, `WorkTagsTests`, `FandomCatalogCacheTests` | Live-HTML drift (parsers are fixture-tested) |
| Native author profiles | Account/pseud routes remain exact; co-authors are independent; anonymous/local text is not guessed; dashboard/profile/works/series/bookmarks parse; tabs are lazy; scope loads cancel; pagination failure preserves content; cache is URL+auth scoped; block/mute confirm forms parse (apply + undo) and flash-reject without synthesizing POSTs; Subscribe/block/mute submits are route+scope+session-generation-fenced — a stale response can neither touch replacement state nor expire a replacement session; the confirm alert's dismiss write-back preserves the submit snapshot (SwiftUI dismisses before the button action runs) so Confirm actually POSTs — only explicit Cancel drops the staged form | `AO3AuthorIdentityTests`, `AO3AuthorProfileParserTests` (incl. moderation confirm fixtures), `AO3AuthorProfileStateTests`, `AO3AuthorActionFencingTests` (the shared submit fence, incl. mid-flight account switches + dismiss-vs-cancel snapshot survival), `AppRouterTests`; local persistence/backup boundary in `KudosBackupTests` | Signed-in block/mute/unblock/unmute against live AO3; official-user rejection; Light/Dark and compact/iPad/macOS layout |
| Networking politeness | Pace/coordinator/coalescer behavior; retry only transient; Retry-After honored | `AO3RequestCoordinatorTests`, `RequestCoalescerTests`; retry/backoff logic untested directly *(gap — pace()/withRetry are private)* | 429 behavior against live AO3 (don't provoke it) |
| Network + path boundaries (WP-B) | Anonymous fetch only talks to `isTrustedURL` hosts; `openAO3Link` does not treat lookalike hosts as AO3; session-validator 302s drop the Cookie header; temp download names cannot escape `Caches/Downloads`; folder-sync asset reads `lstat` and refuse `S_IFLNK`; both readers use a non-persistent website data store and cancel off-publication navigation; `AppRouter.open` admits only http/https (M19); persisted cookies keep SameSite Lax/Strict (M10); capture persists only `_otwarchive_session` + `user_credentials` (M14) | `AO3ClientPolicyTests` (M6), `AppRouterTests` (M7, M19 `openRejectsNonHTTPSchemes`), `LiveAO3SessionValidatorRelayTests` (M9), `StorageTests` (M11), `FolderSyncTests.syncDownRejectsSymlinkedEPUBAsset` (M12 — symlink target is a valid EPUB so A5-F3 cannot be the reject reason), `ReaderWebIsolationTests` (M8 — non-persistent isolated store, cookie-jar split vs `WKWebsiteDataStore.default()`, `javascript:`/`data:` deny-list, script-typed navigation cancel), `AO3SessionTests` (M10 `sameSitePolicySurvivesTheCaptureAndReinstallRoundTrip`, M14 `onlyAllowListedCookiesArePersisted`) | Hostile-EPUB JS on a real device (`window.location` assignment + in-page cookie-store probe) |
| Deletion/recovery | Soft delete → 90-day recovery; restore retracts tombstones; sweep gated; revive-on-reacquire (all paths); TOCTOU merge in `importEPUB`; Recently Deleted rows expose Restore/Delete-Permanently via a context menu in addition to swipes (the discoverable path, and the only per-row affordance mouse users get on macOS) | `PreservedWorkTests` (13+) | Recently Deleted UI tap-through (long-press menu on iOS, right-click on macOS) |
| Backup round-trip | v8 export/import; tombstone suppression + newer-snapshot revival; flags `incomingWins`-gated; progress fields travel; index excluded; EPUBs restored; reading annotations (highlights/bookmarks/notes) round-trip with locator-string identity, re-home onto the restored `SavedWork`, and same-work+kind+exact-locator duplicates from two independently-created UUIDs (multi-device) collapse to one survivor (newest `lastModifiedAt` wins, tombstoned loser, non-empty note salvaged); saved AO3 links and named searches mint immediate-delete tombstones on in-app delete and on Replace-mode omission, and Merge will not resurrect a record whose id has a local or adopted trusted tombstone | `KudosBackupTests` (incl. `replaceThenMergeDoesNotResurrectOmittedBookmarkOrSavedSearch`), `PersistenceSyncTests`, index exclusion in `WorkSearchIndexTests`, `ReadingAnnotationBackupTests` (round-trip, tombstone suppression, LWW re-home, multi-device same-passage dedup), `SearchFiltersTests.aBookmarkIDSurvivesARealSwiftDataRoundTrip` | Cross-device restore on real hardware |
| Folder sync | Two-device convergence; conflict folding; dirty-flag; gate reentrancy; failed write preserves package; skip-unchanged + re-restore on change; a folded File-Provider conflict's restore summary counts toward the displayed sync result instead of being discarded | `FolderSyncTests` (incl. the two T-73 tests), `FolderSyncBackgroundTaskTests` (shouldSchedule) | Real iCloud Drive propagation, quota/offline UX, BGTask firing (Xcode "Simulate Background App Refresh"), a genuine multi-device File-Provider conflict actually folding with correct on-screen counts |
| Reinstall/update survival | EPUBs in App Support (device backup); Keychain session survives; folder bookmark dies but package survives → re-pick folder restores; index/migration self-heal via version stamps | `PersistenceSyncTests` (migration stages) | Actual reinstall walkthrough |
| EPUB import + enrichment | `importUserEPUB` dedup/restore outcomes; `importEPUB` merge-not-duplicate; both import matchers resolve identity through the shared `WorkIdentityIndex` (URL-variant merge + exact non-AO3 source merge covered); metadata/tags seeded from EPUB then enriched from AO3; imports searchable immediately; `WorkDetailView`'s remote-work resolution goes through the same shared `ReadingQueueService.resolveLocalWork` as the card/bulk-action paths; reviving a soft-deleted work whose EPUB file is genuinely missing does not silently mark it readable — `WorkReaderPreparation.hasReadableEPUB`/`.restoreReadableEPUB` still self-heal at read time | `EPUBTests` (incl. sample.epub round-trip), `PreservedWorkTests`, `WorkSearchIndexTests` | Settings → Import EPUB picker actually presenting (see T-73 note) |
| Reader progress | iOS: debounced locator writes (`ReadiumProgressPersistence`, ~2s / delta) + mandatory flush on dismiss/disappear/background; session-open + flush stamp Continue Reading; mid-session writes do not thrash `lastReadDate`/`lastModifiedAt`; first post-seed *different* locator can write (string gate while progression baseline is nil). Visual→position remaining + scrub progression pure helpers. Page bar is swipe-scale only (`pageBarReady`/`visualPage`/`visualPageCount`): first publish and any vertical-count shrink both require ≥2 agreeing layout samples — unconditionally, even on the last scheduled remeasure tick, which instead extends into bounded slower-cadence retries rather than ever unmasking an unconfirmed count. macOS: `ReaderProgressBridge` debounce + flush. Force-quit loss ≤ debounce window if no background flush. | `SavedWorkProgressTests`, `ReadiumProgressPersistenceTests`, `ReaderProgressBridgeTests`, `ReadiumReaderTests`, `ReaderPageMetricsTests`, `ReaderChapterScrubTests`, `ReaderTimeEstimateTests` | Kill-app-mid-chapter (scrolled + paged); long scrolled session hitch check; macOS resume after mode switch; device smoke of the page-bar dual-agree/shrink machinery (open, chapter next/back, same-chapter turn, scrub) — state-machine timing isn't unit-testable |
| Reader TTS (iOS) | Fan Read Aloud (`toggleReadingAloud`) keys off `isSessionActive` and calls `ReaderSpeechController.toggle(from:)`. Engine is `SystemTTSService` (Readium `AVTTSEngine`) unless a selected Kokoro pack is ready, then `SherpaKokoroTTSService`. Int8 is the normal optional download; official FP32 is a developer side-load benchmark, falling back to installed Int8 or Apple until the app fingerprints and marks the complete FP32 runtime. Unknown model/provider preferences safely resolve to Int8/CPU. Core ML is an experimental ONNX Runtime provider request, with a CPU creation fallback; never treat it as guaranteed Neural Engine execution. Missing/unverified pack must still speak via the system voice (not a Display-sheet no-op). Rate/pitch apply on next chunk; Now Playing while playing/paused; remote play/pause/next/previous/stop; explicit Stop clears Now Playing **and** remotes; speech continues when backgrounded with reader open; `tearDown` on leave work; mini-player `<<`/`>>` chapter skip (hold-seek uses `ReaderPageMetrics`, not `page - 1`) | `ReaderSpeechPreferencesTests`, `ReaderTTSEngineKindTests`, `KokoroModelConfigurationTests`, `ReaderSpeechSkipTests`, `ReaderPageMetricsTests` (hold composition), `ReaderTimeEstimateTests`, `TextChunkerTests` | Fresh install: tap Read Aloud — system voice speaks. Download Int8 and confirm next tap uses Kokoro. Select FP32 with neither pack (Apple must remain active); then side-load/verify the official FP32 pack per `KOKORO_FP32_BENCHMARK.md`, switch CPU/Core ML in Settings, and measure cold load, real-time factor, gaps, thermals, and battery; use Instruments/logs to confirm actual Core ML behavior. Lock phone / Island controls; Stop → no ghost remotes; leave work → Now Playing gone; mid-chapter `<<` restarts; hold `>>` seeks |
| Reader highlights/notes/search (iOS) | Locator-string-only match (no text+position fallback); in-reader delete tombstones then hard-deletes; `WorkLifecycle.hardDelete` cascades tombstone+delete to every annotation on the work (no orphans); backup restore re-homes onto the resolved local work and collapses multi-device same-work+kind+exact-locator duplicates (§ Backup round-trip); Find in Work groups results by chapter, current chapter pinned first labelled "This Chapter" (+ honest Preface/Summary/Afterword/Ch. N suffix), remaining chapters ascending, using the same href→spine-index basis as the live position (not a separately derived chapter index that can drift); a pending search auto-pages ahead (bounded) until the current chapter is covered so a common term doesn't bury nearby hits behind the whole book's document order | `ReadingAnnotationMatchingTests`, `ReadingAnnotationBackupTests`, `ReaderSearchGroupingTests` | Highlight/recolor/delete/list on-device; Find in Work with a generic term ("the") vs a specific phrase, confirm This Chapter still surfaces promptly |
| Queues | Membership lifecycle, preservation, series preservation accounting, removal semantics | `ReadingQueueTests` (largest suite) | — |
| Write actions | Kudos/comment/Inbox POST construction, parsed CSRF/form fields, no-retry; work-page action states parse from live-captured fixtures (subscription delete-form ↔ subscribed, `/bookmarks/<id>` + `_method=put` edit form ↔ existing bookmark, prefilled values carried into the composer); bookmark save routes create-vs-update from the *fresh* work page at submit time, carrying collection names through untouched; Mark for Later posts the live-verified `button_to` shape (`_method=patch`) | `AO3WriteActionsTests` (incl. `ao3_work_bookmarked_subscribed`/`ao3_work_unbookmarked` fixtures), `AO3InboxParseTests` (construction only) | ⚠️ Write POSTs never live-verified — release gate item (owner: kudos/comment ✅ 2026-07-16; mark-for-later, bookmark save, subscribe-label refresh, block/mute confirm still owed) |
| Native comments | AO3 thread nesting/actions/icons/timestamps and canonical work creators parse from fetched HTML; `displayThreads` keeps full reply trees with stable unique IDs; Threads-style avatar spine inside one card per top-level conversation; large direct-reply stacks auto-collapse; Inbox standalone-thread opens resolve Parent Thread-or-self and exact chapter context; own-account comments resolve as Me by account path even under a different pseud; per-comment actions mirror AO3's live markup exactly (`data-remote` Edit/Delete links, `delete_comment?id=` href shape, `button_to` forms skipped) — Edit absent on replied-to/frozen/non-own comments is AO3's own rule (`comments_helper.rb`), not parser drift; duplicate-post guard remains intact; page/chapter caches, drafts, in-flight reads/writes/verifications, and mounted owner actions are account/session-generation isolated | `AO3CommentsParseTests` (including creator/coauthor + Me roles, Inbox focus resolution, displayThreads projection, spine geometry, and localized timestamp boundaries), `CommentSubmissionTests`, `CommentsAccountTransitionTests`, `ReaderSectionTests` (reader→chapter mapping) | Logged-in post/reply/edit/delete, deleted-parent placeholder state, large-thread interaction timing (expand replies + menu/highlight), Thread/Parent Thread scroll, and owner Light/Dark/Sepia screenshot gate |
| Native Inbox | Fixture-derived entries, identities, Guest/User/Author/Me role resolution, chapter labels, filter GET values, bulk-selection fields, and bulk request body; visible-page work ids deduplicate in screen order for sequential auth-scoped metadata hydration; all async loads/writes/enrichment/context caching become inert on an account/session-generation transition; same-user relogins use a generation-qualified private cache scope; the same full context drives immediate badges and the Comments work-summary card; per-row ellipsis actions use only parsed Inbox writes plus thread/chapter/copy utilities | `AO3InboxParseTests`, `AO3CommentsParseTests`, `AO3InboxAccountTransitionTests`, `AO3AuthorProfileTests` (session cache key), `AO3AuthServiceTests` (login/restore/logout cookie-transition fences) | Live Mark Read/Unread/Delete remains owner-gated; filter/select failure UX, theme/Dynamic Type/iPad/macOS screenshots |

## Known coverage gaps (acknowledged, not licenses to skip)

1. ~~`LibraryFilters.matches` has no dedicated unit suite~~ — closed by `LibraryFiltersTests` (T-74).
2. ~~Retry/pacing policies untested~~ — closed by `AO3ClientPolicyTests` over the now-internal `AO3Client.retryDelay` and pure `paceStep` (T-74). `withRetry`'s loop itself remains review-enforced.
3. SwiftUI presentation behavior (file importers, pickers, blur rendering) — logic-tested only; the TASKS.md UI approval gate covers it.

Mechanical invariants (single UA, one fileImporter, no `isDeleted` model property, index-out-of-backups, no raw AO3 URLSessions, staged package writes, no `try!`/`as!`) are enforced by `Scripts/check-invariants.sh`, which runs as step 1 of `Scripts/verify.sh`.

**When you add behavior in an area above, add the test to the named suite** — new suites need `@Suite(.serialized)` + registration in the schema helper only if they touch SwiftData/the gate.
