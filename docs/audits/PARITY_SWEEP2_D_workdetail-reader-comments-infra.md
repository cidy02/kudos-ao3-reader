# Parity Sweep 2D — Work Detail / Reader / Comments / Network / Data / UI

**Count: 6 new findings** (verification-only; no code changed)

**Method:** Read `docs/audits/ANDROID_PARITY_REPORT.md` + `docs/audits/ANDROID_PARITY_FINDINGS_VERIFIED.md` first and treated every fixed / confirmed-unfixed / refuted / known-deferred item as out of scope for re-report. Compared current Android under `android/app/src/main/java/io/github/cidy02/kudos/` (including this session’s WorkImporter tombstone revival, WorkUpdateChecker failure backoff, and CommentsScreen Network-error reload) against iOS at `.claude/worktrees/hig-review-reference/kudos-ao3-reader`.

**Explicitly not re-reported** (already covered or session-fixed): Work Detail metadata no re-fetch / no pull-to-refresh; blank bookmark dialog; queue `isSaved` + no auto-EPUB; WorkUpdateChecker failure backoff (now stamps `lastUpdateCheck`); WorkImporter revival tombstone retraction; comments no work header / no reply / no pagination / incomplete idempotency guard; AppNavHost shared selection state; reader skeleton theme flash; reader chrome missing author; custom font not applied to Readium; reader display customize depth / TOC polish; network-write broadly clean; Room JSON crash (fixed); theme Settings↔Material disconnect (fixed); biometric reveal (known deferred); confirmBeforeDelete unused (collections finding); DownloadQueue banner hidden in reader.

| Finding | Technical | Plain English | Confidence |
|---|---|---|---|
| Work Detail adopts soft-deleted rows as “local” and most actions mutate without revival | `WorkDetailScreen.kt` `RemoteSummary` resolve (`L202–215`) uses bare `workRepository.findBySourceUrl(source.summary.workUrl)` (DAO `getBySourceUrl` has **no** `isDeleted` filter), and `hydrateFromAo3WorkId` (`L158–165`) adopts any `WorkIdentityIndex` match the same way — so a Recently Deleted row is bound into `state.local`. iOS `WorkDetailView.resolveExistingIfNeeded` deliberately rejects pending-deletion matches (`localWork = match?.isPendingDeletion == true ? nil : match`, hig-review-reference `WorkDetailView.swift:568–572`). Android only special-cases revival on Save (`toggleSaved` `L414–424` → `restoreFromRecentlyDeleted`). `ensureLocalThen` (`L367–379`) runs Favorite / Finished / tags / queues / collections against the soft-deleted id with **no** restore (`onToggleFavorite`/`onToggleFinished` at `L614–625`, etc.), so `isDeleted` stays true while flags change. Reading an EPUB still on disk (soft-delete keeps the file) can write progress onto a still-deleted row. | If you deleted a work into Recently Deleted and later open that same work from Search/Browse/Home, Android can treat it as a normal library work again (Read, Favorite, queues, etc.) **without** actually restoring it. Favoriting or marking finished still leaves it on the 90-day purge clock and out of the real Library shelves — so actions look successful and then the work can still vanish when the timer ends. iPhone keeps showing remote/Save until you intentionally revive. | high |
| “Chapter Comments” is a miswired duplicate of work-level comments | `WorkDetailScreen.kt` Discussion tab (`L1270–1277`) shows a distinct “Chapter Comments” row, but `onClick = onComments` is the same callback as “All Comments” / “Write a Comment”. `AppNavHost.kt` always sets `selectedCommentTarget = AO3CommentTarget.Work(workId)` (`L126–128`, `L390–392`, `L435–437`). `AO3CommentTarget.Chapter` exists in `AO3CommentModels.kt` (`L26–42`) but has **no** navigation/UI path. iOS comments support chapter-scoped loads via chapter index + `chapterID` (`CommentsModel` chapter loader / By Chapter scope). | The Discussion tab advertises chapter comments as a separate destination, but tapping it opens the exact same whole-work comments screen as “All Comments.” There is no chapter picker and no chapter-scoped thread — the label is misleading. | high |
| Settings accent color is persisted but never applied to app chrome | `SettingsScreen.kt` exposes `AccentColorEditor` and writes `SettingsRepository.updateAccentColor` / DataStore key `accentColorHex` (`L301–306`, repository `L103–105`). `KudosTheme` / `Theme.kt` hard-codes `primary = Ao3Red` (`Color.kt:7`, `Theme.kt:26`) for Light (and fixed primaries for Dark/Sepia) and never reads `settings.app.accentColorHex`. Grep shows no other MaterialTheme/colorScheme consumer of the stored hex. iOS `ThemeManager.accentHex` / `effectiveTint` drives control/link tint app-wide (`ThemeManager.swift:27–61`). | You can pick a custom accent in Settings (and it survives restart/backup), but buttons, chips, and other “primary” chrome stay AO3 red forever. The control looks like it works; the app colors don’t change. | high |
| OLED theme chip is a storage alias for Dark — no true-black palette | `AppThemeSetting` has only Light/Sepia/Dark/System (`SettingsModels.kt:26–30`). Settings OLED chip maps to `AppThemeSetting.Dark` (`SettingsScreen.kt:282–283`, `L586`). `KudosThemeMode` has no OLED case (`Theme.kt:9–13`); Dark uses `SurfaceDark = 0xFF191817` (`Color.kt:14`), not pure black. iOS `ReaderTheme`/app theme includes a real `.oled` with true-black surfaces (`AppThemeSurface.swift`, `SemanticThemeColors.swift`). Footer copy even documents the alias (`SettingsScreen.kt:309–311`). | On iPhone, OLED is a distinct true-black look for AMOLED screens. On Android, tapping OLED only selects the same Dark theme as the Dark chip (one of the two chips highlights Dark), so you never get the deeper black chrome iOS users see. | high |
| WorkUpdateChecker still skips backoff when a “successful” fetch has blank chapters | Failure path now correctly stamps `lastUpdateCheck` (`WorkUpdateChecker.kt:36–40`, session fix). Success path still does `if (chapters.isBlank()) continue` (`L43–45`) **without** stamping, so `shouldCheck` (`L55–59`) treats that work as immediately due again on the next Home open. iOS stamps in the failure `catch` and only updates chapter fields on a usable payload; a blank/unusable chapters string should not bypass the polite interval forever. | If AO3 returns a page the app parses as “success” but with an empty chapters string (layout change, restricted scrape, partial HTML), Android will re-hit that work on every Home visit instead of waiting six hours — undoing part of the “polite update check” intent the failure-path fix just restored. | medium |
| Overflow “Subscribe” never reflects live AO3 subscription state | Work Detail overflow always labels the item `"Subscribe"` (`WorkDetailScreen.kt:803`) with no prefetched page-state. `AO3WriteRepository.toggleSubscribe` **does** parse subscription and unsubscribe when already subscribed (`AO3WriteRepository.kt:49–88`), but UI never surfaces `isSubscribed` the way iOS `AO3WorkActionsMenu` switches Subscribe ↔ Unsubscribe from `workPageStates?.isSubscribed` (`AO3WorkActionsMenu.swift:46–49`). (Distinct from the already-documented blank Bookmark composer: same missing work-page-state load, different user-facing control.) | If you’re already subscribed, Android still shows “Subscribe.” Tapping it quietly **unsubscribes**. Users who expect a sticky “Subscribe” action can remove a subscription they meant to keep, with no label or confirmation that the work was already subscribed. | medium |

## Areas re-checked with no *new* reportable gaps

- **Reader:** Open/progress/save, settings mapper → Readium adapter (except already-tracked custom font / full customize / chrome polish / skeleton theme flash). Tag links intentionally deferred (`ReaderLinkHandler` comment → external open).
- **Comments:** Session partial fix for `AO3Error.Network` (reload + message) does not fully match iOS `CommentSubmissionGuard`; residual still covered by the existing ambiguous-submit finding — not double-counted here.
- **Network read/write core:** Coalescing, 600 ms pacing, GET-only retries, redirect cookie relay, write status/body inspection — consistent with prior “reviewed clean / Android sometimes stricter” notes; no new correctness bugs found.
- **Room/DataStore:** Soft-delete schema, tombstone APIs, settings keys; no new crash/migration issues beyond already-documented unused settings (biometric, confirm-before-delete wiring elsewhere).
- **UI components:** `WorkCoverCard` / `AO3WorkCard` / stats hierarchy look intentional MD3 ports; no new behavioral parity defect beyond theme/accent above.

*Generated as Sweep 2D verification-only pass. Do not treat items already in `ANDROID_PARITY_REPORT.md` / `ANDROID_PARITY_FINDINGS_VERIFIED.md` as new.*
