# Android Parity Findings — Independent Verification

**Source:** Unverified findings section of [`ANDROID_PARITY_REPORT.md`](ANDROID_PARITY_REPORT.md) (68 items: 38 major, 12 minor, 18 note).

**Method:** For each finding, re-read the cited Android code under `android/` (package `io.github.cidy02.kudos`) and the real iOS counterpart in `.claude/worktrees/hig-review-reference/kudos-ao3-reader` (not any iOS tree vendored under `android/`). Verdicts reflect **current** code on this worktree tip, including fixes already landed from the report’s Fixed section.

**Verdict key:**
- **CONFIRMED** — gap/bug is real as described (or still real after allowing for minor citation drift).
- **REFUTED** — not a current bug as described (already fixed, wrong claim, or product impact no longer holds).
- **NEEDS-HUMAN** — ambiguous / product judgment, not a pure code fact.

## Summary

| Verdict | Count |
|---|---:|
| **CONFIRMED** | 63 |
| **REFUTED** | 5 |
| **NEEDS-HUMAN** | 0 |
| **Total** | **68** |

**REFUTED (5):** Settings theme picker disconnect (Fixed #9); Collection soft-deleted works leaking into membership UI (post-fix DAO filters); WorkImporter/WorkMetadataMerger “no tests” claim (tests now in `WorkLifecycleTest`); merger revival tombstone/backup re-suppress product bug (production paths call `restoreFromRecentlyDeleted`); collections hard-delete + false Recently Deleted copy (Fixed #6 soft-delete + updated copy).

**Highest-priority CONFIRMED majors (still open):** shared NavHost selection state (wrong work on back); WebView login host `endsWith` trust boundary; queue-add marks `isSaved` + no EPUB preserve; Library `isSaved` gates entire shelf set + no See All / filter UI; Search no local-first + `clearFilters` leaves stale results; custom font selected but not applied in Readium; WorkUpdateChecker failure never backs off; comments lack reply/pagination/idempotency.

---

## Verification table

| Area | Finding | Verdict | Justification |
|---|---|---|---|
| Account hub | Activity › Inbox is a static “not available yet” stub; no fetch path (`Inbox → null`) | CONFIRMED | `AccountActivityKind.Inbox` maps to `null` in `AccountUiState.kt`; `ActivityTabContent` shows `EmptyStateCard("Inbox not available yet")` in `AccountScreen.kt`. iOS has a full stack (`AO3InboxModel`, `AccountInboxViews`, inbox client). Also listed as a known tracked gap, but the code gap is real. |
| Account hub | “My Dashboard” is a static 6-row shortcut menu duplicating Overview, not a live profile | CONFIRMED | `AO3DashboardScreen.kt` is a fixed list of six `onOpenList`/`onOpenAO3Collections` rows. iOS `AO3DashboardView` pushes `AuthorProfileView` for the signed-in user with live fandoms/works/series/bookmarks. |
| Account hub | “Privacy & Local Data” navigates to the same Settings route as “Settings”; no PrivacyDataView controls | CONFIRMED | `AppNavHost.kt` wires `onOpenPrivacy` and `onOpenSettings` both to `Routes.Settings`. Android Settings privacy section is mature-content toggles only; no ads/analytics statement, Clear Browse Cache, Clear Reading History, or dedicated Remove Session surface matching iOS `PrivacyDataView`. |
| Account hub | “More on AO3” opens the user’s own profile WebView instead of curated destinations | CONFIRMED | Android Overview row opens `https://archiveofourown.org/users/<username>` via WebView. iOS `AccountMoreOnAO3View` lists ten creator/challenge destinations (Drafts, Pseuds, Skins, Statistics, Sign-ups, Gifts, etc.). |
| Account hub | No JVM tests for AccountViewModel / AccountListViewModel / AO3CollectionsViewModel / AccountUiState mapping | CONFIRMED | Only `AccountRepositoryTest.kt` under account tests; no ViewModel or `toAccountListType` / HubWorksPane filter tests. |
| App shell / navigation | Settings theme picker disconnected from `KudosTheme`; only unpersisted palette icon works | REFUTED | Fixed in current code (report Fixed #9). `KudosApp.kt` reads `settings.app.appTheme`, maps to `KudosThemeMode`, and drives `KudosTheme`; palette cycle writes `settingsRepository.updateAppTheme`. Comments document the old bug. |
| App shell / navigation | Flat NavHost + app-scoped `selectedWorkSource` (and siblings) show the wrong item when multiple same-route entries are on the back stack | CONFIRMED | `AppNavHost.kt` keeps single `remember { mutableStateOf }` selection vars (`selectedWorkSource`, `readerWorkId`, `selectedAuthorName`, etc.). `Routes.WorkDetail` re-reads the current shared value; Work A → Author → Work B → back twice still shows B. No typed per-entry args / `launchSingleTop` isolation like iOS `NavigationStack` path values. |
| App shell / navigation | `DownloadQueueBanner` hidden while route is Reader; iOS keeps it over the reader | CONFIRMED | `MainScaffold.kt` wraps banner in `if (!isReader)`. iOS `ContentView` overlays `DownloadQueueBanner` on the tab container regardless of reader presentation. Progress still runs; only visibility differs. |
| App shell / navigation | `NavigationRoutesTest` only covers static `Routes` data, not AppNavHost/theme wiring | CONFIRMED | Test asserts titles/top-level destinations only; no coverage of shared selection state or Settings→theme wiring. |
| Authentication | Login WebView host check uses bare `endsWith(WORKS_HOST)` (suffix superstring hosts allowed) | CONFIRMED | `AO3WebLoginScreen.kt` `shouldOverrideUrlLoading`: `!host.endsWith(AO3Constants.WORKS_HOST)` — `evilarchiveofourown.org` would load. `AO3CookieJar` / `AO3StoredCookie` correctly require `==` or `endsWith(".${WORKS_HOST}")`; iOS `isTrustedURL` uses the same boundary. Only this call site is wrong. |
| Authentication | `restoreSession()` can crash on launch if session file I/O throws outside SerializationException | CONFIRMED | `AO3AuthRepository.restoreSession()` calls `sessionStore.load()` without try/catch. `FileAO3SessionStore.load()` only catches `IllegalArgumentException`/`SerializationException`; `IOException` can escape into `AccountViewModel`’s uncaught coroutine. iOS wraps vault load in do/catch → signedOut. |
| Authentication | Web login completion requires scraped username; no fallback if logged-in markup lacks it → silent stall | CONFIRMED | `onPageFinished` only calls accept when `loggedIn && username != null`. No native expected-username fallback. iOS `completeAuthentication(username: observation.username ?? expectedUsername)`. |
| Authentication | No direct unit tests for `acceptWebLogin()` / `sessionDidExpire()` | CONFIRMED | Auth tests cover restore/logout/cookie/parse paths; no direct exercise of web-login completion or mid-session expiry entry points. |
| Author profile | Author surface is search-by-name works list only; no full profile (bio, Subscribe/Mute/Block, Series/Bookmarks/About, pseuds) | CONFIRMED | `author/` is only `AuthorWorksScreen.kt` (creator search + paginated cards). iOS `AuthorProfileView` + services provide hero, actions, tabs, fandom filter, pseud switcher. Known tracked gap; still accurate. |
| Author profile | No tests for AuthorWorksScreen loading/error/pagination state machine | CONFIRMED | Only `AO3AuthorUrlsTest` / `AuthorTapNamesTest`; no author package screen/state tests. |
| Browse | Fandom list shown in AO3 alphabetical order; iOS re-sorts by work count descending | CONFIRMED | Android renders parser order with no sort (`FandomListScreen` / `parseFandomList`). iOS `FandomListView` sorts `workCount` desc (“Surface the biggest fandoms first”). |
| Browse | Fandom works screen has no filter/sort UI; always default RELEVANCE | CONFIRMED | `FandomWorksScreen` only pages `worksForFandom`; repository builds filters with fixed sort. iOS `FandomWorksView` hosts `AO3FilterPanel` + clear-filters recovery. |
| Browse | `parseFandomList` full Jsoup DOM on huge media pages; iOS uses linear string scan (BUG-5 memory fix) | CONFIRMED | Android `Jsoup.parse` + `select("ol.fandom.index li")`. iOS `parseFandomIndex` is an intentional non-DOM scan to avoid multi-MB DOM / jetsam. |
| Browse | Fandom name filter recomputes every keystroke, no debounce; no diacritic folding | CONFIRMED | Inline `contains(..., ignoreCase = true)` on full list in composition. iOS uses normalized index + 120ms debounce; “pokemon” vs “Pokémon” gap is real. |
| Browse | In-app WebView fallback has no `setDownloadListener`; EPUB downloads from page links silently drop | CONFIRMED | `AO3WebViewFallbackScreen` never sets a download listener (repo-wide none). iOS `BrowserModel`/`WKDownloadDelegate` intercepts epub and imports. Mitigated by native Work Detail download path. |
| Browse | No tests for WebViewFallbackScreen wiring or FandomList/Works composable logic | CONFIRMED | Policy/parser/repository tests exist; screen wiring untested. |
| Comments | Comments screen has no work title/author/fandom header; models lack work context fields | CONFIRMED | Loaded UI is composer + flat list; top bar title static “Comments”. `AO3Comment`/`AO3CommentThread` have no work metadata. iOS `infoSection` shows title/author/fandom/stats via `AO3CommentsWorkContext`. |
| Comments | Comment POST has no idempotency / ambiguous-failure guard; retry after timeout can duplicate | CONFIRMED | Single POST; on failure draft preserved and button re-enabled. No verification GET / submission guard. iOS `CommentSubmissionGuard` + reverify suite. |
| Comments | No per-comment Reply; submit has no parent id — nesting is display-only | CONFIRMED | `CommentRow` is read-only indent; `submitComment(target, content)` posts top-level only. iOS reply composer + `parentCommentID` path. |
| Comments | Only first comments page ever loaded; no page query param or pagination UI | CONFIRMED | `pageUrl()` omits `page`; `loadThread` once; no totalPages fields/UI. iOS tracks pages and shows Previous/Next when `totalPages > 1`. |
| Comments | Comment tests lack timeout/ambiguous submit, multi-page, reply/edit/delete | CONFIRMED | Repository/parser tests cover load + happy POST + one validation error only — consistent with missing features. |
| Download / import | No share/Open-with intent filters; no external file sniff/convert/import path | CONFIRMED | Manifest is MAIN/LAUNCHER only; no ACTION_VIEW/SEND for fic mimes; no Android equivalent of iOS document import pipeline. Work import is AO3 EPUB download only. Known deferred non-EPUB design; external open-in still absent. |
| Download / import | No tests for WorkImporter/WorkMetadataMerger; DownloadQueueTest never seeds soft-deleted rows | REFUTED | Lead claim false: `WorkLifecycleTest.kt` includes `WorkMetadataMergerTest` and `WorkImporterLifecycleTest` (merge, revival, e2e importer + tombstone). Residual still true: `DownloadQueueTest` does not seed `isDeleted = true` rows. |
| Home | No queue-only flag; queue add via `saveMetadataOnly(markSaved=true)` makes full Library items; iOS excludes queue-only from Home | CONFIRMED | `SavedWork` has no `isQueuedForLater`. `WorkImporter.saveMetadataOnly` always `markSaved = true`; Work Detail queue actions use `ensureLocalThen`. iOS sets `isSaved = false` for queue-created works and filters `isQueueOnlyWork` out of Home sections. |
| Home | Subscriptions shelf renders raw remote cards with no merge against local library progress | CONFIRMED | `SubscriptionsShelf` → `RemoteWorkCover` from `accountListRepository` only; no `CanonicalWorkMerge`. iOS merges remote subscriptions with local library for progress/privacy/local open. |
| Home | WorkUpdateChecker failure path does not stamp `lastUpdateCheck` (no 6h backoff on fail) | CONFIRMED | `AO3Result.Failure` → `continue` without upsert (`WorkUpdateChecker.kt`). iOS catch stamps `lastUpdateCheck` so failures wait `minInterval`. Docstrings claim the same polite throttle. |
| Home | Favorites / Recently Opened open paths skip `markUpdateSeen`; Continue Reading / Recently Updated do not | CONFIRMED | Only Continue Reading and Recently Updated wrap with `viewModel.onOpenLocalWork` → `markUpdateSeen`. Favorites/Recently Opened pass bare open callbacks. |
| Home | No HomeViewModel unit tests (only HomeDashboardTest) | CONFIRMED | `home/` tests are only `HomeDashboardTest.kt`; no VM soft-failure / sign-in / mark-seen coverage. |
| Library | `observeSavedWorks` filters `isSaved == true`, so unsaving removes a work from every Library shelf | CONFIRMED | `WorkRepository.observeSavedWorks` / `listSavedWorks` filter `isSaved`; Library snapshot is built only from that stream. Unsave has no confirm. iOS `@Query` is all non-deleted works; `isSaved` only affects Saved for Later section predicate. |
| Library | Every shelf capped at 12 with no See All; no per-section full list | CONFIRMED | `ShelfLimit = 12` on work carousels; `onSeeAll` not passed for Reading Now / Saved / Finished / Downloaded / History. iOS pushes `LibrarySectionListView` via section kind. |
| Library | Full `LibraryFilterState` / sort API exists but UI only wires fandom chips + Reset | CONFIRMED | ViewModel has sort/favorite/finished/download/tag/collection setters; `LibraryScreen` only uses fandom filter + clear. No iOS-style filter inspector. |
| Library | LibraryRepositoryTest minimal; locks isSaved-gates-all behavior; no reveal-all tests | CONFIRMED | Single repository test asserts unsaved works absent from snapshot; no privacy reveal-all / unsave-vs-remove behavioral tests. |
| Collections | Soft-deleted works still listed/counted in collections (DAO joins lack `isDeleted = 0`) | REFUTED | Fixed for UI: `getWorksForCollection` filters `works.isDeleted = 0`; counts use `getActiveWorkIdsForCollection` (also filtered). `getWorkIdsForCollection` remains unfiltered **on purpose** for backup export (commented). Detail screen uses filtered path. |
| Collections | No Add Works on collection detail; Work Detail “Add to Collection” is free-text name only | CONFIRMED | Collection empty state directs to Work Detail; Work Detail dialog is type-a-name, not checklist of existing collections. Library card menu *does* list existing collections — reverse paths still weaker than iOS dual-direction UX. |
| Collections | Collection Remove has no confirmation; `confirmBeforeDelete` unused outside Settings/backup | CONFIRMED | `CollectionWorkRow` removes immediately. Grep shows `confirmBeforeDelete` never read by delete/remove UI. iOS gates via `@AppStorage("confirmBeforeDelete")`. |
| Collections | Sparse collections tests; no soft-deleted-work exclusion assertion | CONFIRMED | Lifecycle tests cover soft-delete of collections; no dedicated assertion that soft-deleted **works** are excluded from membership UI queries. |
| Reader | Opening skeleton / outer Surface use app `MaterialTheme` background, not reader theme → flash when themes diverge | CONFIRMED | `ReaderPageSkeleton` and `ReaderScreen` Surface use `MaterialTheme.colorScheme.background`. Reader can set independent theme via Display sheet (`matchAppReaderTheme = false`). iOS skeleton uses reader theme background. |
| Reader | Reader top bar shows title only; omits author | CONFIRMED | `ReaderTopBar` takes/renders `title` only; call site passes `state.work.title` not author. iOS chrome shows author line. |
| Reader | Display sheet only font size + theme; full customize (spacing/margins/fonts/live preview) deferred | CONFIRMED | `ReaderDisplaySheet` is size slider + Light/Sepia/Dark. Prefs/adapter fields exist but no UI setters. Matches HANDOFF known incomplete reader settings UI. |
| Reader | No unit tests for ReaderPageSkeleton fill arithmetic | CONFIRMED | No `ReaderPageSkeleton` tests under Android `src/test`; iOS has dedicated skeleton tests. |
| Reading queues | Queue add marks work `isSaved = true`; remove only drops membership (no queue-only cleanup) | CONFIRMED | Same root as Home queue finding: `ensureLocalThen` → `saveMetadataOnly(markSaved=true)`. `removeWork` deletes membership (+ tombstone) only. iOS creates queue-only with `isSaved = false` and `removeFromQueueAndDeleteIfQueueOnly`. |
| Reading queues | `addWork` / `addToSavedForLater` do not trigger EPUB download/preserve | CONFIRMED | Repository only inserts membership rows; no `WorkImporter.download` / `DownloadQueue` from queue add sites. iOS `addAndPreserve` downloads EPUB. |
| Reading queues | ReadingQueueRepositoryTest misses createQueue dedup/sortOrder coverage | CONFIRMED | Tests cover SFL ensure/add/remove/list; not `createQueue` name-dedup or sort-order assignment. |
| Reading statistics | `ReadingStatistics.hasStarted` omits `readiumLocator` while model/`CategoryStats` include it | CONFIRMED | `hasStarted` checks finished/lastReadDate/spine/scroll only. `SavedWork.hasStartedReading` and `CategoryStats.hasBeenRead` include locator. iOS defers to model `hasStartedReading` after this exact drift. Reachable via backup restore with locator-only progress. |
| Reading statistics | `formatCompactNumber` can emit “1000.0K” near 999950 due to round-then-suffix | CONFIRMED | K-bucket for `abs < 1_000_000` then `%.1f` of `/1000` → 999950 → “1000.0K”. iOS Foundation compact notation avoids this. |
| Reading statistics | Missing `readiumOnlyWorkCountsAsStarted` regression test | CONFIRMED | `ReadingStatisticsTest` has no readium-only case; test helper cannot pass locator. Would fail given hasStarted bug. |
| Reading statistics | Missing readiumOnly test + no privacy-filter tests for stats pipeline | CONFIRMED | Still no readiumOnly or privacy unit tests. Privacy filtering **is now implemented** in the statistics ViewModel (Fixed #4) — report’s “filtering not existing” subclaim is outdated; the **test gap** remains. |
| Recently Deleted | Merger revival clears soft-delete fields but not tombstone/`lastModifiedAt` → backup can re-suppress | REFUTED | Pure merger still only clears delete fields (by design). Production paths fixed: `WorkImporter.reviveIfNeeded` → `restoreFromRecentlyDeleted` (retracts tombstone + bumps `lastModifiedAt`); DownloadQueue and Work Detail save paths also restore. Covered by `WorkImporterLifecycleTest`. Product consequence no longer holds. |
| Recently Deleted | Collections hard-deleted; queues not soft-deletable; copy promises 90 days for collections/queues | REFUTED | Collections soft-delete exists (`softDeleteCollection`, Recently Deleted list/restore/purge). UI copy now says “works and collections…90 days” only (no queues claim). Residual: reading-queue soft-delete still not implemented in repository/UI (schema may have flags, no product path) — not the original hard-delete/false-copy claim. |
| Search | No local-first as-you-type results (library/fandoms/tags/collections); network-only on submit | CONFIRMED | Typing only mutates filters; Idle shows saved searches/prompt; search button hits AO3. No WorkSearchIndex/local results list. iOS debounced local matches + “Search AO3 for…”. |
| Search | `clearFilters()` does not reset `SearchUiState`; stale Results remain on screen | CONFIRMED | `clearFilters` only replaces `filters` via `clearedFiltersPreservingQuery`; never touches `state` or re-runs search. |
| Search | Duplicate local-first claim: typing does nothing until Search; no on-device library match layer | CONFIRMED | Same root as local-first finding above; independently re-verified on `SearchScreen` Idle/query wiring. |
| Search | `TagSuggestField` is local-library candidates only; no AO3 autocomplete/popular tags | CONFIRMED | Documented local-only; candidates from `collectLocalTagSuggestions`. No Android `autocompleteTags`/`popularTags` client. iOS TagSelectField hits live AO3. |
| Search | Minor restatement: Clear / Clear all leaves results stale vs filters | CONFIRMED | Same `clearFilters` body as the major clear finding; still true. |
| Search | No SearchScreen state-machine tests (run/retry/clear/save dialog) | CONFIRMED | Helpers/codec/saved-search tested; no SearchUiState transition tests. |
| Settings | Custom font import/select works in Settings but Readium never applies fontFamily | CONFIRMED | Import + selection persist; `ReaderSettingsMapper` maps font id; `ReadiumSettingsAdapter` documents custom fonts NOT applied and omits fontFamily when building `EpubPreferences`. iOS embeds custom font via stylesheet. |
| Settings | Report a Bug is bare mailto to noreply address with no body/diagnostics; iOS GitHub prefilled form | CONFIRMED | `ACTION_SENDTO` to `cidy02@users.noreply.github.com` with subject only. iOS `BugReportView` + `AppLinks.newIssue`. |
| Settings | “Reset settings to defaults” has no confirmation; wipes DataStore immediately | CONFIRMED | Button calls `resetToDefaults()` → `dataStore.edit { clear() }` with no dialog. Android-only control, but destructive and unguarded as described. |
| Settings | Missing Preservation / Queue Storage settings; auto-preserve-series only in backup schema | CONFIRMED | Settings Library group has confirm-before-delete only. `autoPreserveSmallSeries*` lives on backup payload, not live prefs/UI. iOS has Preservation + Reading Queues settings. |
| Work Detail | WorkUpdateChecker failure doesn’t stamp lastUpdateCheck (re-check every Home open) | CONFIRMED | Same code path as Home minor: Failure → continue without stamp (`WorkUpdateChecker.kt`). |
| Work Detail | Local saved work never re-fetches AO3 metadata; no pull-to-refresh; Redownload is EPUB-only | CONFIRMED | `LocalWork` → `refreshLocal` DB only; no network refresh/pull-to-refresh. Redownload enqueues EPUB via DownloadQueue/`downloadExisting` without metadata merger refresh of tags/stats. |
| Work Detail | Bookmark dialog always blank / always “Bookmark on AO3”; never loads existing bookmark | CONFIRMED | Dialog state is blank `remember` locals; menu label fixed; always `createBookmark`. iOS prefills from `existingBookmark` and switches label to Edit. |
| Work Detail | `WorkTags.kt` has zero dedicated unit tests | CONFIRMED | No `WorkTagsTest`; only incidental use via identity/URL helpers in other tests. iOS has `WorkTagsTests`. |

---

## Notes for follow-up

1. **Duplicates in the original list are intentional.** Search local-first appears twice (major+major) and clearFilters twice (major+minor); WorkUpdateChecker failure appears as both Home minor and Work Detail major; queue `isSaved` appears under Home and Reading queues. Each row was judged independently; all still hold.
2. **Known tracked gaps ≠ REFUTED.** Inbox, full author profile, non-EPUB external import, and deep reader customize remain accurately described absences even when already scheduled elsewhere.
3. **iOS comparison root used:** `.claude/worktrees/hig-review-reference/kudos-ao3-reader` (+ `KudosTests` for regression-test claims).
4. **No source files were modified** for this verification; this document is the sole output.

*Generated as an independent re-verification of the Unverified findings in `docs/audits/ANDROID_PARITY_REPORT.md`.*
