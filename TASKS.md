# TASKS.md — Kudos task board

Shared, lightweight task board for all AIs + the human. **Read [`AGENTS.md`](AGENTS.md) first.**
Update this file whenever you start, finish, or hand off work — it is the primary
handoff channel between sessions and between agents.

## How to use it (30 seconds)

- **Claim** a task → move it to **In Progress**; set `Owner`, `Branch`, status `🔄`.
- **Finish** → move it to **Completed** with the commit SHA(s) + date.
- **Hand off / block** → leave it in **In Progress** with `✋ HANDOFF` or `⛔ BLOCKED`
  and a clear **Next step** / **Blocker**.
- Keep entries short; link to `docs/…` or `READIUM_MIGRATION_NOTES.md` for detail.
- Task IDs are `T-NN` (just for cross-reference; pick the next free number).

**Status:** 🔄 in progress · ✋ handoff (ready for pickup) · ⛔ blocked · ✅ done · 🅿️ backlog

---

## 🔄 In Progress

| ID | Task | Owner | Branch | Status | Next step / notes |
|----|------|-------|--------|--------|-------------------|
| T-122 | **App version shows the build's commit SHA** — Settings → About (and bug reports, which reuse `AboutView.versionString` in `BugReportView`) now display `1.0 (1) · <short-sha>`. Implementation: the `project.pbxproj` script phase formerly named "Add Background Modes to Info.plist (iOS only)" was widened into **"Inject Info.plist keys (git SHA + iOS background modes)"** — it stamps a `GitCommitSHA` key (`git -C "$SRCROOT" rev-parse --short=7 HEAD`) into the processed Info.plist on **all platforms** (background-modes injection stays iOS-only inside a `case "${PLATFORM_NAME}"` guard; it also now writes via `${TARGET_BUILD_DIR}/${INFOPLIST_PATH}` instead of `${CODESIGNING_FOLDER_PATH}/Info.plist`, which was iOS-only-correct). When git or the repo is unavailable (e.g. source-tarball build) the script warns and skips, the key is simply absent, and `versionString` falls back to the old `1.0 (1)` format. A second Info.plist-writing phase was not an option — two phases can't both declare the processed plist as an output. **⚠️ Required flipping `ENABLE_USER_SCRIPT_SANDBOXING` YES→NO (both project-level configs) — owner veto welcome:** the sandbox whitelists only *literal* declared input paths, but git reads branch-dependent loose refs + pack files under `.git/` that can't be enumerated statically. Proven empirically by replaying Xcode's generated `.sb` profile via `sandbox-exec` against a plain clone: `git rev-parse HEAD` dies with `fatal: Needed a single revision`; it only worked in this git *worktree* by accident (a worktree's real gitdir lives outside `SRCROOT`, i.e. outside the sandbox's `deny file-read*` subpath rules — so worktree-only testing would have masked the breakage in normal checkouts). All script phases here are first-party, so the lost guardrail is minimal; if unacceptable, the alternative is an unsandboxed helper aggregate target that writes the SHA to a derived file the (re-sandboxed) main phase declares as input. | Claude | `claude/app-version-commit-sha-427004` | ✅ DONE | **Verified:** iPhone 17 (iOS 26.5) sim build injects `GitCommitSHA=a021573` and PlistBuddy confirms it in the built `Kudos.app/Info.plist`; build log shows zero `sandbox-exec` wrappers post-flip; macOS build log shows the same injection line. `Scripts/verify.sh` **ALL GREEN** — invariants, lint, iOS **610 tests / 57 suites**, macOS Debug build, whitespace. Not runtime-screenshot-verified in the About sheet UI (text-only change to an existing `Text` line). |
| T-121 | **HIG Wave 2 review-fix round** — fixed all 4 findings from an owner-supplied adversarial review of `77c9649` (T-120), per the owner's standalone task prompt (`~/Downloads/HIG_Wave2_Review_Fixes_Prompt.md`, gitignored). **Finding 1 (two 44pt surfaces silently regressed to 28pt) — fixed one, documented the other's deviation.** `AuthorProfileContentSections.swift`'s fandom-filter chips ("All" + per-fandom, lines ~163-181) restored from `.minimumHitTarget(28)` back to `.minimumHitTarget()` (44pt) — this bar is a single horizontal `ScrollView` row, not a wrapping `FlowLayout`, so the overlap risk that motivated the 28pt floor elsewhere doesn't apply here, and 44pt is what this bar already had pre-Wave-2. `WorkDetailSections.swift:53-63`'s Tags-card chips (a true `FlowLayout`, `rowSpacing: 4`) kept at 28pt — re-inflating to 44pt there risks the row-overlap problem — but T-114's row (Completed table) now has an explicit correction: F4's "≥44pt targets" claim no longer holds for these chips, superseded by this decision. **Finding 2 (AO3AuthorNavigation gesture-order risk) — fixed by construction, NOT runtime-verified.** `UIComponents/AO3AuthorNavigation.swift`'s tappable byline `Text` had its `.frame(minWidth: 28, minHeight: 28, alignment: .top)` + `.contentShape(Rectangle())` moved to apply *before* `.highPriorityGesture` is attached (previously after), removing the ambiguity the review raised — the gesture is now scoped to the full 28pt region from the moment it exists, rather than relying on an unverified assumption that a later outer `.contentShape` retroactively extends an already-attached gesture (a different, unverified mechanism from the Button+`.minimumHitTarget()` case T-119 empirically proved). The original small `.contentShape(Rectangle())` that used to sit before the gesture is now redundant and was removed (the new earlier, larger one supersedes it). **Runtime verification (tap-margin test, keyboard-focus test) could not be completed**: three separate attempts across this round and the T-120 implementation itself were all blocked by the same stuck `axAuditService` system click-interception state in Accessibility Inspector (see T-119/T-120 for the first two occurrences) — confirmed still stuck on a fresh attempt this round (a fresh screenshot showed no visible stuck-state indicator, but the very first click was rejected by the tool with "would land on axAuditService"). This is now a 3-for-3 pattern across three separate review rounds; treat as a standing environmental limitation, not a one-off. **Finding 3 (ReorderHandle claim corrected) — done**, see the T-120 row above (now annotated in place) and `Features/Library/ReadingQueues.swift`'s `dragHandle(for:)` comment, both corrected to state the `.minimumHitTarget(28)` call is a geometric no-op (the handle's `.padding(6)`-wrapped effective region was already ~40pt) kept only as a self-documenting floor; the real UI-3/A9-F2 fix is the 4 accessibility actions. **Finding 4 (bookkeeping) — done.** (1) Roadmap doc's Wave 2 section reworded so A6-F2 reads as "not a Wave-2 item after all, still open on Track B3" rather than skimmable as resolved. (2) The pending VoiceOver-pass gate (`docs/audits/Fixes_and_HIG_Implementation_Plan.md` Human Review Gates §6) now explicitly asks the owner to confirm the `CardNavigationModifier` design's accepted double-title announcement (row content + the labeled "open" stop) reads as tolerable, not confusing. (3) Branch Strategy section now has an explicit callout: Wave 2 touched files across Search/Library/WorkDetail/Account/Authors, not just `UIComponents/`, so Waves 3-7 must cut from the tip carrying T-120+T-121, never from `775ed22`. (4) Added: `ReadingQueueService.moveOffsets(currentIndex:requestedIndex:count:)` — extracted the pure index arithmetic out of `ReadingQueueDetailView.moveWork(_:toIndex:)` (same forward/backward `toOffset` convention, same boundary-clamp no-op contract) so it's directly unit-testable without a live UI/`ModelContext`; `moveWork` now calls it. 8 new tests in `KudosTests/ReadingQueueTests.swift` pin forward/backward moves and all 4 no-op boundary cases (already-at-index, past-the-end clamp, before-the-start clamp, empty queue). | Claude | `claude/hig-wave2-review-fixes-77c9649` | ✅ DONE | **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (only the same pre-existing `ReadingQueues.swift` file-length warning), iOS test suite incl. the 8 new `moveOffsets` tests, macOS Debug build, whitespace. **Remains manual (owner) — unchanged from T-120, still open:** AO3AuthorNavigation's tap-margin/keyboard-focus runtime check (Finding 2, now blocked 3 times by the same tooling wall — **if the Simulator looks unresponsive to clicks, a direct click from your own mouse clears it**), WorkCarouselSection's header-height growth, TagChip's `FlowLayout` reflow at 28pt, Wave 1's screenshot gate, T-119's Finding-2 VoiceOver spot-check. **New:** a screenshot of the restored 44pt fandom bar (Finding 1) for the owner gate, per the review's own verification bar. |
| T-120 | **HIG Wave 2 — `UIComponents/` sweep.** Applied X1/X5/X6 across the shared components per the T-116 roadmap, per 6 owner decisions on genuine product/design questions the research surfaced (floor sizes, whether to fix-not-defer the highest-risk item, scope of two "bundle both controls" calls, keyboard-focus strategy, reorder-action completeness, and leaving one finding's styling alone). Research phase: 6 parallel agents deep-read the actual current code for TagChip, CardNavigationModifier, ReorderHandle+A9-F2, WorkCarouselSection's see-all chevron, AO3AuthorNavigation+A9-F4, and WorkCardListControls before any owner decision was asked for. **TagChip** (`UIComponents/TagChip.swift` untouched; fix is all at call sites) — restored the missing `.isSelected` VoiceOver trait at `AuthorProfileContentSections.swift`'s fandom-filter chips (parity with `LibraryView.swift:443`'s identical chip) and applied `.minimumHitTarget(28)` (owner-chosen floor, not the 44pt default, since chips render back-to-back in dense `FlowLayout`/horizontal-scroll groups) to all 6 Button-wrapped call sites (`WorkRow.swift`, `AO3WorkRow.swift`, `MediaBrowserView.swift`, `LibraryView.swift`, `WorkDetailSections.swift` ×2 — the last replacing a hand-rolled, non-last-applied `.frame+.contentShape` that predated the Wave-1 helper). **WorkCarouselSection** (`UIComponents/WorkCarouselSection.swift`) — `.minimumHitTarget()` (44pt default; isolated header buttons, not a dense flow) added to BOTH the collapse-toggle and the "see all" chevron, per owner decision to bundle rather than fix only the worse offender; the collapse button was previously assumed "already close to 44pt" in T-118's notes, which fresh analysis showed doesn't hold (~28-29pt). **ReorderHandle** (`UIComponents/ReorderHandle.swift`, `Features/Library/ReadingQueues.swift`) — `.minimumHitTarget(28)` on the drag handle (zero blast radius, its only consumer) **— corrected 2026-07-19, T-121 review-fix round: this is a geometric no-op, not an enlargement.** `ReorderHandleView` is already 28×28pt and the `.padding(6)` wrapping it in `dragHandle(for:)` already grows the drag source to ~40pt in each dimension, comfortably above the 28pt floor before this call was ever added — the real UI-3/A9-F2 fix for this handle is the accessibility-action work below, not this modifier. Kept as an explicit, self-documenting floor (see `ReadingQueues.swift`'s updated comment) rather than removed. New `moveWork(_:toIndex:)`/`currentIndex(of:)` helpers reusing `ReadingQueueService.reorder`, exposed as 4 `.accessibilityAction(named:)` custom actions (Move Up/Down/to Top/to Bottom, owner chose "more complete" over just Up/Down) on the compact-grid's reordering-mode card — the only genuinely inaccessible reorder path (the default `detailedList` mode already uses `List`+`.onMove`+`EditMode`, which VoiceOver drives natively); `ReorderHandleView`'s hint updated to point at the Actions rotor instead of describing a drag VoiceOver can't perform. **AO3AuthorNavigation** (`UIComponents/AO3AuthorNavigation.swift`) — hit-target: `.frame(minWidth: 28, minHeight: 28, alignment: .top)` (NOT the shared `.minimumHitTarget()`, which centers vertically and would reproduce the file's own documented "dropped it below 'by'" baseline-alignment regression — the `.top` alignment keeps the growth downward-only, off the baseline) on each tappable co-author name. Keyboard focus (A9-F4, owner chose strategy (a)): added `.focusable(true)` + `@FocusState` + `.onKeyPress(keys: [.return, .space])` + a manual focus-ring overlay, applied uniformly rather than converting to a real `Button` — a real Button was tried at this exact spot before and reverted (its tap leaked through and double-navigated under the app's `cardNavigation` background-link pattern); the file's own comment now explicitly warns against reintroducing one. **CardNavigationModifier** (`UIComponents/AppThemeSurface.swift`, owner: "don't defer, fix it right") — `cardNavigation(to:)` gained a required `accessibilityLabel: String` parameter, applied to the invisible background `NavigationLink` along with `.accessibilityAddTraits(.isButton)` and `.accessibilityHidden` gated on the existing `cardNavigationSuppressed` flag, threaded through all 16 call sites (each passing the row's own title) — deliberately NOT combining/hiding the row's other elements, so nested fandom-search/expand/byline controls stay independently VoiceOver-reachable; this was chosen over the fuller "one combined stop per card" rewrite specifically to avoid that trade-off. **WorkCardListControls** — no code change; owner confirmed leaving the "Clear All Filters" destructive styling as-is after research found it already matches 3 established sibling conventions (`AO3FilterPanel`/`LibraryFilterPanel`'s "Reset Filters", `CustomizeThemeView`'s "Reset Theme" — all `role: .destructive`, zero confirmation, for recoverable actions); A6-F2's "bulk-action bar lives here" plan note confirmed to mean the `UIComponents/` directory in general, not this file specifically — no bulk-action-bar code exists in `WorkCardListControls.swift`. **Adversarial review caught and fixed a real bug before it shipped:** an independent multi-lens review of the CardNavigationModifier change found that 4 call sites (`AccountComponents.swift`, `AO3AccountWorksList.swift`, `AO3SeriesDetailView.swift`, `AuthorProfileContentSections.swift`) wrapped a `SensitiveWorkRow` in an *external* `.cardNavigation(...)` even though `SensitiveWorkRow` already applies it *internally* (`Features/Privacy/MatureContent.swift`) for its non-blurred branch — a pre-existing double-wrap that was harmless while unlabeled, but became a **privacy bypass** once this change made the label mandatory and real: for a blurred Mature/Explicit work, the outer link sat behind the "Tap to reveal" gate but was not itself hidden or gated, so VoiceOver could reach it directly, read the real (supposedly hidden) title, and navigate straight to the work — skipping `PrivacyGate`'s reveal step entirely. Fixed by removing the redundant external wrap at all 4 sites; independently re-verified after the fix. Two further findings from the same review were confirmed real but left as-is (both pre-existing, both minor): `AO3SeriesSummary.title`/`AO3Collection.title` parsing (`Services/AO3Client+Authors.swift:196`, `AO3Client.swift:406`) has no empty-string guard, so a blank AO3-side title would theoretically produce an empty accessibility label — same gap the row's own visible `Text` already has, not introduced by this wave, deferred as a `Services/` parsing concern outside this wave's scope. | Claude | `claude/hig-wave2-uicomponents-775ed22` | ✅ DONE | **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (only pre-existing warnings, incl. `ReadingQueues.swift`'s file-length warning which predates this wave at 847 lines), iOS **602 tests / 57 suites** (iPhone 17/iOS 26.5), macOS Debug build, whitespace. One verify run mid-session showed a failure (`ReadiumProgressPersistenceTests.noiseDoesNotArmATrailingWrite`) caused by my own concurrent `simctl terminate`/`launch` calls killing the test runner's app process mid-suite ("Restarting after unexpected exit, crash, or test timeout") — confirmed as self-inflicted, not a regression, by a clean re-run immediately after with zero other changes. **Remains manual (owner):** the AO3AuthorNavigation hit-target fix's `.top`-alignment reasoning is sound by SwiftUI's frame/baseline semantics but genuinely unverified at runtime — this file has a documented history of an almost-identical technique failing visually in exactly this spot, so a live check (a multi-coauthor byline, both `compact` and full-width) is warranted before Wave 3 reuses the pattern; same for WorkCarouselSection's ~15pt header-height growth and TagChip's `FlowLayout` reflow at 28pt. Two attempts to do these visual checks via computer-use this session were both blocked by Accessibility Inspector / a stuck system click-interception state (`axAuditService`, not grantable) — same tooling wall hit in T-119; **if the Mac's Simulator window seems unresponsive to clicks after this session, click it directly yourself to clear the stuck state.** Also carries forward Wave 1's still-open screenshot gate (Library X1 row height, Account 14→12pt radius, Sepia recolor) and T-119's Finding-2 VoiceOver spot-check, both untouched by this wave. |
| T-119 | **HIG Wave 1 review-fix round** — fixed all 5 findings from an owner-supplied adversarial review of `b1bc839` (T-118), per the owner's standalone task prompt (`~/Downloads/HIG_Wave1_Review_Fixes_Prompt.md`, gitignored). **Finding 1 (X1 `.minimumHitTarget()`, `UIComponents/MinimumHitTarget.swift`) — no code change; empirically REFUTED the review's no-op hypothesis.** Method: real device taps via computer-use on the built app in the iPhone 17 simulator (owner-granted access), targeting `WorkRowExpandButton` (`Features/Library/WorkRow.swift:201`) on "A Broken Soul" in a Reading Queue. Established the button's screen center by a working baseline tap, then measured offsets: a tap ~9pt outside the visible ~16pt-diameter circle's edge (~17pt from center, clearly off the rendered chrome) toggled the row — direct proof `.contentShape(Rectangle())` applied *outside* a `.buttonStyle(.bordered)` `.buttonBorderShape(.circle)` control DOES extend its hit-test region on this SwiftUI/iOS 26.5 toolchain, contrary to the review's hypothesis; a tap ~21pt from center (near the 22pt/half-of-44pt boundary) also toggled it; a tap ~30pt from center (clearly past 44pt) did NOT toggle it — confirming the extension is bounded, not "whole row tappable." T-118's "28pt circle → 44pt tap target" claim is now runtime-verified, not just asserted. **Finding 2 (X5 `combinedAccessibilityRow`) — fixed, code-verified, VoiceOver runtime spot-check NOT completed (tooling blocked, see below).** `WorkStatLabel` (`UIComponents/WorkStatLabel.swift`) gained an `accessibilityLabel: String?` parameter (falls back to the visible `text`); `CardMetaLabel` (`Features/Home/HomeCards.swift`) gained the same. Every call site that fed a bare/ambiguous value now passes a descriptive label: word counts → `"<n> words"` (`WorkStatLabel.swift` `WorkListStatsRow`, `HomeCards.swift` `CoverCardStatsRow`, `AuthorProfileComponents.swift:199`, `WorkDetailComponents.swift:79`), chapters → `"Chapters <n>"` (`WorkStatLabel.swift`, `WorkDetailComponents.swift:70`, `CommentsView.swift:371`), kudos → `"<n> kudos"` (`WorkStatLabel.swift`), comment totals → `"<n> comments"` (`CommentsView.swift:374`), cover-card short ratings ("T"/"M") → the full rating name via new `CoverCardStatsRow.ratingFull` threaded from `work.rating` at both call sites (`HomeCards.swift` `WorkCoverCard`/`AO3WorkCoverCard`), and author/fandom `CardMetaLabel` rows → `"Author: <name>"` / `"Fandom: <name>"` (4 call sites). Values that were already self-describing (full rating text, "Complete"/"WIP") were left as-is, matching the review's scope. **Finding 3 (X3 `onAccentColor`) — fixed.** Renamed to `onEffectiveTint` (`App/ThemeManager.swift`) and now derives luminance from `effectiveTint` (what controls actually render with — Sepia's fixed brown, not the user's raw `accentColor`) instead of `accentColor` directly; zero call sites exist yet (Wave 3+ adopts it), so the rename was free. Sanity-checked `Color.relativeLuminance` (`Utilities/ColorHex.swift`) by hand against the WCAG formula for both Sepia's `appTint` brown (`Color(red: 0.604, green: 0.404, blue: 0.196)` → luminance ≈0.168) and AO3 red (`#990000` → ≈0.068) — both correctly land under the 0.45 threshold and resolve to white, no bug found. **Finding 4 (X6 reference site) — fixed.** `Collections.swift`'s `CollectionDetailView` now reads `@AppStorage("confirmBeforeDelete")` (matching `WorkCardActions.swift:200,329` / `LibrarySectionListView.swift:19`) and only stages `pendingRemoval` (routing through the `destructiveConfirmation` X6 helper) when the setting is on; when off, the swipe action calls `remove(work)` directly, same as every other destructive swipe in the app. **Finding 5 (X2 duplicate idiom) — fixed via migration (option 1).** Deleted `WorkDetailView.animateUnlessReduced` (was `Features/WorkDetail/WorkDetailView.swift:691-699`); its 3 call sites in `WorkDetailOverviewSections.swift` (lines 36, 175, 256) now call the shared `withAnimationUnlessReduced(reduceMotion: reduceMotion)` directly — `WorkDetailView` already exposed `@Environment(\.accessibilityReduceMotion) var reduceMotion` (line 43), so no new state was needed, and `withAnimation { }`'s implicit `.default` animation is byte-identical to the shared helper's default parameter, so behavior is unchanged. `WorkDetailPresentationTests` untouched and still green. | Claude | `claude/hig-wave1-review-fixes-b1bc839` | ✅ DONE | **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (zero new warnings in any touched file), iOS **602 tests / 57 suites** (iPhone 17/iOS 26.5) incl. `WorkDetailPresentationTests`/`AO3URLResolverTests`/`CommentsAccountTransitionTests` passing untouched, macOS Debug build, whitespace. **Remains manual (owner) — Finding 2's VoiceOver/Accessibility-Inspector spot-check:** attempted via macOS Accessibility Inspector targeting the simulator; its element-picker ("Point to Inspect") mode routes all hit-testing through a system helper process (`axAuditService`) that cannot be added to a computer-use allowlist (not a user-facing app), and once active it globally intercepts every click — including the clicks needed to turn it back off — so the session got stuck in that mode. **If Accessibility Inspector looks stuck/unresponsive to clicks after this session, click or Cmd+Tab away from it directly at your own keyboard/mouse (not through automation) to clear it, or force-quit it.** The code change itself is low-risk (mechanical use of the already-proven `.accessibilityLabel()` API, same mechanism Wave 1 already used successfully for `WorkRowExpandButton`'s own label) but the specific announced strings are unverified by ear/Inspector — worth a real VoiceOver pass before Wave 2 mass-adopts `combinedAccessibilityRow`. Carries forward Wave 1's still-open owner screenshot gate (Library hit-target row height, Account 14→12pt tile radius, Sepia segmented-control recolor) — untouched by this round. |
| T-118 | **HIG Wave 1 — Shared foundation.** Landed all six cross-wave helpers (X1–X6) + the PONY-6.1 href resolver from the T-116 roadmap, each proven on one reference call site per the plan (mass adoption is Wave 2+). Research phase: 8 parallel agents deep-read the exact current code + models for each helper before any line was written (`~/.claude/…/subagents/workflows/wf_396422b8-fb1/journal.jsonl`). **X1** `.minimumHitTarget(_:)` (new `UIComponents/MinimumHitTarget.swift`) — pads a control's tap region without growing its visible chrome; proven on `WorkRow.swift`'s `WorkRowExpandButton` (28pt circle → 44pt tap target — **runtime-verified by real device taps in T-119**, not just asserted). **X2** shared reduce-motion helpers (new `UIComponents/ReduceMotion.swift`: `withAnimationUnlessReduced(_:reduceMotion:_:)` + `.animation(unlessReduced:value:)`) — proven on `WorkCarouselSection`'s collapse toggle (previously ungated, UI-5). **X3** semantic theme color roles (new `UIComponents/SemanticThemeColors.swift`: `errorColor`/`excludeColor`/`favoriteColor`/`statusSuccessColor` on `ReaderTheme`; `relativeLuminance` on `Color` in `Utilities/ColorHex.swift`; `onAccentColor` on `ThemeManager` for A9-F3 — **renamed `onEffectiveTint` and fixed to derive from `effectiveTint` in T-119** after review found it judged the wrong color) — proven on `WorkRow`'s favorite star (`.yellow` → `theme.appTheme.favoriteColor`). **X4** unified `CardRadius` token (`UIComponents/AppThemeSurface.swift`) — `CardListMetrics`/`CarouselCardMetrics`/`AccountControlMetrics` now delegate to it (16/12/20/12 unchanged); retired both stray 14pt literals (`AccountShortcutGridTile`, `WorkQuickActionTile` → `CardRadius.tile` = 12pt, a small intentional visual delta flagged for owner screenshot review). **X5** `.combinedAccessibilityRow(_:)` (`UIComponents/WorkStatLabel.swift`) — collapses icon+text stat rows into one VoiceOver stop (`.ignore` + explicit label, not a `Label` conversion — `AccountComponents.swift`'s own comment documents `Label`'s icon-tint diverging between `List` and plain `ScrollView` in this codebase); applied to `WorkStatLabel` (fixes UI-2's highest-frequency finding across every card) and `HomeCards.swift`'s `CardMetaLabel`. **X6** unified `destructiveConfirmation` idiom (`UIComponents/DeleteConfirmation.swift`: `DestructiveConfirmationStyle` + item- and Bool-driven overloads, `.alert`/`.confirmationDialog` both supported since the audit's real finding was "no shared shape," not "wrong modifier") — proven on `Collections.swift`'s "Remove from collection" swipe action, which previously fired with **zero confirmation** despite `role: .destructive` styling. **PONY-6.1** `AO3URLResolver.resolve(_:allowExternalHost:)` (new `Services/AO3URLResolver.swift`) consolidates the ~10 duplicated AO3-href-resolvers found across `Services/`; unlike several of the sites it replaces, it validates scheme (http/https only) **and** host (archiveofourown.org or subdomain, unless explicitly allowed off — needed for author rich-text bios) — a real correctness/hardening fix, not just deduplication. 14 new table-driven tests (`KudosTests/AO3URLResolverTests.swift`) pin the edge-case matrix (nil/empty/whitespace, missing-leading-slash, protocol-relative same-host vs. foreign-host, `javascript:`/`mailto:` rejection, query/fragment preservation) that no prior call site had covered. Proven on `AO3Client.parseBlurb`'s `seriesURL`. **Folded in:** `App/ContentView.swift`'s Sepia `UISegmentedControl` appearance proxy now reads `ReaderTheme.appBaseBackground`/`appElevatedBackground`/`textColor` instead of 3 hardcoded `UIColor(red:green:blue:)` literals that had silently drifted from the semantic tokens for ~3 weeks (git history: `775e4c74` predates `appBaseBackground`/`appElevatedBackground` by 3 weeks) — small color shift expected, flagged for screenshot review. | Claude | `claude/wave-0-pla-owner-decisions-ce0ae0` | ✅ DONE | **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (zero new warnings in any touched file), iOS **602 tests / 57 suites** (iPhone 17/iOS 26.5, +14 new `AO3URLResolverTests`), macOS Debug build, whitespace. Also did a standalone `xcodebuild build` for the simulator + `xcrun simctl install/launch` + screenshot on the existing iPhone 17 sim (real seeded library data) — app launched clean, Home's `WorkCarouselSection` (X2) rendered normally. **Remains manual (owner):** a full screenshot pass on Library (X1 hit-target row height), Account (X4's 14→12pt tile radius), and Sepia theme (the ContentView fix + X3's favorite-star recolor) — declined computer-use access to drive the Simulator interactively mid-session, consistent with this repo's existing pattern of owner-run screenshot gates. `errorColor`/`favoriteColor`/`statusSuccessColor`'s exact RGB values are placeholder estimates, not WCAG-verified against each theme's real backdrop — check before Wave 2 migrates the 3 exclude + 5+ status call sites onto them. **Not done (out of Wave 1 scope, call-site migration is Wave 2+):** the other ~40 call sites these helpers exist to fix. **Next:** Wave 2 (`UIComponents/` sweep, the biggest multiplier) once the owner clears the screenshot gate above; API review of the six signatures is worth a skim first since Wave 2 mass-adopts them. |
| T-117 | **Wave 0 — Owner decisions (no code)** — resolved the five decision-gated items blocking Waves 4/10 + Track B1 of the T-116 roadmap. **(1) BUG-8/KD-LAYOUT-READERAFFORD:** not a bug — confirmed live that Library cards open the reader directly on **both** iOS and macOS (`openMode: .reader` set at every Library card call site: `LibraryView.swift:692,699`, `LibrarySectionListView.swift:313,327`, `Collections.swift:133`, `ReadingQueues.swift:285`); the "cards open detail" line in Key Decisions was stale and has been corrected. **(2) A1-F2/KD-TEAMID:** keep `DEVELOPMENT_TEAM = NQH85H7343` as-is (2 of 4 build configs) — no scrub, no git-history purge; the exposure is low-value and not worth a history rewrite. **(3) PONY-6.7:** raise `KudosBackupManifest.supportedVersions` to the latest version only — no pre-July `.kudosbackup` files still in use. Decided, **not yet implemented** (`Services/KudosBackup.swift:174-175`, one-line change, unscheduled). **(4) KD-MIGRATION-META:** split by platform — iOS consolidates Library metadata onto Readium; macOS keeps the custom OPF layer (no Readium navigator there to source from). **(5) SHORT-R5:** keep `@AppStorage` Compact-default semantics, no forced migration for existing Detailed users. **New scope item raised alongside it:** the compact/detailed toggle should be one app-wide setting, not per-screen — today it's **5 independent, un-synced stores**: `AccountView.swift:33` (`@AppStorage("account.displayMode")`, default `.compact`), `HomeSectionListView.swift:36` (`@AppStorage("home.\(kind).displayMode")`, keyed **per Home section**, default `.detailed`), `LibrarySectionListView.swift:44` (same pattern, keyed **per Library section**), `ReadingQueues.swift:107` (plain `@State`, **not persisted at all**), plus two externally-driven parameters (`AccountComponents.swift:606`, `AuthorProfileContentSections.swift:194,364`) with no storage of their own. Flagged as a Wave 1 candidate (same "one helper, many call sites" shape as X1–X6); unscheduled. | Owner + Claude | `claude/wave-0-pla-owner-decisions-ce0ae0` | ✅ DONE | `TASKS.md` Key Decisions + Bugs registry updated to match; `docs/audits/Fixes_and_HIG_Implementation_Plan.md` Wave 0/4/Track-B1 updated to reflect resolution. **Unblocked:** Wave 1 (shared foundation) can start; Wave 4 drops the BUG-8 routing item; Track B1 drops the team-id action item. **Not implemented (both are code, Wave 0 is decisions-only):** PONY-6.7's one-line version-floor change and the app-wide display-mode unification — schedule into Wave 1 or a standalone fix when ready. |
| T-116 | **HIG implementation wave planning (planning-only)** — transform the T-115 HIG audit into an efficient implementation roadmap, **folding in every still-open finding from the other audits** so shared files are touched once. Harvested (multi-agent) **80 open non-HIG findings** (44 P2 / 36 P3) from `RELEASE_READINESS_FABLE5.md` (Areas 1-10 + T91-RF), `PONYTAIL_AUDIT_RELEASE_FIXES.md`, `COMMENTS_AO3_API_AUDIT.md` (Part D CAA-8/9 + CAA-10/12/13/14/15), the 3 adversarial reviews, and TASKS registries — **28 overlap HIG files** and are folded into the HIG waves; the rest sit on Track B (security / comments-parser / functional / perf / reader-correctness) or the explicit out-of-scope list (backlog features + manual gates). Strategy: **shared-foundation first** (6 cross-wave helpers — `.minimumHitTarget()`, promoted reduce-motion helper, semantic theme color roles, unified card tokens, combined-a11y-row helper, shared destructive-confirm — that collapse ~40 call-site fixes into 6 component changes), then a `UIComponents/` sweep (biggest multiplier), then **disjoint per-directory waves that run in parallel** (Search/Library/WorkDetail/Account/Comments — no shared files after Wave 2), then the two highest-risk waves last and sequential (Dynamic-Type structural UI-4; Reader UI-9+BUG-7). Deliverable: [`docs/audits/Fixes_and_HIG_Implementation_Plan.md`](docs/audits/Fixes_and_HIG_Implementation_Plan.md) — Executive Summary, 10 waves + Track B (goal/findings/shared-components/files/deps/risks/testing/complexity each), Cross-Wave Improvements, Branch Strategy (`claude/hig-wN-*` spine + parallel fan + Track B), Human Review Gates. **No code — planning only.** | Claude | `claude/hig-review-task-f5e043` | ✅ DONE | Committed (see log). **Verified:** `Scripts/verify.sh` ALL GREEN (docs + TASKS.md only). **Next (owner):** Wave 0 decisions (BUG-8 routing, team-id policy, manifest versions, migration-meta, Compact default) unblock Waves 4/10 + Track B1; then implementation starts at Wave 1 (shared foundation) off `release-fixes`. Harvest evidence: `~/Downloads/HIG_T115_evidence/harvest_flat.json` (80 findings, overlap-tagged) + `harvest_raw.json` (per-source + completeness critique). |
| T-115 | **HIG adherence deep-dive review (review-only)** — structured audit of Kudos against Apple's current HIG (fetched live from developer.apple.com), per the owner's standalone task prompt. Multi-agent pipeline: 6 HIG research agents → 10 codebase-area audits (full prompt scope: root shell, T-114 hub, `UIComponents/`, Search, Home, Library, Account+Settings, reader chrome, authors/Browse/Comments, onboarding/privacy/support + app-wide a11y greps) → synthesis → **adversarial verification of all 56 potential issues** (every claim re-read against source by an independent agent; grep claims re-run). Result: **0 P1 · 24 P2 · 28 P3 confirmed, 4 refuted** (verifier severities final — every provisional P1 was downgraded or refuted under verification). Runtime passes: iPhone 17 sim across Sepia/Dark/OLED (+Light seen live), default + accessibility-XL Dynamic Type (T-114 hero card + quick-action grid reflow correctly at AX sizes; the segmented control and the carousel cover cards do not — cover cards truncate nearly every field), Reduce Motion on/off (skeleton + T-114 section switches behave correctly; the 12+ ungated animation sites stand as verified static findings); live macOS pass on the real app found **BUG-7** (legacy reader renders under the sidebar — leading edge of every line occluded; reproduced on "The Queen's Mercy") and **BUG-8** (macOS Library card opens the reader directly, contradicting the documented cards-open-detail decision). Full report: [`docs/audits/HIG_ADHERENCE_REVIEW_T115.md`](docs/audits/HIG_ADHERENCE_REVIEW_T115.md); registries updated (UI-2…UI-9, BUG-7/BUG-8). Evidence for owner review (23 sim screenshots + verdict JSONs): `~/Downloads/HIG_T115_evidence/`. **No code changed — review-only per the prompt.** The review spanned three sessions (two usage-limit deaths); the 33 verification verdicts lost to the second death were recovered from the workflow journal and cross-checked against the workflow's persisted output (0 mismatches) before writing. | Claude | `claude/hig-review-task-f5e043` (from `release-fixes` tip `08cbc11`, no divergence) | ✅ DONE | Committed `792504a` (2026-07-18, docs + TASKS.md only; pushed). **Verified:** `Scripts/verify.sh` ALL GREEN on the docs-only diff. **Remains manual (owner, report §9):** screenshot review; a VoiceOver/Accessibility-Inspector spot-check (all VoiceOver findings are code-verified but not runtime-exercised — two are explicitly flagged as needing it); BUG-8 intended-or-bug decision; `ReaderTheme` palette contrast audit (4.5:1 per theme — the palette file was outside the reviewed set); UI-2…UI-9 prioritization (suggested: UI-2/UI-3 first — highest-impact, mostly mechanical, with in-repo exemplars to copy). |
| T-114 | **Work Details redesign** — reorganize the single long WorkDetailView Form into the Account-style hub: hero identity card + large 4-option segmented control (Overview / Tags / Discussion / Library), preserving every existing action/metadata field and the lazy remote→local resolve behavior. Overview = summary (long ones collapse behind Show More) + state-aware quick-action grid (8 tiles, Account's `AccountShortcutGridTile` visual language; Comments tile/stat jump to Discussion) + purpose-based Publication/Work/Stats cards + series card; Tags = categorized AO3 tag chip cards (unchanged `router.searchAO3` taps, "Tags from AO3" footer on the last card); Discussion = All Comments / Chapter Comments (`CommentsView(initialFocusesChapter: true)`) / Write a Comment entries, **no prefetching** (footer says so; comment pages still load only inside the pushed CommentsView, whose T-111 session cache is untouched); Library = Status rows (state-indicator toggles for Saved/Later/Finished, queue rows show "Queue — #position of count", chevrons only on picker rows, old actions-footer text preserved as the Status footer) + Storage (Downloaded · file size, preservation state, Retry row) + Activity (Added/Last Opened/Progress bar) + My Tags editor card (unchanged Tag model/persistence, suggestions row). New files `WorkDetailComponents.swift` (WorkDetailTab, WorkDetailHeroCard, WorkQuickActionTile, testable `WorkDetailPresentation` label/state helpers), `WorkDetailOverviewSections.swift`, `WorkDetailSections.swift`; new `removeFromSavedForLater()` wires the Later toggle to the same `removeFromQueueAndDeleteIfQueueOnly` command the card menus use. No networking/parsing/persistence changes; toolbar (favorite + shared `AO3WorkActionsMenu`), pull-to-refresh, series preservation prompt, byline-conflict dismiss guard all preserved. Spec: owner-supplied implementation prompt (gitignored, local). | Claude | `claude/work-details-redesign-a28e94` (from `release-fixes` `e1fe36d`; continues the interrupted `claude/work-details-redesign-ec9e48` session's uncommitted shell — that worktree's copies are now superseded) | ✅ DONE | Committed `269c2a3` (2026-07-17). **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint, iOS **580 tests / 55 suites** (iPhone 17 / iOS 26.5, incl. new 7-test `WorkDetailPresentationTests`), macOS Debug build, whitespace. **Simulator (seen, iPhone 17, OLED theme, owner's real data):** all four sections on a remote work (AO3 marked-for-later "Business and Pleasure" — partial-metadata cards omit missing rows, Library shows the not-in-library card, quick actions show unsaved states) and a local work ("A Broken Soul", queued+finished — Read tile, "In 1 Queue"/"Test queue — #1 of 2", finished checkmark, "Downloaded · 40 KB"/"Preserved offline", Added/Last Opened/Progress, My Tags suggestions); Show More expands the full summary and the expansion survives section switches; section selection survives child pushes (Comments, tag search) and Back returns to the immediately previous screen (Library tab); All Comments opens the existing chapter-aware threaded CommentsView (52 real comments) with zero prefetch from the hub; tag chip tap ran the real AO3 fandom search (952 pages). **Remains manual (owner):** light/sepia theme pass (app pinned to the owner's OLED theme; sections use only the shared card tokens Account already exercises), iPad/macOS layout look-over, and a mutation spot-check of the quick-action/Library toggles (they call the same commands as before — no new write paths). **Review-fix round (`4e21ca8`, 2026-07-17):** owner-supplied adversarial review (Codex 5.6 Sol, `WORK_DETAILS_REDESIGN_REVIEW.md`) raised 9 findings — each validated against the code first, **all 9 confirmed real**, all 9 fixed: F1 stale soft-deleted record actionable from a `.saved` detail (new `postRemovalAction` routing + `withLocalWork` now restores a pending-deletion record before any action per the re-acquisition invariant; +revive regression test incl. tombstone retraction); F2 Chapter Comments/Write a Comment were label-only (real `CommentsModel` intents added — chapter scope opens By Chapter on ch.1, `initialComposes` auto-presents the composer post-load, one-shot, signed-in only, cleared on account transition; +4 `CommentsInitialIntentTests`; both **verified live in the sim**); F3 hero VoiceOver flattening (combine now scoped to the stat pills; co-author buttons individually focusable); F4 tag truncation/hit targets (multiline `TagChip` variant + ≥44pt targets — **superseded 2026-07-19, T-120 review-fix round:** the ≥44pt part no longer holds for these specific chips; Wave 2 (T-120) replaced the hand-rolled `.frame(minHeight: 44)` with the shared `.minimumHitTarget(28)` helper at a reduced 28pt floor, an accepted HIG deviation for this `FlowLayout` tag cloud — the multiline-wrapping fix from F4 is untouched); F5 impossible-recovery copy for non-AO3 imports (Tags empty card + Storage row key off `ao3WorkID`); F6 remote lifecycle guidance restored (statusFooter as the not-in-library footer); F7 remote refresh keeps Published (`WorkMetadataRefresh.remoteMetadata`, screen-local, no model change); F8 Continue Reading state; F9 queue rows keyed by membership UUID. Plus review's a11y matrix gaps (reduce-motion respected at all 3 `withAnimation` sites; grid 3→2 columns at accessibility Dynamic Type) and its simplification pass (duplicate chapter parser deleted → `SavedWork.totalChapterCount`; summary stripped once/render; queue ordering unified on new `ReadingQueueService.orderedWorks` shared with the queue screen). **Deliberately not done:** `WorkQuickActionTile`↔`AccountShortcutGridTile` merge (different affordances: busy/detail vs count/external-badge); segmented control left matching Account's fixed picker (parity is the design goal; labels are short single words); no Discussion comment preview (no shared cache exists post-T-111 by design — prompt permits the entry card). **Verified:** `Scripts/verify.sh` ALL GREEN — iOS **587 tests / 56 suites** (+7: 4 comments-intent, 2 queue incl. F1 revive regression + `orderedWorks` projection, 2 presentation, −1 deleted duplicate), macOS build, lint/whitespace clean; sim spot-checks of F2 both flows + F4 layout. |
| T-113 | **Search typing hang + index completeness** — typing in Global Search hung seconds per keystroke. Root cause: `FandomCatalog.cachedFandoms` scanned the whole cached AO3 fandom catalog per keystroke with `.lowercased()` allocations inside the sort comparator, ×2 evaluations per render, no debounce (the library side already used `WorkSearchIndex` correctly). Fix: precomputed normalized+deduped+count-sorted catalog search index (rebuilt lazily on catalog change, `revision`-stamped for observers) with an allocation-free prefix-first ranked scan that early-exits at the limit; SearchView's five per-render match lists replaced by one debounced (150 ms) `.task(id:)` snapshot keyed on query + record counts + catalog revision, with `modelContext != nil` liveness guards on snapshot models; `WorkSearchIndex` v2 adds `seriesTitle` + user-tag names — reindex added at WorkDetailView tag add/remove, KudosBackup restore reindex moved *after* user-tag linking, launch sweep migrates v1 records; catalog + local name matching unified on diacritic-folded `WorkSearchIndex.normalize` (typing "pokemon" now matches "Pokémon"; old code couldn't); `WorkRow` strips summary HTML 1× per render instead of 3×. Audited every `SavedWork` mutation path for reindex coverage — importer ×5, WorkTags ×2, metadata refresh, queue service, backup restore all covered; folder-sync/canonical-merge mutate no indexed fields. | Claude | `claude/search-indexing-performance-3d5408` | ✅ DONE | Committed `cf79177` (2026-07-17). **Verified:** `Scripts/verify.sh` ALL GREEN (invariants, lint, full iOS suite on iPhone 17/iOS 26.5, macOS Debug build, whitespace); targeted suites 8 `WorkSearchIndexTests` + 4 new `FandomCatalogSearchTests` all pass. **Simulator (seen, iPhone 17 + real warm catalog & library):** per-keystroke typing updates live with no hang; "harry" → Fandoms on AO3 ranked Harry Potter (599K) first then count-desc; substring matches work ("ahrr" → "Fahrrad fahr'n"); library works section matches; clear → instant idle; Return still runs the real AO3 search (4.7k pages rendered). **Remains manual:** owner feel-check typing latency on the physical device with their full library. **Follow-up (same day, owner reported still hanging on device):** three more per-keystroke scan sites found + the real device-side culprit — the one-time v2 launch migration reindexed the whole library in ~200-record main-actor bursts (measured ~1ms/record on M-series in-memory; multiples of that on-device), freezing typing for its whole duration. Fixed: (1) `rebuildIfNeeded` now time-sliced (≤5 ms main-actor slices + 2 ms breathers) with incremental saves every 250 records (bounds autosave churn; a force-quit mid-sweep resumes instead of restarting); (2) `FandomListView` (Browse → category filter, up to ~20k names) — precomputed normalized entries + 120 ms debounced snapshot instead of per-keystroke `localizedCaseInsensitiveContains` + full-list re-diff; (3) `LibraryOptionPicker` (filter-panel tag lists) — options normalized once per presentation, filter evaluated once per render; (4) `AddWorksToCollectionView` "Filter works" — now matches via `WorkSearchIndex` (index text, AND-across-terms; richer + no per-keystroke lowercase/fandom-decode rescans), candidates/matches evaluated once per render. New scale test: 1,200-work sweep completes exactly once across save/slice boundaries. **⚠️ Install-provenance correction (2026-07-17 evening):** the two earlier device installs shipped a **stale Jul 15 `.app` from a different DerivedData folder** (picked via `ls …/Debug-iphoneos/Kudos.app \| head -1` instead of the invoking build's own products dir) — the phone never ran the first fix, so the owner's "still hanging" report was made against pre-T-106/pre-T-113 code (the follow-up's migration-slicing + extra filter-surface fixes remain real hazards and stand on their own). Verified-current build (strings-checked: no stale `"Shortcuts & Preferences"`, new symbols present; product of this worktree's DerivedData, Jul 17 18:08) installed on the owner's iPhone + re-verified in the simulator on the same product: search sections render instantly and T-106's Overview-card removal is visible (owner's "extra Overview button" report was the stale binary — no code change needed, `07cb369a` already removed it). **Lesson: always take the `.app` path from the build log's own products dir and strings-check provenance before installing.** **Merged to `release-fixes`** (fast-forward `0fbe2e6..8e5fadc`, 2026-07-17, pushed) — this brought T-106–T-112 (`claude/general-improvements-branch-87e11b`) along, since T-113 was stacked on that branch's tip. Task branches/worktrees for the merged stack cleaned up on owner instruction; `release-fixes` remains the integration tip. |
| T-105 | **Final release-blocker certification** — verify A2-F1, A5-F1/F2/F3/F4, A7-F1/F2, T91-RF1/RF2/RF3, and A10-F1 on the exact final candidate; run targeted/full/Release/archive/install checks; record manual gates and verdict without merging to `main`. | Codex | `release-fixes` | ✅ DONE | **Certified product content:** `b82807d8709ac2ce08fc2f2fc51a262e893fca09`, based on Inbox-merged local `main` `37987dda`; report/auth-wording commit `7d144ed`. Every `main..candidate` production/test change was classified and later blocker-path edits re-read; no blocker regression/new P1 found. **PASS:** focused **284 tests / 18 suites**; `Scripts/verify.sh` ALL GREEN (**566 / 53**, invariants/lint/macOS Debug/whitespace); isolated clean iOS + macOS Release builds; unsigned generic-iOS archive (arm64/entry point/matching dSYM, all Readium/SPM resources, correct version/background metadata, byte-identical GPL/notices, no app fixtures/source/credentials); Release app install/launch and visible Home render on iPhone 17 simulator. **Unchanged:** 28 P2 + 7 P3. **NOT RUN:** all 9 signed-device/hardware/live-AO3/A→B/reader/accessibility/long-session gates. **Verdict:** P1 certification PASS; overall NOT READY. **Next:** owner fixes or explicitly accepts P2/P3, completes every manual gate on the final signed candidate, reviews the evidence, then explicitly authorizes any `release-fixes`→`main` merge. No merge performed. |
| T-104 | **Ponytail audit fixes, Session A (`docs/audits/PONYTAIL_AUDIT_RELEASE_FIXES.md`)** — the audit's own "trivial/mechanical, no behavioral surface" tranche: all 8 confirmed-dead deletions (§3.1-§3.7: `AO3AccountSection.swift` whole file, `MatureContentHiddenView`, `FolderSyncOnboardingState.shouldShow`, `AppRouter.Panel.settings`, `CommentDraftStore.clear(for:identity:)`, `AO3AccountWorksList.Kind.myWorks` — re-grepped immediately before landing per the audit's own caution, still unrouted — `AO3CommentsPage.totalOnPage` only, **not** `descendantCount` which is load-bearing/tested) plus the low-risk consolidations §4.4a-c, §4.8e, §4.8g, §4.8h, §4.8i, §4.2c. **Structural only — no behavior changes intended.** Notable judgment calls beyond the audit's literal text: (1) §4.4a's `AO3AuthorPaginationRows` took a whole `AO3AuthorProfileModel`, but `AO3SeriesDetailView` uses local `@State`, not a model — generalized the component to primitives+closure with a `model:auth:` convenience initializer so the 5 existing model-based call sites are untouched byte-for-byte; (2) §4.8g's "Save for Later" label/icon pair turned out **not** fully byte-identical — `WorkBulkActionBar` uses `clock.arrow.circlepath` for the unsaved state where the two `WorkCardActions.swift` context menus use `bookmark.fill` — consolidated only the genuinely identical `WorkCardActions.swift` pair via new `WorkActionLabels`, left `WorkBulkActionBar`'s save-toggle icon as-is rather than silently changing visible UI; the "Mark as Finished/Still Reading" pair *was* identical across all 4 sites and is fully consolidated. New shared files: `Features/Onboarding/OnboardingComponents.swift` (`OnboardingScaffold`/`OnboardingPointRow`, used by `WelcomeView`+`SyncFolderOnboardingView`); new `FilterTextMatching` enum in `Models/AO3Models.swift` (shared by `LibraryFilters`/`AO3SummaryFilter`); new `WorkActionLabels`/`isLoginRedirect(path:)` helpers added to existing files. **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (pre-existing warnings only, none in touched files), iOS **552 tests / 52 suites** (iPhone 17/iOS 26.5, same count as the pre-change baseline — one test assertion removed alongside its now-dead primitive, no new tests needed for a structural-only change), macOS Debug build, whitespace. | Claude | `claude/ponytail-audit-release-fixes-1ba687` (from `release-fixes` `f7a66a9`; merged back `0f68363`) | ✅ DONE | **Session A committed** `51cb543`. **Session B committed** `5c7a6cf` (mechanical consolidation, §4.2a-b/§4.3a-b/§4.8a-d/§4.8f/§4.6/§4.9). Covers: `AO3Client` pagination-total loop (4 sites → shared `paginationTotal(in:currentPage:)`, widened to `Element` so the Comments call site can scope it to `#comments_placeholder`) and `stat`/`statInt` digit-parsing (unified on the more-robust `.filter(\.isNumber)` strategy, fixing the inconsistency the audit flagged as the actual defect); the two authenticated-write-action items done with the extra care §9 called for — CSRF-fetch (`AO3WriteActions`/`AO3CommentActions`, 8 call sites → one `fetchCSRFPage(at:)` returning `(html, token)`; the `postComment`/`postCommentReply` generation-recheck now happens right after the combined fetch+parse rather than interleaved, provably equivalent since no `await` occurs in between — Swift's cooperative concurrency guarantees synchronous code between awaits is atomic) and the comment-verdict switch (5 sites incl. `AO3InboxActions` which the audit's own §4.3b list missed → one `commentWriteResult(status:body:onSuccess:rejectionFallback:)`; `giveKudos`'s 2xx/422 kudos-specific switch deliberately left alone, per the audit's own instruction); `WorkLifecycle.saveBestEffort` ×5 sites → new `ModelContext.saveBestEffort(reason:)` in `Utilities/`; Account external-nav URL builder → new `AccountExternalNavCard` in `AccountComponents.swift`; preference-key stripping ×4 → new `preferenceFieldKey(_:)` in `AO3PreferencesModels.swift`; `FandomCatalog.loadMissing`/`refresh` → shared private `fetch(_:onlyStale:)`; series-progress-text → shared `SeriesPreservationResult.summaryParts(verb:)` (kept the phase-specific cancelled/empty/prefix copy separate per surface — the two functions have genuinely different progress-vs-completion architectures, not just cosmetic differences, so a full merge would have been the wrong call); §4.6 turned out to be **two** distinct duplicate pairs, not one shared component as the audit's text implied — `HomeCards`' cover-card stats (rating-short/chapters/completion/word-count, compact notation) and `WorkRow`/`AO3WorkRow`'s list-row stats (rating-full/word-count-full/chapters/**kudos**, not completion) are different field sets/formatting/spacing, so built `CoverCardStatsRow` and `WorkListStatsRow` separately rather than forcing one shape; §4.9 test doubles consolidated into new `KudosTests/Support/AO3AuthTestDoubles.swift` (9 mock types + shared `Signal` gate actor, `AO3LoginPerforming` boilerplate defaulted via protocol extension) — verified `InboxTestSessionValidator`'s session-echoing behavior (needed for the alice→bob account-transition suites) is **not** a safe substitute for `MockAO3SessionValidator`'s fixed-result behavior before almost merging them incorrectly, so it stayed its own distinct shared type instead. **Verified:** `Scripts/verify.sh` ALL GREEN (after fixing one real miss — `Utilities/ModelContext+SaveBestEffort.swift` needed explicit `Foundation`/`OSLog` imports for `Log.library.error`/`localizedDescription`, caught by the first build) — invariants, lint (pre-existing warnings only; `AccountComponents.swift` was already over its 700-line soft threshold before this session and grew further, still just a warning not a hard error), iOS **552 tests / 52 suites** (same count — structural only, no tests added/lost), macOS Debug build, whitespace. **Session C committed** `252c433` (§4.3c) + `05fec6b` (§4.1) + `441d103` (§4.5) — the needs-behavioral-verification tranche, one commit per finding (this tier is the one worth reverting piecemeal). §4.3c: `AO3AuthorProfileModel`'s three authenticated actions (Subscribe, block/mute begin, block/mute confirm) now share one `performFencedAuthorAction` with the stale-response fencing preserved exactly — success path re-checks route+scope+generation, both failure paths re-check scope+generation only (matching the originals: an error's side effects are session-wide, not route-scoped), and expiry is reported against the *captured* generation so a stale response can never log out a replacement session; new `KudosTests/AO3AuthorActionFencingTests.swift` (7 tests, built on Session B's shared doubles) drives the fence end-to-end through the injectable page loader per the audit's §9, incl. mid-flight account switches on the success, failure, and expiry paths. §4.1: `WorkImporter`'s two hand-rolled matchers now resolve through `WorkIdentityIndex` (making the "ONLY matcher" doc claim true); the user-EPUB matcher gains the canonical-URL tier it was missing; the audit's §9 open question on `existingWork(forSource:)`'s exact-`sourceURL` 4th tier answered **yes, keep** — it's the only identity signal for non-AO3/non-canonicalizable sources — retained as a documented final tier after the index lookup; the title/author/file-size heuristic for sourceless user EPUBs stays (similarity, not identity, so it doesn't belong in the index); 2 new `PreservedWorkTests` cover the §9-required dedup paths (download-URL-variant merge via the canonical tier; exact non-AO3 merge). Tier-priority nuance recorded in the commit message: strongest-tier-first can pick a different record than the old fetch-order OR-scan only when the library *already* holds same-work duplicates (the degenerate state `CanonicalWorkMerge` exists to repair). Also dropped `WorkImporter`'s now-superfluous `file_length` lint disable. §4.5: the 4× duplicated selection/bulk-action shell (`FandomWorksView`, `TagWorksView`, `SearchView`, `AuthorProfileView`) collapsed into new `UIComponents/RemoteWorkSelection.swift` — `RemoteWorkSelectionController` (@Observable: the nine formerly-per-view @State properties + toggle/exit/selected(in:) + the four bulk ops), `RemoteWorkSelectionToolbar`, `RemoteWorkBulkActionBar`, `SelectableAO3WorkRow` (the byte-identical row wrapper 3 of the 4 surfaces shared), and `.remoteWorkSelectionChrome()` (sheets + failure alert + cancel-on-disappear); net −280 lines (631 deleted across the views). Key design decision: chrome + batch task stay **host-side**, not bar-side — bar-owned state would cancel an in-flight batch and drop its failure alert/Add-to sheet the moment Done swaps the toolbar branch; host ownership preserves pre-consolidation behavior exactly. Rows/data sources, pagination, overlays, and non-selecting toolbars stay per-surface (genuinely different, per the audit's own "parameterized shell, not monolithic view" guidance); bulk-button enablement still keys off `selection.isEmpty`, so stale-selection-after-refresh behaves as before. **Verified:** `Scripts/verify.sh` ALL GREEN three times (after §4.3c: iOS **559 tests / 53 suites**; at the Session C tip twice: **561 / 53** — +7 fence tests, +2 import tests), macOS Debug build each time; regression-matrix rows updated (author profiles, multi-select shell, EPUB import). **Owner still owed (audit §9):** manual selection/bulk/pagination pass on all 4 surfaces + a live-session Subscribe/Block/Mute spot-check — structural-only changes, but they sit on the bulk-action and authenticated-write surfaces. **Session D explicitly skipped by owner decision** (generic `TTLCache` — too much risk for too little codebase-size/efficiency gain). **Session E (research pass on the audit's 6 `investigate` leads, §6) done** — no source changes, a research report only, committed as a `## 10` addendum to `docs/audits/PONYTAIL_AUDIT_RELEASE_FIXES.md`. Ran as a `Workflow` (6 finder agents, one per lead, each verdict adversarially verified by a second agent — same methodology as the original audit's Pass 2); one verifier call died mid-response on a transient API error and was recovered by resuming the same run from cache (11 of 12 agent calls replayed instantly, only the failed one re-ran). 2 of 6 initial verdicts needed a real correction under verification, not just confirmation: **§6.1** (4+ duplicate AO3-href-to-absolute-URL resolvers, turned out to be 10 across 6 named functions + 4 inline) — the duplication is real, but the finder's "credential-leak risk" framing was refuted: every write path funnels through `AO3AuthService.authenticatedRequest`'s `isTrustedURL` gate regardless, so this is a correctness/robustness cleanup, not an open vulnerability; **§6.6** (`WorkDetailView.withLocalWork` vs `ReadingQueueService.resolveLocalWork`) — the consolidation itself is still confirmed safe, but the finder's specific safety argument (leaning on `PreservedWorkService.softDelete`'s docstring) was wrong — `ReadingQueueService.normalize(_:)` can leave a soft-deleted work's EPUB missing on disk, contradicting the docstring's premise — the verifier traced the *actual* safety net (the `hasReadableEPUB`/`restoreReadableEPUB` self-heal chain at read-time, plus `preserve()`'s own recheck at queue-add) that makes the consolidation safe for a different reason than originally claimed. The other 4 verdicts held under verification: **§6.2** (`SyncMerge.deterministicMembershipOrder`) confirmed dead, safe to delete, as mechanical as any §3 finding; **§6.3** (`FolderSyncService.foldConflictContents`) confirmed *not* a simple duplicate — found a real bug along the way (production's `foldFileProviderConflicts` silently discards restore summaries, so the sync-result counts shown in Settings undercount whenever a File-Provider conflict is folded), needs an owner product decision, not a delete; **§6.4** (`AccountWorksInlineSection` vs `AO3AccountWorksList`) and **§6.5** (`AccountView`'s two root layouts) both confirmed genuinely distinct / mostly false positives — retain, close both leads. Full per-lead evidence, citations, and recommended next actions in the audit doc's new §10. **Session E follow-through implemented and committed** (`cc7ae02`/`db52216`/`099a727`, one commit per finding matching the Session C convention): (1) §6.2 — deleted `SyncMerge.deterministicMembershipOrder` + its sole test, zero behavior change. (2) §6.3 — owner decided to fix the undercount: `foldFileProviderConflicts` now returns a `FolderSyncResult` (was a bare `Int`) via the same `absorb`-per-version pattern `foldConflictContents` already used, keeping the exact per-version throw/resolution semantics; new `FolderSyncResult.absorb(_ other: FolderSyncResult)` overload lets `performSyncDown` fold it into its own total instead of only keeping the count; new unit test on the overload itself (the NSFileVersion conflict path stays untestable in isolation, unchanged limitation). (3) §6.6 — `WorkDetailView.withLocalWork` now calls `ReadingQueueService.resolveLocalWork` directly (mirroring `WorkCardActions.performRemoteAction`'s existing wrapper pattern), deleting the duplicated download/chapter-count/import/apply-metadata sequence and the now-unused `applyRemoteMetadata` wrapper; the diff cites the *corrected* safety argument from §10.6 (the `hasReadableEPUB`/`restoreReadableEPUB` self-heal chain at `read()` + `preserve()`'s own recheck at queue-add), not the disproven `softDelete` docstring premise; new regression test locks in that `resolveLocalWork`'s fast path revives a soft-deleted work with a genuinely-missing EPUB without silently marking it readable. §6.1 (href resolvers) and §6.4/§6.5 (retained leads) were left untouched, per scope — §6.1 was flagged as a future correctness cleanup, not urgent, and wasn't part of what was greenlit this round. **Verified:** `Scripts/verify.sh` ALL GREEN — iOS **562 tests / 53 suites** (net +1: two new regression tests, one deleted dead-code test), macOS Debug build, whitespace, lint clean in every touched file. **Merged into `release-fixes` (`0f68363`, 2026-07-16) on owner instruction after the live-test fixes landed** — the merged tree is byte-identical to the verify.sh-ALL-GREEN branch tip. **Live-test fixes (2026-07-16):** the owner's simulator + physical-iPhone pass found 5 issues; all resolved (one commit per fix), continuing the interrupted first fix session's uncommitted work after re-verifying every change against the code. (1) Comment "Edit" menu item missing → **expected AO3 behavior, not an app bug** (`320e690`): otwarchive's `comments_helper.rb#can_edit_comment?` renders Edit only on the session's *own* comment with *zero replies* and not frozen, while Delete additionally appears on any comment on the user's own works — a replied-to comment showing Delete-but-no-Edit is AO3's rule, and the app's contract is to expose exactly what AO3 rendered; parser confirmed correct against live markup, fixture action-list realigned to the live capture (`data-remote` links, `delete_comment?id=` href shape, Freeze-Thread `button_to` the anchor-only parser skips). (2) Work-level Subscribe/Bookmark labels never reflected state (`f6f01f2`): new advisory `AO3AuthService.workActionStates` work-page read (fetched when menu content appears, signed-in only, once per work + refetched after each relevant write) + new `AO3Client.parseExistingBookmark` (AO3 embeds the user's bookmark as an *edit* form — `/bookmarks/<id>` + `_method=put`, prefilled — vs. a create form when unbookmarked; both captured live into fixtures) drive conditional "Unsubscribe"/"Edit Bookmark on AO3" labels; the composer prefills the existing bookmark and `saveBookmark` (was `createBookmark`) routes create-vs-update from the *fresh* work page at submit time so a stale label can never duplicate-create, carrying collection names through untouched; +2 tests. (3) "Mark for Later" always failed (`588a712`): AO3's control is a `button_to` carrying `_method=patch` (captured live) — the body was missing the Rails method override so the POST matched no route; pre-existing bug (Session B's CSRF-helper rename was byte-identical), never live-verified before the owner's pass. (4) Block/Mute confirm silently no-op'd (`99fbb50`): SwiftUI writes the alert's `isPresented = false` *before* running the tapped button's action, and the binding's dismiss handler called `cancelPendingModeration()` — clearing the very submit snapshot that exists to survive dismiss — so Confirm's Task found nothing to submit; new dismiss-only `moderationAlertDidDismiss()` preserves the snapshot (explicit Cancel still drops both), +2 fence tests locking in dismiss-vs-cancel; the rest of the chain audited clean — `submitScrapedHTMLForm` submits the scraped fields (incl. `_method`) wholesale over the same `writeRequest`/`submitWrite` path the live-confirmed author-Subscribe uses, and `parseAuthorModerationForm` scrapes every `input[name]` of the matched blocked/muted form (the alert rendering correct labels live proves the parse), so the cleared snapshot was the whole bug. (5) Recently Deleted restore invisible (`d244374`): restore existed only as a leading swipe — undiscoverable on iOS, and no mouse path at all on macOS — rows now also expose Restore/Delete-Permanently in a context menu (long-press iOS, right-click macOS), same handlers. Regression-matrix rows updated (`aaa935c`). **Verified:** `Scripts/verify.sh` ALL GREEN at the tip — iOS **566 tests / 53 suites** (+4: 2 dismiss-vs-cancel fence, 2 work-page-state), macOS Debug build, lint pre-existing-only, whitespace; the fixed build is installed on the iPhone 17 simulator ready to test. **Next: owner live-tests the 5 fixes (esp. the never-live-verified writes: mark-for-later POST, bookmark save/update + label states, block/mute confirm submit) + runs the §9 manual checks (live-session Subscribe/Block/Mute + selection/bulk/pagination on the 4 remote-work surfaces), decides on §6.1 (correctness cleanup, not urgent) if/when wanted, and reviews everything (merge to `release-fixes` already made at the owner's instruction; release-fixes-to-main stays a separate later call).** |
| T-103 | **Comments audit fixes Part C — account/generation isolation (CAA-3, CAA-11, CAA-16).** Scope Comments page/chapter caches, async loading/submission/verification, and drafts to immutable authentication identity + session generation; prevent stale continuations and anonymous fallback. | Codex (finished by Claude after Codex ran out of usage mid-review-fix) | `codex/comments-account-isolation-caa3` (from `origin/release-fixes` `a3a3966`) | ✅ DONE | **CAA-3:** `CommentsModel` captures an immutable `AuthContext` (bare-username identity / cache scope / `sessionGeneration`) snapshot per load/submit/verification and re-checks it after every await (`isCurrent`); `CommentsPageCache.Key` **and** the chapter-index cache carry scope + generation, so a same-username relogin cold-loads and an owner's `/navigate` draft-chapter titles never serve across accounts or sign-out; required reads/verification surface auth-request construction failure as `AO3Error.authenticationRequired` (no `try?` anonymous downgrade — the last two sat in `verifyCommentPosted`, now throwing); `CommentsView` resets + force-reloads (or, for Inbox destinations, dismisses) on generation change via one `onChange(of: authenticationKey)`. **CAA-11:** draft keys are account-scoped (`identity|w-c-p`; unknown-username sessions stay `unknown-session:<generation>`-local; the "AO3 Account" placeholder never becomes an identity), the same bare-username identity now flows through adoption/submission/verification-clearing/logout consistently (Sol finding 5), and `postComment`/`postCommentReply`/`editComment`/`deleteComment` take a required `expectedGeneration` re-checked via `requireSessionGeneration` at entry **and** immediately after the form GET, so an account switch between form GET and POST aborts pre-POST (Sol finding 1); stale successful/failed submits and stale verifications are inert against the replacement account's composer/drafts/guard/page (findings 2, 8), `CommentSubmissionGuard.resetForAuthenticationChange()` unwinds busy presentation state without touching durable entries (finding 3), transition reset also clears `scope`/`selectedChapter`/`workContext` (findings 4, 9), and `CommentsView.delete` captures + fences its generation end-to-end. **CAA-16:** verification captures `pendingKey` (and identity) *before* resolution nils it, and `CommentDraftStore.clearVariants` clears every chapter sibling on verified or definitive success (finding 6). **Inbox-destination hole (Sol follow-up):** `AccountView` now passes `destination.sessionGeneration` → `CommentsView(requiredSessionGeneration:)`; every public `CommentsModel` entry is gated by `belongsToCurrentSession`, so a pre-first-load account change renders nothing and dismisses instead of loading Alice's private context under Bob. **Post-cutoff finish (Claude):** Codex's session died mid-final-re-review with 2 tests failing on the runner's second same-process suite execution (process-global `CommentsPageCache` static + test fakes restarting generation numbers → run-2 cache-key collisions); fixed by injecting the cache (`CommentsModel(pageCache:)`, production unchanged via `CommentsPageCache.shared`) with a fresh instance per test — the exact failing invocation (parallel-testing double execution) now passes 22/22; also moved `cachedCount` into `AccountView`'s existing private extension because the one-line `requiredSessionGeneration:` addition pushed the struct body to 901 lines (SwiftLint hard error at >900, caught by verify's lint gate after Codex's last green run). **Tests:** new `CommentsAccountTransitionTests` (11 deterministic cases: gated stale page/chapter completions inert, stale Inbox destination never loads, same-username relogin cold-loads, no anonymous fallback incl. verification throwing `authenticationRequired`, drafts per-account + restored on relogin, unknown-username drafts generation-local, stale submit/verification can't touch Bob's composer/guard/drafts, stale owner Edit/Reply/Delete refuse, chapter→work-scope verified success clears sibling drafts) + `CommentSubmissionTests` store-level variant-clearing case; reuses the Inbox suite's `Signal`/fakes (made internal). **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (pre-existing warnings only), iOS **552 tests / 52 suites** (iPhone 17 / iOS 26.5; **540 → 552** from the Part B baseline), macOS Debug build, whitespace; targeted double-execution run additionally green (22/22). Audit report §5 CAA-3/11/16 statuses updated; `AO3_NETWORKING_POLICY.md` Comments row + `REGRESSION_TEST_MATRIX.md` native-comments row updated. **No live AO3 requests were made; write endpoints remain never live-verified by an agent — owner live pass stays the release gate.** **Merged into `release-fixes` 2026-07-15** (commit `6f8df38`, alongside T-101/T-102). **Next:** Part D (CAA-8 + CAA-9), not started. |
| T-101 | **Package license notices and correct release documentation** — close release-blocking A10-F1 (standalone distributions omit GPL/dependency notices) and doc-accuracy A10-F2 (README/EPUBParsing/AO3Authentication drift) from `docs/RELEASE_READINESS_FABLE5.md`, per `Fable5_Release_Fix_Prompts/07_fix_distribution_licenses_and_docs.md`. | Claude | `release-fixes` (worktree `.claude/worktrees/license-notices-a10f1`, base `277c113`) | ✅ DONE | **A10-F1:** root `LICENSE` (GPL-3.0) and a generated `kudos-ao3-reader/Legal/ThirdPartyNotices.txt` (one full-text entry per pinned `Package.resolved` identity: CryptoSwift, DifferenceKit, Fuzi, GCDWebServer, SQLite.swift, `swift-toolkit`, SwiftSoup, Zip, ZIPFoundation) are checked in under `kudos-ao3-reader/Legal/`, inside the app target's `PBXFileSystemSynchronizedRootGroup` — no pbxproj edit needed, both files bundle automatically on iOS and macOS. New `Settings/LegalNoticesView.swift` reads either file from `Bundle.main` and renders it full-text/monospaced/selectable; `AboutView` gained two `NavigationLink`s to it plus expanded per-dependency credit rows naming all nine packages (was two: SwiftSoup, Readium). `Scripts/check-invariants.sh` gained check #9: fails if either Legal file is missing, if the bundled `LICENSE.txt` drifts from the root `LICENSE`, or if any `Package.resolved` identity has no matching entry in `ThirdPartyNotices.txt`. **A10-F2:** README's "Branch strategy" now describes the actual gated `main`/`merge-test` workflow (links `AGENTS.md`) instead of the retired single-branch claim; its "imports merge safely" claim was checked against A2-F1/A5-F3 specifically and left unqualified since both are already resolved (T-92); `docs/EPUBParsing.md` now states `WorkImporter.importEPUB` uses legacy `EPUBMetadata` rules on **both** platforms today and that `ReadiumMetadataMapper` is unused dead code (unshipped follow-up, matches Area 7's recorded drift); `docs/AO3Authentication.md` now names the four write clients that ship today (`AO3WriteActions`, `AO3CommentActions`, `AO3PreferencesActions`, `AO3InboxActions`) instead of describing account writes as future work. **Found in-progress, uncommitted work already on this worktree** (all diffs above pre-existed from a prior session) — verified every line rather than trusting it, then finished what was missing: cross-checked all 9 `Package.resolved` identities against `ThirdPartyNotices.txt` (all present), confirmed the AGENTS.md anchor the README links to is real, confirmed A2-F1/A5-F3 are genuinely resolved so "merges safely" needed no hedge, confirmed no other README claim (privacy/platform requirements) was stale. **Verified against real built artifacts, not just source:** clean macOS Release build and clean iOS Simulator Release build both produced; `LICENSE.txt`/`ThirdPartyNotices.txt` located in both bundles (`Kudos.app/Contents/Resources/` on macOS, `Kudos.app/` flat on iOS) and diffed byte-identical against the checked-in source; `LegalNoticesView` symbols confirmed compiled into the macOS binary via `nm`. **Live UI walkthrough** (macOS Release app launched and driven via computer-use): Account → Settings gear → About Kudos → "Read the Full License Text" renders the real bundled GPL text; back → scrolled all 9 dependency rows → "Read Full Third-Party Notices" renders the real bundled notices document; both screens scrollable/selectable, both work fully offline. (Noted but out of scope, pre-existing and unrelated to this diff: the About screen's License-summary `Text` renders literal `**...**` instead of bold — built via runtime string concatenation, not a markdown-parsed literal; not touched.) **Verified:** `Scripts/verify.sh` ALL GREEN — invariants (incl. new check #9), lint (pre-existing warnings only), full iOS suite **522 tests / 51 suites** (iPhone 17 / iOS 26.5), macOS build, whitespace; separately confirmed both dedicated Release builds (iOS Simulator + macOS) succeed and bundle the notices correctly (see above). Audit ledger (`docs/RELEASE_READINESS_FABLE5.md`) updated: A10-F1/A10-F2 marked resolved with evidence: Executive Verdict (1 open P1 → 0), Area 10 summary row, Open Findings P1/P2 lists, the GPL/notices gate row (FAIL → PASS), and the Exact Release Recommendation section all updated. **Not done (explicitly out of scope):** a formal legal review of the generated notices before store submission is a recommendation, not a finding — remains manual. **Merged into `release-fixes` 2026-07-15** alongside T-102/T-103. |
| T-98 | **Scrolled-reader hang — diagnosis + fix** — intermittent freeze after short Scrolled-mode reading. | Grok | `reader-scrolled-hang-diagnosis` (from local `release-fixes`, **not** merged to `main`/`release-fixes`) | ✋ AWAITING OWNER SMOKE | Diagnosis: `SCROLLED_READER_HANG_DIAGNOSIS.md`. **Fix + adversarial review:** `docs/SCROLLED_READER_HANG_FIX_ADVERSARIAL_REVIEW.md`. Debounced locator writes; flush on dismiss/disappear/background; stamp split; viewport end flips only; folder-sync token quantize. **Review fixes:** completion path advances `progressModifiedAt`; trailing write only for meaningful deltas; `markDirty()` on debounced/flush writes (no mid-scroll syncUp). **Verified:** iOS **486 tests / 49 suites** green. **Next step:** owner scrolled-mode smoke then merge decision for `release-fixes`. |
| T-97 | **Comments & Inbox vs. `ao3_api` audit (review-only)** — audit the native comments/inbox implementation against `wendytg/ao3_api` master, with `otwcode/otwarchive` as tie-breaker authority. | Claude | `claude/api-audit-codex-review-09bf99` (reset to the `release-fixes` tip `4e57337c` so the audited tree includes T-95/T-96) | ✋ AWAITING HUMAN REVIEW | Report: `docs/COMMENTS_AO3_API_AUDIT.md` (audited Kudos `4e57337c`, ao3_api `02e34998`, otwarchive `41d3878b`). **16 findings: 3 High / 8 Medium / 5 Low.** Highs: CAA-1 comment POSTs omit `comment[pseud_id]` (replies always — the plain `/comments/<parent>` page renders no form; top-level for single-pseud accounts — hidden input not parsed; otwarchive applies guest validations and rejects); CAA-2 write success inferred from absence of known error selectors (`.flash.comment_error` unmatched; delete never scans the body); CAA-3 comments page/chapter caches and fetches not isolated by `sessionGeneration` (T-96 pattern not applied to Comments; `/navigate` owner draft-chapter titles can cross accounts; `try?` downgrades signed-in fetches to anonymous). Notable moderating discovery: otwarchive rejects byte-identical duplicate comment content server-side (`comment.rb:84-88`), softening most guard-residue findings. Zero live AO3 requests; no production code, tests, or fixtures touched; only this row + the report were written. **Next step: human reviews the report and explicitly approves specific findings before any implementation.** |
| T-95 | **Inbox reply verification and durable duplicate-post guards** — close release-blocking findings T91-RF1 (ambiguous Inbox replies verified a guessed work-comments page instead of the exact parent thread) and T91-RF2 (the unresolved duplicate-post guard was model-local and lost across target changes/screen recreation) from `docs/RELEASE_READINESS_FABLE5.md`. | Claude | `release-fixes` (developed on `claude/inbox-duplicate-guards-470eba`, based on `claude/reader-completion-progress-fa8b77`) | ✅ DONE | **T91-RF1:** `AO3AuthService.verifyCommentPosted` (`Services/AO3CommentActions.swift`) now routes a reply's verification through a pure `verificationPlan(for:)` — a reply always resolves to `.standaloneThread(parentCommentID:)`, fetched via `AO3Client.commentThreadPage(commentID:)` (`/comments/<parentCommentID>`), never a work-comments page guess; top-level comments keep the existing page-1/last-page plan. The prior `knownPage` parameter (which fed a hardcoded `currentPageNumber = 1` from focused-thread loads straight into a work-comments page fetch — the exact false-negative-into-`.absent` double-post path the finding described) is gone. `containsComment` gained an optional `postedAfter` timing check (10-minute tolerance for clock skew/minute-granularity AO3 timestamps) so a coincidental old reply with the same author/parent/text can't be mistaken for proof THIS attempt landed; when a candidate's timestamp didn't parse, the check is skipped rather than false-negatived. **T91-RF2:** new `UnresolvedCommentSubmissionStore` (`Services/CommentSubmission.swift`) is a process-lifetime, auth-scoped (keyed by identity, then by full `CommentSubmissionKey`) record of ambiguous submissions that every `CommentSubmissionGuard` instance reads/writes through instead of holding a single local `pendingKey` slot. `CommentsModel.submissionGuard` shares `UnresolvedCommentSubmissionStore.shared`, so a guard recreated by target switching, popping to Inbox and reopening, or Comments-screen recreation rehydrates its blocked state from the store via the new `adopt(_:)` (called from `startComposer`, now `auth`-scoped) instead of resetting to idle. Distinct keys (different target, or genuinely different text) coexist independently — resolving one never touches another. `AO3AuthService.logout()`/`clearStoredSession()` each capture the outgoing session's username before clearing it and call `store.clear(identity:)`, so one account's unresolved state can't leak into or block whichever session follows. Entries are in-memory only (no cookies/CSRF/HTML/persisted-to-disk text) and expire after 1 hour if never resolved. **Tests (18 new):** `AO3CommentsParseTests` +7 (`verificationPlan` always resolves a reply to the standalone thread regardless of "page," a top-level context resolves to `.workComments`, a standalone-thread-shaped page containing the reply matches, timing tolerance accepts a plausible-clock-skew match / rejects a pre-existing unrelated reply / skips the check when `postedAt` didn't parse); `CommentSubmissionTests` +11 (A→B→A distinct-target isolation, ambiguous-A-pop-reopen, brand-new-guard model recreation, resolving one key leaves another blocked, cross-account key isolation, logout clears the logged-out account's entries, three concurrent unresolved keys never collide, store anchors `submittedAt` to the *original* attempt across a "Check Again" re-mark, `maxAge` eviction, `adopt` shows idle for an untracked key, `adopt` never clobbers an in-flight submission). **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (pre-existing warnings only), full iOS suite **446 tests / 47 suites** (iPhone 17 / iOS 26.5), macOS Debug build, whitespace; iOS + macOS **Release** builds also green. Audit ledger updated (T91-RF1/RF2 resolutions, Area Summary rows, verdict 4 → 2 open P1). **Not touched (out of scope per prompt):** T91-RF3 (account-switch write race) — explicitly deferred to the next prompt. **Minimal live-AO3 check remaining:** a real ambiguous reply (kill connectivity mid-POST) against a parent thread seeded on a later work-comments page, confirming the standalone-thread fetch finds it and no duplicate posts; write actions remain never-live-tested by an agent, per existing project policy. **Follow-up (2026-07-13, review hand-back):** a second-pass review found two residual guard-escape paths, both closed same day. **P1:** `CommentsModel.reverify` verified the composer's *current* text (editable while `.ambiguous`, Check Again stays visible) instead of the pending submission's actual body — editing then tapping Check Again could authoritatively verify the wrong text, get a spurious `.absent`, and release the block on the real unresolved original (enabling a genuine duplicate on retry). Fixed with a new pure `CommentsModel.verificationTarget(pendingKey:composerContext:)` (deliberately takes no composer-text parameter) that `runVerification` now goes through exclusively, always resolving `pendingKey.normalizedBody`; added `syncSubmissionGuardToComposerText` (called from the composer's text `onChange`, scoped to `.ambiguous` only) so editing away from the blocked text exits that display immediately instead of showing a stale banner. **P2:** `CommentSubmissionKey` carried the requesting screen's `chapterID`, so the same logical reply got different keys (and a disconnected block) from an Inbox focused thread vs. a work-comments screen; fixed by normalizing `chapterID` out of the key in `CommentSubmissionKey.init` (both endpoints are chapter-agnostic). Also hardened: `resolveAmbiguity(.unknown)` no longer risks a stuck `.verifying` with a nil `pendingKey`; `verifyCommentPosted`'s standalone-thread branch now requires the fetched page to actually contain the requested parent before trusting an `.absent`. **Tests (+6):** chapter-scope key equality (replies + top-level), cross-scope block enforcement, `verificationTarget` correctness (ignores composer text, nil-pendingKey case, found-resolves-success), and the exact edit-then-Check-Again regression repro proving only the original key resolves while a distinct edited key stays independently postable. **Verified:** `Scripts/verify.sh` ALL GREEN — **452 tests / 47 suites**, iOS + macOS Debug + Release builds green. Audit ledger's T91-RF2 resolution note amended with this follow-up. |
| T-94 | **Reader completion and macOS progress** — close release-blocking findings A7-F1 (iOS marks a work finished at 99% totalProgression and may free its EPUB before true completion) and A7-F2 (macOS never persists or restores intra-chapter reading position) from `docs/RELEASE_READINESS_FABLE5.md`. | Claude | `release-fixes` (developed on `claude/reader-completion-progress-fa8b77`) | ✅ DONE | **A7-F1:** removed the `>= 0.99` shortcut from `ReadiumReaderView.openBook`'s locator callback; progress persistence and completion are now separate signals. True end = the navigator's own last-resource/end state via Readium 3.9.0's `ViewportObservingNavigator`: new pure `ReadiumReaderCompletion.isAtEnd` (`Features/ReaderReadium/ReadiumReaderCompletion.swift`) requires the final reading-order resource visible with its intra-resource trailing edge at **exactly 1.0** — the value Readium clamps to at a resource's end and itself compares `== 1.0` against — so no float tolerance can re-admit the defect; `totalProgression` (the *leading* edge, inexactly interpolated) is never consulted. `ReadiumBook` implements `navigator(_:viewportDidChange:)` and fires rising-edge `onReachedPublicationEnd`; the view finishes only `isComplete` works there (WIPs stay manual); `.onDisappear`'s `freeEPUBIfFinished` stays gated on that verified `isFinished` + unchanged `isProtected`. **A7-F2:** the legacy layout script now maintains one normalized semantic position for both modes (`window.__fraction` = content fraction at the viewport's leading edge: `scrollY/scrollHeight` scrolled, `page/total` paged), posts it rAF-throttled, re-derives page/scroll from it in `applyPaged`/`applyScroll` (mode switches, style reflows, and a now-both-modes resize handler preserve the spot), and exposes `readerRestore(fraction)` invoked post-`didFinish`+inject. Every script message carries a per-load `gen`; `ReaderController` increments it per load and platform-neutral `ReaderBridgeMessage.parse` (`Features/Reader/ReaderProgressBridge.swift`) drops stale-generation messages (old-chapter callbacks can never overwrite current state; paged page/total and scroll-bottom events are newly gated too). Persistence reuses the existing `lastSpineIndex` + previously reader-unused `lastScrollFraction` (both already in backups + folder sync), written through `ReaderProgressBridge`'s debounce (≥2s between streamed writes, 0.001 noise floor) with guaranteed flushes on dismissal (+explicit save), chapter transitions (spine+fraction as one pair), mode/style transitions, and `NSApplication.willTerminate`. Chapter revisits restore a per-session remembered fraction (A→B→A lands at A's spot); explicit same-chapter re-pick resets to top; backward paging keeps last-page landing; `WorkReaderPreparation.restoreReadableEPUB` clears the fraction with the spine so re-downloads can't restore stale positions. **Tests (25 new):** `ReadiumReaderTests` +7 completion cases (0.99 / 0.999 / mid-final-resource don't complete; true end does; non-final end doesn't; two-resource spread; nil guards); new `WorkLifecycleCompletionTests` (4: unfinished at 0.99/0.999 keeps EPUB → offline reopen intact; protected finished keeps EPUB; unprotected finished frees); new `ReaderProgressBridgeTests` (14: stale-gen gating, message shapes, exact paged round-trips incl. totals 3/7/313, containing-page mapping, proportional reflow remap, mode-switch fixed point, A→B→A memory, seeded reopen, same-chapter reset, debounce/noise/flush). **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (pre-existing warnings only), full iOS suite **428 tests / 47 suites** (iPhone 17 / iOS 26.5), macOS build, whitespace; **iOS and macOS Release builds** green. Audit ledger updated (A7-F1/A7-F2 resolutions, Area 7 rows, verdict 6 → 4 open P1). **Still owed (manual gate, unchanged):** real-device/simulator read-throughs — finish a real work at its true end (iOS), macOS scrolled/paged midpoint reopen + mode-switch + resize + A→B→A + quit-relaunch. |
| T-92 | **Backup and EPUB archive integrity hardening** — close release-blocking findings A2-F1 (stale-archive tag clobber), A5-F2 (unsafe MiniZip archive input), and A5-F3 (unvalidated backup EPUB replacement) from `docs/RELEASE_READINESS_FABLE5.md`. | Claude | `claude/backup-archive-integrity-fd21df` | ✅ DONE | **A2-F1:** `KudosBackupService.restore`'s per-work tag merge (`KudosBackup.swift`) no longer replaces `work.tags`; it unions in only archived tag names missing locally (exact `Tag.name` match) and never removes a tag absent from a stale archive — there's no per-tag tombstone, so removal is never inferred from absence. **A5-F2:** `MiniZip.swift` rewritten around a throwing `init(data:) throws` (was failable `init?`): checked-arithmetic bounds on every central-directory/local-header range, signature checks, stored-vs-declared size consistency, an allow-list of method 0/8 with the encrypted-flag bit rejected, and per-entry (200 MB) / cumulative (500 MB) / compression-ratio (1100:1) limits — all enforced before any `subdata` or `Data(count:)` allocation. `unzip(to:)` normalizes/rejects unsafe entry names (absolute, `..`, backslash, drive/scheme-like) and extracts into a **fresh staging directory** first, replacing the real destination only after every entry validates — a hostile archive can never leave partial or unsafe output behind. `EPUB.swift`/`WorkImporter.swift` updated for the throwing initializer (`try?`, no behavior change for valid EPUBs). **A5-F3:** `KudosBackupService.restore` now stages every incoming EPUB blob and preflights it through the same hardened validator `ReadingQueueService.replaceEPUB` already uses for AO3-download/user-import replacement (`EPUBDocument.inspectPackage`, backed by the hardened MiniZip), so backup and folder-sync restore (which both funnel through this one function) share the identical safe path. Policy: invalid assets are **skipped with a recoverable result** (new `KudosBackupRestoreSummary.skippedInvalidEPUBs`, surfaced in `conflictMessage`) rather than failing the whole restore transaction; the existing file, `hasEPUB`, and preservation state are left untouched. **Tests:** new `KudosTests/MiniZipHostileTests.swift` (11 hand-assembled hostile-ZIP fixtures — traversal, absolute/backslash/drive-letter paths, truncated record, oversized declared size, implausible ratio, cumulative-size limit, encrypted entry, unsupported method, plus a valid-EPUB round trip); `KudosBackupTests.swift` gained `staleArchiveWithoutTagsDoesNotRemoveNewerLocalTag`, `repeatedRestoreOfArchiveTagsIsIdempotent`, `invalidBackupEPUBLeavesValidLocalEPUBUnchanged`, `tombstoneSuppressedWorkDoesNotWriteEPUBBlob`; `FolderSyncTests.swift` gained `syncDownRejectsInvalidEPUBWithoutOverwritingLocalCopy` (proves the shared validation path end-to-end). The two prior positive backup tests that used arbitrary strings (`"restored-epub"`, `"queued-epub"`) as EPUB bytes now use the real minimal `KudosTests/Fixtures/sample.epub`. **Verified:** `Scripts/verify.sh` invariants/lint green (no new warnings); full iOS suite **387 tests / 43 suites** green (`iPhone 17`, iOS 26.5); macOS build green; whitespace clean. Audit ledger (`docs/RELEASE_READINESS_FABLE5.md`) updated: A2-F1/A5-F2/A5-F3 marked resolved with evidence, Executive Verdict/Area Summary/Open Findings counts updated (11 → 8 open P1). **Not touched (out of scope per task):** A5-F1 (cookie isolation), A5-F4 (Keychain deletion), A2-F2 (P3 stale-but-valid blob overwrite), backup v7 schema, reader architecture. **Follow-up (2026-07-13, code review hand-back):** found and closed a validator/extractor asymmetry — entry-name safety was only checked during `unzip` (extraction), not `MiniZip.init` (validation), so `EPUBDocument.inspectPackage` (the A5-F3 preflight) could pass a structurally-valid EPUB carrying one extra, unsafely-named entry the OPF never referenced; `replaceEPUB` would overwrite a valid local EPUB with it, only failing later at real open time on the macOS legacy reader. Fixed by validating every entry name during `init`, matching extraction; bundled the same-cause duplicate-entry-name divergence (`data(named:)` first-match vs `unzip` last-wins) into a rejection too. New shared `KudosTests/HostileZipFixture.swift` (extracted from `MiniZipHostileTests`, now also used by `KudosBackupTests`); new tests `structurallyValidEPUBWithUnreferencedHostileEntryIsRejected`, `duplicateEntryNameIsRejected`, and `backupEPUBWithUnreferencedHostileEntryLeavesValidLocalEPUBUnchanged` (proves the exact scenario end-to-end through real `KudosBackupService.restore`). Re-verified: invariants/lint clean, full iOS suite **390 tests / 43 suites** green, macOS build green (both independently re-run this pass, not just claimed). |
| T-93 | **Authentication isolation and logout hardening** — close release-blocking findings A5-F1 (anonymous AO3 reads inherited the shared authenticated cookie jar) and A5-F4 (Keychain deletion failures were suppressed while the UI reported logout) from `docs/RELEASE_READINESS_FABLE5.md`. | Claude | `claude/auth-isolation-logout-dc9aae` | ✅ DONE | **A5-F1:** `AO3Client`'s single session (`AO3Client.swift:23-49`, new `makeAnonymousSessionConfiguration()`) now runs with `httpCookieStorage = nil`, `httpShouldSetCookies = false`, `httpCookieAcceptPolicy = .never` instead of `URLSessionConfiguration.default` — it can no longer read or write any cookie jar, so a signed-in session's cookie can never ride along on a nominally anonymous search/browse/tag request. `AO3CookieBridge.install`/`clearAO3Cookies` (`AO3SessionVault.swift:295-321`) stopped mirroring into `HTTPCookieStorage.shared` (the very store the anonymous client used to read from) — only WebKit's own cookie store is touched now, which is all login/browse WebViews ever needed. The authenticated coalescer's key is now the extracted, testable `AO3Client.authCoalescingKey(url:cookieHeader:)`; it was already URL+Cookie-scoped per account and structurally separate from the anonymous URL-only coalescer, so closing the cookie leak closes the account-bridging risk too. **A5-F4:** `CascadingAO3SessionVault.delete()` (`AO3SessionVault.swift:218-292`) always attempts both the Keychain and file deletes now (the old flow re-threw a Keychain failure before ever calling `file.delete()`); both stores are protocol-typed for test injection. New non-secret `AO3SessionRemovalTracking` flag (`UserDefaultsAO3SessionRemovalTracker`) is set whenever a durable delete fails and cleared on success (or a fresh login). `AO3AuthService.logout()` still clears all soft/live state (memory, WebKit/HTTP cookies, username hint) unconditionally, but only reports "Logged out of AO3." when the vault delete actually succeeds — on failure it surfaces a retryable notice instead, now visible on `PrivacyDataView` right where the user tapped Remove AO3 Session. `restoreSession()` checks the pending flag before ever calling `vault.load()`, so a session that couldn't be durably deleted can never be restored on relaunch, no matter what it contains; the same `try?`-free semantics apply to the expiration-cleanup path. **Tests:** `CascadingAO3SessionVaultTests` (4 cases: both-stores-attempted, both-succeed, missing-entitlement-ignored, file-only-failure-surfaced), `AO3CookieBridgeTests.installNeverWritesToTheSharedHTTPCookieStorage`, `AO3ClientPolicyTests` gained 3 cookie/coalescing-scope cases, and `AO3AuthServiceTests` gained 3 cases (`logoutSurfacesRetryableFailureInsteadOfClaimingSuccess`, `pendingRemovalRefusesToRestoreStaleSessionOnRelaunch`, `pendingRemovalRetrySucceedsAndClearsPendingState`) plus every existing case updated to inject an isolated in-memory removal tracker (avoids `UserDefaults.standard` cross-test leakage). **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint (warnings only, pre-existing, none in touched files), **382 tests / 44 suites**, iOS + macOS builds, whitespace clean. **Not covered (documented, out of scope):** `AO3Client.downloadEPUB` has no authenticated variant, so a "registered users only" restricted work's EPUB download — which previously could only succeed by riding the now-removed ambient cookie — now behaves like any anonymous request; adding an authenticated download path is a separate feature change. Signed-device Keychain verification (a real locked/unavailable Keychain) remains a manual gate. This branch was developed from pre-T-92 `main` (37987dd) as a sibling of T-92's `claude/backup-archive-integrity-fd21df`; both are now reconciled together in this merge into `release-fixes`. **Follow-up fix (same day, review-caught upgrade gap):** the initial pass removed both the write and delete of `HTTPCookieStorage.shared` but never purged what a pre-fix, signed-in build had already left there — a persistent "remember me" cookie with nothing left in the app to ever delete it, still live-reachable via `AO3AuthorAvatar`'s `AsyncImage` (`URLSession.shared`, same default cookie jar) against guaranteed AO3-host avatar URLs. Added `AO3CookieBridge.purgeLegacySharedCookieJar()` (`AO3SessionVault.swift`). **Second follow-up (same day, review-caught ordering race):** the purge was first called from the top of `restoreSession()`, but that only runs from the root view's `.task`, and SwiftUI gives no ordering guarantee between sibling `.task`s — a sibling avatar's own `AsyncImage` fetch could in principle win the race on the very first post-upgrade launch and carry the cookie once before the purge ran. Moved the call into `AO3AuthService.init()` instead: `auth` is a `@State` initializer (`ContentView.swift:23`), which SwiftUI always evaluates before that view's `body` — and therefore before any `.task` in the tree — can run, closing the race structurally. Test renamed/strengthened to `initPurgesLegacyCookieFromTheSharedHTTPCookieStorageBeforeAnyTaskCanRace`, now asserting the cookie is gone immediately after construction, before `restoreSession()` is ever called. Re-verified both times: `Scripts/verify.sh` ALL GREEN, **384 tests / 44 suites**, iOS + macOS builds. **Regression found 2026-07-14 (T-100):** disabling cookie handling outright (`httpCookieStorage = nil`, `.never`) also discarded Cloudflare's own `cf_clearance`/`__cf_bm` cookies, so every request looked like a brand-new client and AO3 started serving a CAPTCHA/managed challenge; separately, it silently dropped the `Set-Cookie` on a write's redirect (otwarchive re-signs `_otwarchive_session` there to carry the flash message via Rails' `CookieStore`), which a later Comments-write-path change's honest-success-reading logic (T-99) exposed as false "couldn't confirm" outcomes. See T-100 for the fix (private ephemeral cookie jar for Cloudflare's cookies only, plus a redirect-cookie relay for the auth cookie) — A5-F1's actual invariant (the auth cookie never lives in shared/ambient state) is preserved throughout. |
| T-91 | **AO3 Inbox v2** — native bulk read/unread/delete, filter/sort controls, and current-page select mode using AO3's parsed inbox forms. | Codex | `inbox-refinement` | 🔄 IN PROGRESS | Parsed fixture-backed bulk/filter forms and implemented current-page selection/actions. Inbox cards share Comments' Guest/User/Author/Me chips, paced avatars, Reply capsule, green trailing-check Replied badge, and the same ellipsis control. Me resolves by canonical account path and wins over Author; Author resolves coauthors/alternate pseuds by canonical creator identity without pseud-name or guest false positives; explicit Anonymous Creator remains provenance-gated. Card taps open the bounded Parent Thread-or-self view; Chapter opens the real chapter page and focuses the exact root; Reply now loads that same native focused thread and opens its existing composer. Ellipsis actions are thread/chapter, copy link, parsed single-item Mark Read/Unread, and confirmed Delete From Inbox; Reply stays exclusively in its dedicated capsule. Because official Inbox markup omits creators and work stats, Activity › Inbox now hydrates only unique work ids on the visible page: local/profile/auth-scoped cache first, then sequential paced authenticated metadata requests with view-owned cancellation, systemic-failure stop, and a capped auth-scoped cache. That one full `AO3CommentsWorkContext` updates badges automatically and supplies title/creator/fandom/rating/chapters to the destination summary card. Writes use a scope-owned operation token across account resets and invalidate every cached Inbox page/filter for the originating auth scope after success. **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint gate (warnings only), **371 tests / 42 suites**, iOS + macOS builds, whitespace; focused adversarial follow-ups closed identity, navigation/focus, visible-page lifecycle, stale-cache, account-switch write-guard, Reply, and non-work routing blockers. Latest build installed on iPhone 17 simulator for owner testing. **Still open before done:** make filter submission fully exact/fail-closed, expand failure/body coverage, full-feature adversarial review, owner live Mark Read/Unread/Delete and Author/Me/metadata checks, plus theme/Dynamic Type/iPad/macOS screenshots. Writes remain fixture-tested and never live-verified by an agent. |
| T-88 | **Native Account page redesign** — rebuild Account as the signed-in user's native profile hub with Overview/Works/Bookmarks/Activity, real posting-pseud selection, AO3 Inbox comment activity, shared author-profile content, and move local History/Favorites to Library. | Codex, Claude | `account-page-redesign` | ✋ HANDOFF | Branched from `comment-ui-refinement` (`c8bf8c0f`); Codex started (posting-pseud model + store), Claude implemented the rest 2026-07-11 on `claude/account-page-redesign-a9e3f5` per `Claude_Account_Page_Redesign_refined.md` with Codex's sub-decision defaults (approximate cached counts; read-only flat Inbox). **Landed:** (1) extracted `AO3AuthorWorksSection`/`AO3AuthorBookmarksSection`/fandom-filter/pagination/message rows out of `AuthorProfileView` into `AuthorProfileContentSections.swift` (profile view now consumes them; behavior-preserving); (2) new AO3 Inbox pipeline — `AO3Client.inboxURL`/`parseInboxPage` (selectors grounded in otwarchive `inbox/show.html.erb`; recognized-empty vs parser-drift distinction), `AO3InboxItem/Page` models, `AO3InboxModel` (fetches via `AO3AuthorProfileFetcher` TTL cache); (3) `AO3AccountListCountsCache` — in-session, scope-keyed, TTL'd approximate counts recorded by `AO3AccountWorksList`/`AO3CollectionsList`/`accountWorks`/`accountSubscriptions`, shown on Overview cards only when already available; (4) posting-pseud: `AO3Client.parsePostingPseudOptions`, `AO3AuthService.resolvePostingPseudID` (preference matched by name against the fetched form's own options, fallback = form default), threaded into `postComment` + `postCommentReply`, persisted per-account via Codex's `UserDefaultsAO3PostingPseudStore`, "Posting As" menu on the Profile Card; (5) `AccountView` rebuilt: Profile Card (square `AO3AuthorAvatar`, session-health line, View Profile → own `AuthorProfileView`, overflow w/ Verify Session/Open on AO3/Log Out), native segmented Overview/Works/Bookmarks/Activity; Works/Bookmarks embed the extracted self-scoped sections; Activity = History/Later (inline `AccountWorksInlineSection` via cached pages)/Comments (full inbox); Overview = rich My-AO3 nav cards + Recent Comments preview + On-AO3 preferences link; content activates only when the Account tab is actually visible; (6) local History/Favorites → Library (`LibrarySectionKind.history/.favorites` carousels + generic `LibrarySectionListView` see-all; bespoke Account list views deleted); (7) `SettingsRoute.privacy` decoupled from `AccountView.Route`; `AO3DashboardView` deleted (Overview replaces it). Tests added: inbox parser (incl. drift-throws), pseud options/resolution/store, counts cache, Library section predicates. **Adversarial review DONE** (2026-07-11, successor Claude session — the first 5-lens workflow died entirely on usage limits): 8 finder angles (line-scan / removed-behavior / cross-file / reuse / simplification / efficiency / altitude / conventions) + hand verification; 4 confirmed findings fixed — (1) `createBookmark` still used `parseDefaultPseudID`, so bookmarks ignored the chosen "Posting As" pseud while comments honored it → now `resolvedPostingPseudID`; (2) counts cache: paginating to a short final page overwrote a stronger cached lower bound ("80+" → "24+") → `record` now keeps the stronger estimate (`isAtLeastAsStrong`), +3 regression tests; (3) `AccountWorksInlineSection` retry/pagination fired untracked concurrent `Task`s racing the same @State → single-flight `launch()` mirroring `AO3InboxModel`; (4) AO3-History's refine-filter panel + Mature-reveal toggle became unreachable (nothing pushed `AO3AccountWorksList(.history)` anymore) → Activity›History/Later headers gained a "Filters" button pushing the full screen. Deferred (documented, low-sev reuse/duplication): inline-section vs `AO3AccountWorksList` dual fetch paths/caches for the same URLs; `AccountInboxItemRow.avatar` vs `CommentAvatar`; `AO3AuthorPaginationRows` vs series-detail's `paginationRow`; counts-cache vs `AO3AuthorPageCache` TTL shape; double HTML parse in `resolvePostingPseudID` fallback. `Scripts/verify.sh` ALL GREEN post-fixes — **352 tests / 41 suites**, iOS + macOS. **Sim pass DONE** (2026-07-11, live signed-in session on the iPhone 17 sim, Dark theme): inbox parser verified against the real AO3 inbox (commenter/source-line/relative-timestamp/excerpt rendered on both the Overview preview and Activity›Comments, incl. pagination totals); Bookmarks segment rendered a **Private-chipped** bookmark + notes + bookmark-date on the self-scoped profile (private-bookmark completeness confirmed); counts cache live ("100+" Subscriptions / "20+" Marked-for-Later); Works segment self-scoped with fandom chips; Activity›History live + new Filters button pushes the full list (Mature-reveal + filter + 69-page pagination); Library's Reading History/Favorites carousels present with empty states; Posting-As menu opens with the account's pseud options; session-health line updates. **Residue (owner/live):** `comment[pseud_id]`/`bookmark[pseud_id]` explicit-value acceptance needs live write verification (write actions remain never-live-tested, pre-existing gate); theme variants (Light/Sepia/OLED), Dynamic Type, iPad + macOS layouts not yet screenshotted; `AO3AccountWorksList.Kind.myWorks` no longer routed from Account (kind kept for potential reuse). |
| T-90 | **Account control-shape consistency** — round Account-owned action and submenu trigger surfaces to match Settings, excluding Shortcut tiles and system `Menu` popups. | Codex | `claude/review-grok-changes-9373ca` | ✅ DONE | Reworked the initial 24pt pass after owner feedback: Account-only navigation/submenu cards now use a proportional 20pt radius, 6pt internal vertical inset, and 8pt between-card gap; the compact Account stack uses 12pt grouping. Rounded 12pt bordered triggers cover Posting As, account overflow, sign-in, and Filters. Shortcut tiles, work-list cards, and native `Menu` popups remain unchanged. New `AccountControlStyle.swift` centralizes those metrics; `cardRow` gained parameterized padding/gap defaults, leaving non-Account call sites unchanged. **Verified:** `Scripts/verify.sh` ALL GREEN (invariants, lint warnings pre-existing, iOS tests, macOS build, whitespace); rebuilt, installed, and owner-approved in the iPhone 17 simulator. |
| T-86 | **Comment UI threading** — Library cards + Threads-style spine; max two card shells (root + direct-reply), deeper replies spine-only inside nested cards. | Grok | `comment-ui-refinement` | ✋ HANDOFF | Two-level cards only (no matryoshka); continuous avatar spine with gap filled inside the rail (not VStack spacing holes); nested reply cards use shadow lift; depth≥2 flattened into the parent nested card. Tests green. **Next:** owner screenshot Light/Dark/Sepia + branched thread spine check. |
| T-87 | **Native AO3 author profiles + app-wide author navigation** — add structured account/pseud identities, one lazy native profile destination (Works / Series / public Bookmarks / About), central AO3 user-link routing, and reusable pseud-correct author bylines without guessing identities or adding decorative requests. | Codex | `comment-ui-refinement` | ✋ HANDOFF | Cherry-picked (`f86fa07`) into `comment-ui-refinement` alongside T-86. `codex/native-ao3-author-profiles` (local + origin) deleted after confirming full containment: 25/52 touched files byte-identical, the other 27 differ only by additive evolution (review fixes below, OLED theme, moderation/mute-block flow) — no content lost. Adversarial-review pass (7 lenses, adversarially verified) found 9 issues; 8 fixed directly on `comment-ui-refinement` (`6114d54`) — HIGH: bulk-select byline tap could open a profile instead of toggling selection; MED: subscription/moderation header-reload race outside `activeTask`, comment "Author" badge losing its comma-split fallback, missing skeleton shimmer; LOW: pseud-alias loss on header reload, subscribe-button scope hint, missing 44pt tap target, deduped anonymous-blurb detection — plus 1 (`needsAO3Refresh`) already fixed by this branch's own prior polish commit. `Scripts/verify.sh` ALL GREEN. **Next:** owner sim pass (queued) — Works/Series/Bookmarks/About tabs, pseud switching, Subscribe/Mute/Block, bulk-select, Light/Dark/Sepia. |
| T-85 | **Comments thread nesting, timestamps, and action-row polish** — preserve reply-to-reply depth in the lazy row projection, align subtle avatar-to-avatar connectors, place localized timestamps inline with the author/badge, and keep Reply left / overflow right without changing networking or submission behavior. | Codex | `comments-thread-nesting-polish` | ✋ HANDOFF | Implementation complete (`d0a51ea`): shallow rows retain logical depth plus lightweight branch-continuation metadata; visual indent caps at 3; neutral elbow connectors align actual parent/child avatar centers; fetched AO3 timestamps parse once and display relative / local Yesterday / compact absolute with raw fallback; timestamp is inline and Reply-left/overflow-right (signed-out remains login-explicit). No new AO3 requests, likes, parser rewrite, or submission changes. Added 5 tests covering branched projection/depth cap and timestamp named-zone/offset/recent/yesterday/older/local-midnight/invalid fallback. `Scripts/verify.sh` ALL GREEN — **279 tests / 33 suites**, iOS + macOS, invariants, lint gate, whitespace. Launch-only production-row fixture checked parent→reply→reply-to-reply, Guest/Author, inline dates, and actions in Light + Dark; fixture removed. **Next:** owner screenshot approval (incl. Sepia/Dynamic Type) and live correct-parent reply action; deleted-parent fixture unavailable because existing parser skips id-less AO3 placeholders. |
| T-83 | **Native AO3 comments UI polish, unified entry points, and interaction performance** — align threads/composer/chapter picker with the supplied mockups; add parsed avatars/placeholders; remove legacy/misleading routes and nonfunctional actions; prevent floating-control overlap; diagnose and fix large-thread interaction stalls without extra AO3 traffic. | Codex | `comments-ui-polish` | ✋ HANDOFF | Implementation complete (`this commit`): parsed AO3 icons + placeholders; stable shallow/lazy rows and nested bubbles; debounced drafts; cancellable scope/sort/page loads; clearer AO3-red controls/chapter picker/reply sheet; markup-gated actions; Report Abuse and the legacy composer removed; shared card/Work Detail/readers route to `CommentsView`. `Scripts/verify.sh` ALL GREEN — invariants, lint gate (pre-existing warnings only), **274 tests / 33 suites**, iOS + macOS builds, whitespace. Launch-only fixture visually checked logged-out comments, real-icon/fallback rows, chapter sheet, reply sheet, and Dark mode; it caught/fixed the CTA overlap. **Next:** owner screenshot/live-session pass for Work Detail action, real logged-in menus + CTA, Light/Sepia, post/reply/edit/delete, and large live thread timing; human UI approval still required before merge. |
| T-64 | **Carousel selection refinements** — allow direct select/deselect from carousel cards using the existing parent selection state and fix the detailed card select bubble so it sits cleanly inside the card edge. | Codex | `ui-select-refinement` | ✅ DONE | Implemented against Library's existing `selection`/`EditMode` model: carousel cards toggle selection in place, detailed select list stays available from the toolbar, and edit-mode card rows gain leading room for the system select bubble. Follow-up `afc1a6e` tags detailed select-list rows with `SavedWork.id`, so carousel selections remain selected when switching to the detailed card list. Verified: `Scripts/lint.sh` (warnings only, pre-existing), iPhone simulator build/install. Earlier full verification: iPhone test suite, iPad simulator build, macOS build. |
| T-65 | **Lint cleanup** — fixed the pre-existing SwiftLint warning set without intentional behavior changes: safe naming/line-wrap/optional-init/style fixes, tiny tuple-to-struct cleanup, centralized AO3 HTML decoding to preserve existing lossy UTF-8 behavior, and documented local lint exceptions for large behavior-sensitive screens/services. | Codex, Claude | `claude/lint-cleanup-review-q9x71g` | ✅ DONE | Commit `71b16c3`. Codex verified (their environment): `Scripts/lint.sh` clean, `git diff --check`, iPhone 17/iOS 26.5 test suite, macOS build with `CODE_SIGNING_ALLOWED=NO`. **Claude adversarial review pass:** inspected the full diff line-by-line against every listed risk (Codable/persistence keys, tuple→struct field access, string-token formatting used for cache invalidation, decode/parse behavior, disable-comment scope); found it minimal, behavior-preserving, and correctly scoped (no file crossed a lint threshold without a disable, no file under-threshold got one; all `swiftlint:disable` are `:this`/`:next` except two justified file-scope `file_length` disables on files already over the 700-line threshold). Could not independently re-run `Scripts/lint.sh`/build/tests in this session's Linux sandbox (no Swift toolchain, network egress to install one denied by org policy) — re-run before merge. **Mac verification pass (Claude):** re-ran everything the Linux sandbox couldn't. Found two real compiler errors invisible without a Swift toolchain — `Log.epub.info(...)`/`Log.library.info(...)` string interpolations referencing `toc`/`finishedCount`/`failedCount` inside closures where an earlier redundant-`self` cleanup had stripped a `self.` that `OSLogMessage`'s interpolation actually requires for capture-semantics clarity — fixed by restoring `self.` at those three sites (`ReadiumReaderView.swift`, `DownloadQueue.swift`). Also fixed one remaining `line_length` warning (`SettingsView.swift:163`). After those two fixes: `Scripts/lint.sh` clean (0 warnings), `git diff --check` clean, full iOS test suite green (`iPhone 17`/iOS 26.5), iOS + macOS builds both succeed. Ready to merge. |
| T-63 | **Native pull-to-refresh across Home / Library / Browse / Search / Work Detail** — add native refresh affordances that refresh only visible/current-context AO3 data, merge metadata on successful parses, preserve filters/navigation, and never delete or purge local works on AO3/network/rate-limit/unavailable failures. New `WorkMetadataRefresh` service, deliberately separate from background tag sync, with field-level merge guards and save-only-after-successful-parse. **Review pass (Claude, `aaad490`):** adversarial multi-agent review confirmed data-safety/merge/scope properties hold across all screens; fixed a real cancellation-type-mismatch bug (`URLError(.cancelled)` vs `CancellationError`, so the refresh loop's own cancellation guard was silently dead code) and a work-completion-status selector bug (`dt.status` doesn't exist on real AO3 markup — only the sibling `dd.status` is classed — and the test fixture had been hand-written to match the bug). **Gap-closure pass (Claude):** closed the three items flagged as follow-ups — (1) added `cancelRefreshOnTabChange` (`UIComponents/CancellableRefresh.swift`), tracking each large-list refresh (Library section/Collection/Reading Queue "see all", Home section "see all", Library select-mode) as an explicit `Task` and cancelling it on tab switch, since `TabView` keeps hidden tabs mounted so a `.refreshable` task otherwise survives a tab change; (2) made `AO3RequestCoordinator.acquire()` cancellation-aware via `withTaskCancellationHandler` so a queued waiter stops immediately instead of waiting for an unrelated slot-holder to finish (new regression test `cancelledWaiterStopsWaitingWithoutConsumingASlot`); (3) deduped the triplicated tag-merge/normalize helpers (`WorkTags`/`WorkMetadataRefresh`/`ReadingQueueService`) into a shared `TagMerge` enum. **Regression fix (Claude, owner-reported):** pull-to-refresh on Home/Library was clearing the Subscriptions/Marked-for-Later carousels on any transient fetch failure (network hiccup, rate limit, expired session) — `AO3AuthService.accountWorks`/`accountSubscriptions` unconditionally returned `[]` on error (by original design, for the one-shot initial-load case), and `HomeView`/`LibrarySectionListView`/`LibraryView` all unconditionally overwrote their `@State` list with that result. Changed both `AO3AuthService` methods to `throws` (still returns `[]` only for the legitimate signed-out case) and updated all three callers to keep the existing list and log on failure instead of wiping it — matching the same never-destructively-overwrite-on-failure principle already applied to `WorkMetadataRefresh`. | Codex, Claude | `ui-refresh` | ✅ DONE | Verified: `Scripts/lint.sh` (warnings only, pre-existing), full iOS test suite on `iPhone 17`/iOS 26.5, macOS build. Pushed to `origin/ui-refresh`. **Not merged to `main`** — owner has not requested merge. |
| T-57 | **Work-card list controls + card-color regression fix** — (1) Fixed a regression where the metadata-inside-card revamp left every Library/Home cover card the same color: reintroduced the stable per-title hue (`CoverArt.hue`) as a subtle theme-aware surface tint + accent border on `WorkSummaryCardSurface` (dark/light/sepia tuned), so neighbouring cards read as distinct again — Codex's metadata layout/elevation untouched. (2) Added **Expand-all + page-contextual Filter** to every full-work-card page, reusing existing features: new shared `WorkCardListControls` toolbar cluster; local pages (Library section list, Home section list, Collection detail, Local History, Local Favorites) reuse `LibraryFilters`/`LibraryFilterPanel` applied live to that page's works; remote summary pages (Account lists — bookmarks/history/subs/my works/collections/marked-for-later — and Browse→Tag works) reuse `AO3FilterPanel` in a new `.refine` mode + new client-side `AO3SearchFilters.apply(to:[AO3WorkSummary])` (`AO3SummaryFilter.swift`), narrowing the loaded page in place (not a website-wide search); `WorkRow` gains the same expand affordance as `AO3WorkRow`; every filter-empty state has a Clear Filters escape; idle (no filter) preserves each section's own ordering. **Verified:** lint clean (new code), iOS + macOS builds, full iOS test suite green (`iPhone 17`, OS 26.5). Uncommitted on branch — owner reviewing in sim. **Follow-ups (same review cycle):** (3) **Tag retention** — new `SavedWork.ao3Unavailable` flag set when AO3 returns 404 in `WorkTags.refreshFromAO3` (`AO3Error.notFound`) so works **deleted from AO3 stop being re-fetched** and keep their existing tags; `needsAO3Refresh` short-circuits on it. Extracted the on-open EPUB-tag backfill into `WorkTags.backfillFromEPUB` and run it in the Library background pass too, so **downloaded works always keep their EPUB tags** (pure-local, works offline / for deleted works); works still missing categorized/extra data are still enriched from AO3, never overwriting existing tags (owner chose "Enrich, never overwrite"). (4) **Progress card duplicate %** — `WorkCoverCard.progressText` no longer echoes the Readium `readingProgressLabel` ("60%") on the bottom-left of the progress bar (that was the duplicate); shows "Reading"/"Finished"/"Ch N" instead, keeping the bottom-right percent. Verified in sim. | Claude | `feature/native-ao3-actions` | 🔄 IN REVIEW | Implemented + verified; awaiting owner sim check, then commit. |
| T-54 | **UI / Navigation / AO3-Parity Refinement** (large product-structure overhaul): 4 core tabs **Home / Library / Browse / Account** + a **global floating Search**; native **Browse** (Category→Fandom→Works, WebView demoted to fallback); **Account** absorbs the Bookmarks + Settings tabs; card consistency; work-level AO3 actions; privacy screen; saved searches; error states. | Claude | `feature/ui-refinement`→`main` | ✅ MERGED | **Plan APPROVED (phases back-to-back, pause at UI audit gate).** ✅ Prereq: merged `test/home-tab-overhaul`→`main` (`679b8cc`, pushed). ✅ **Phase 1 DONE** (`825db64`): 4 tabs Home/Library/Browse/Account + global Search (search-role slot); new `AccountView` absorbs Bookmarks+Settings (AO3 Account · My AO3 · On-AO3 web fallbacks · Local History/Favorites · App/Settings · Help); retired `BookmarksView`/`AO3AccountSection`; Links dropped. iOS+macOS build; sim-verified. ✅ **Phase 2 DONE** (`6f9de55`): local-first Global Search in `SearchView` — typing shows on-device matches live (Library works via `WorkRow`, fandoms, tags, collections) + explicit "Search AO3" action; no per-keystroke scraping; fandom browser still idle-state. Also: Global Search matches the **cached AO3 fandom catalog** (`18baad0`) — instant, no per-keystroke scraping; real search on tap corrects. ✅ **Phase 3 DONE** (`8893692`): native **Browse** — `MediaBrowserView` categories root → `FandomListView` (w/ filter) → `FandomWorksView` (native AO3 results, reuses `AO3WorkRow`/pagination/search); old web `BrowseView`→`AO3WebBrowserView` fallback ("Open AO3 Website" + `router.open`); Search idle now a prompt. Verified end-to-end in sim. ✅ **Phase 4 DONE** (`ee15e9a`): card/shelf consistency — shared `WorkStatLabel` (DRY'd `WorkRow`+`AO3WorkRow`) + `CoverStatsLine` adds a consistent rating+chapters line to the Home/Library cover cards (e.g. "T · 2/7"). ✅ **Phase 5 DONE** (`05222c0`): work-level AO3 actions — shared `AO3WorkActionsMenu` ("More actions" overflow on WorkDetail + Reader): Give Kudos / Comment / AO3 Bookmark / Mark for Later / Subscribe / Open on AO3, under an "On AO3 (opens website)" section. **Honest web fallbacks** (router.open → web view); native writes gated (CSRF, Part 16) — NOT implemented/faked. Local actions stay separate. ✅ **Phase 6 DONE** (`b50166e`): **Privacy & Local Data** screen (Account→App) — what's stored locally + Remove AO3 Session, Clear Browse Cache (`FandomCatalog.clearCache`), Clear Reading History. Error/stale states already covered (per-surface `ContentUnavailableView` + Retry-After). Also fixed the Library toolbar (tight icon cluster, Select→icon: `61d2a48`,`3376e21`). ✅ **All 6 phases complete.** **Post-phase work on branch:** skeleton loading across remote first-loads; Browse fandom-works filter button (shared `AO3FilterPanel`); Subscriptions fixed (`parseSubscriptionsPage` for the `dl`/`dt` page — was wrongly using `li.work.blurb`); **unified Work Detail** (`1875849` — one canonical `WorkDetailView` for local+remote, `Read`→Reader not a 2nd detail, lazy import-on-action, floating tab/search hidden on pushed screens) + **warnings/category/status/stats parity for local works** (`84bdaf7`, added `comments`/`hits` to `SavedWork`); ✅ **Saved Searches** (`bfd6b3d`, owner-approved `SavedSearch` model — full `AO3SearchFilters` made Codable, "Save Search…" in the filter panel, listed/re-run/swipe-delete on the Search idle screen). **Remaining:** local History/Favorites still to relocate to Library. ✅ **MERGED to `main` (`1c0c0d7`, pushed) 2026-06-24** after a no-regression check vs `readium-migration` (every non-reader feature present on both; only the in-progress Readium reader migration is intentionally legacy-excluded). iOS+macOS build + full test suite green on `main` (fresh DerivedData). See [[ui-nav-ao3-parity-refinement]]. |

---

## 🅿️ Backlog (prioritized)

### P1 — remaining
- **T-07 · Lazy / on-demand chapter extraction** 🅿️ *deferred.* Legacy/macOS reader
  only (the iOS Readium reader is already lazy); correctness-risky (WKWebView must
  resolve each chapter's CSS/image/font resources). Low ROI — do only if large-work
  perf becomes a real problem. Target: legacy `EPUBDocument.open` upfront-unzip.

### P2 — features (roadmap "Later" list; ID assigned on pickup)
- **Library Sync Folder hardening** — folder-based sync is implemented without
  CloudKit entitlements; remaining work is real-device iCloud Drive verification,
  conflict UX polish, and any follow-up from large-library testing.
- **Highlights / notes / annotations** — large; a reader annotation system.
- **Text-to-Speech (TTS)** — large; `AVSpeechSynthesizer` in the reader.

### Phase 3 — native AO3 writes (most fragile; needs the per-page CSRF token)
- **Leave kudos · comment · subscribe · AO3-side bookmark** — fetch the work/page,
  extract its `authenticity_token`, POST the form, detect a login redirect. Build on
  `AO3AuthService.authenticatedRequest`. See [[native-ao3-roadmap]] Phase 3 +
  [`docs/AO3Authentication.md`](docs/AO3Authentication.md).

### UI-refinement & maintainability pass (user-planned — *after* the feature backlog)
- Polish the AO3 account sub-picker (the "Subs" label, 4-segment layout, "AO3
  History" vs the local "History"); a dedup / clarity sweep over the Phase-2 +
  download-queue / bulk-actions code; general maintainability pass.

### Readium reader — Phase-4 polish (now on `main`, iOS reader; see `READIUM_MIGRATION_NOTES.md`)
- **T-20 · Phase-4 interaction polish** — auto-hide chrome on scroll, custom
  page-turn animation, safe-area inset tuning. (Notes ▸ Migration status, Phase 4 = partial.)

### ⚠️ Verification & housekeeping debts (clear before trusting the new features)
- **🔴 Live AO3 verification (HIGH).** Every scraping feature shipped 2026-06-20
  (auth login; Marked-for-Later / Bookmarks / History / Subscriptions selectors +
  URLs; the series-page scrape for the download queue) is **build + unit-test
  verified only — never run with a real AO3 session.** Selector/URL assumptions are
  encoded in `KudosTests` fixtures; some may need a one-line fix once tested logged
  in. A single live login test validates the whole Phase-2 batch at once.
- **🔴 Visual verification (HIGH).** The simulator's display surfaces were
  unavailable for the entire 2026-06-20 session, so nothing new was screenshotted
  (AO3 sub-picker, Library bulk-select, download-queue banner, About page, Continue
  Reading shelf). All build/test-verified and the app boots clean, but unseen.
- **Keychain on a signed device build.** Confirm the session actually persists to
  Keychain on real hardware (the WebKit-store fallback is meant to be the
  Simulator/dev backstop only). See [`docs/AO3Authentication.md`](docs/AO3Authentication.md).
- **Single-branch consolidation (2026-06-24).** The `main` (legacy) / `readium-migration`
  split was merged into a **single `main` branch**; all other branches were deleted
  (local + remote). The old cross-branch hazards once listed here no longer apply — no
  scratch worktrees, no "pollution stashed on main", and `main` now *legitimately*
  includes Readium (iOS), so a `readiumLocator` reference is correct, **not** a stale
  build artifact. The legacy WKWebView reader is now `#if os(macOS)`-guarded (iOS uses
  the Readium navigator). Still: build **both** iOS (Readium) and macOS (legacy) before
  claiming a cross-platform change is done.

### Bugs
- _No active bugs._ ↳ see the **🐛 Bugs registry** below.

---

## ✅ Completed (recent — newest first)

**T-84 verification follow-up (`55f7d63`, 2026-07-09):** assigned every
app-created `WKWebView` to one shared `WKProcessPool`, so the launch-time prewarm
actually covers login, Browse, and the legacy macOS reader. `Scripts/verify.sh`
ALL GREEN — invariants, lint gate (pre-existing warnings only), **274 tests / 33
suites**, iOS + macOS builds, whitespace. Manual owner confirmation that the
first login sheet no longer freezes is still required.

| ID | Task | Owner | Branch(es) | SHA (main / readium-migration) | Date |
|----|------|-------|------------|--------------------------------|------|
| T-112 | **Preserve actual navigation history (Back/Done)** — kudos-qol-prompts batch item 01. Audited Work Details, Author Profiles, Comments, Series, Collections, Bookmarks, Tags/fandom, Account, and sheets for pushes that reset/switch tabs/dismiss instead of stacking. Fixed 3 real bugs: `WorkDetailView`/`LocalWorkDestinationView`'s `dismissIfAuthorBylineConflict()` reacted to *any* flip of the global `cardNavigationSuppressed` flag, so tapping an already-settled screen's own author byline dismissed itself out from under the author-profile push (the literal reported repro) — gated it to a 0.5s post-appearance grace window via a new `appearedAt` timestamp; `LibraryView`'s `pendingLibraryTag` and `SearchView`'s `pendingTagSearch` handlers never reset `path`, silently updating a hidden root behind a still-pushed screen — both now collapse `path` on a fresh cross-tab request (`AO3TagSearch` gained `isFreshTabJump` so Search's filter history isn't polluted by an unrelated prior session). Every other flow traced was already correct. **Verified:** Debug build green; full suite **566 tests / 53 suites** passing. | Claude | `claude/general-improvements-branch-87e11b` | `a396dc1` | 2026-07-17 |
| T-111 | **Session-scoped chapter cache for Comments** — kudos-qol-prompts batch item 03. `CommentsModel` already cached comment pages correctly (auth-scoped key, skips failed/429/cancelled, invalidates via `forceRefresh` on mutation, cancels in-flight loads on chapter/scope switch) but via `CommentsPageCache.shared`, a process-lifetime global singleton with a 5-minute TTL. Since `CommentsView` already constructs a fresh `CommentsModel` via `@State` per open, switched the default to an instance-owned `CommentsPageCache()` and dropped the now-unneeded TTL machinery — the cache now lives and dies with the screen, no singleton. **Verified:** Debug build green; `CommentsAccountTransitionTests`/`CommentSubmissionTests`/`AO3CommentsParseTests` — 97 tests / 3 suites passing. **Follow-up fix (2026-07-17, code review):** dropping the TTL entirely was a regression, not just a cleanup — a Comments sheet left open would silently serve its first fetch forever (the stale banner only shows while offline), and `CommentsView` constructing a fresh model per open means the old singleton's 5-minute TTL had been the *only* thing bounding a re-visit's staleness, not merely deduping requests. Restored the TTL as `CommentsPageCache.freshPage(for:)` (5 min, matching `AO3AuthorPageCache`'s own TTL in `docs/AO3_NETWORKING_POLICY.md`), used by the happy-path read; the unconditional `page(for:)` remains for the offline-stale-fallback and `knownTotalPages` reads, which intentionally ignore staleness. Singleton removal and the dead `chapterIndexes` timestamp cleanup stand as correct. **Verified:** lint clean, Debug build green, full suite **566 tests / 53 suites** passing. | Claude | `claude/general-improvements-branch-87e11b` | `b641007` | 2026-07-17 |
| T-110 | **Reading Queues "Show All" chevron** — kudos-qol-prompts batch item 07. `readingQueuesCarousel` (`LibraryView.swift`) already used the shared `WorkCarouselSection` chevron, but `onSeeAll` was gated behind `customQueues.count > 11` instead of the low-bar convention every Works section uses. Changed to `!customQueues.isEmpty`. | Claude | `claude/general-improvements-branch-87e11b` | `875a6d9` | 2026-07-17 |
| T-109 | **Collections "Show All" chevron** — kudos-qol-prompts batch item 06. Same fix as T-110, one function up (`collectionsCarousel`): `onSeeAll` gated behind `collections.count > 11` → `!collections.isEmpty`. | Claude | `claude/general-improvements-branch-87e11b` | `397713b` | 2026-07-17 |
| T-108 | **Fix Compact dropdown icon theme color** — kudos-qol-prompts batch item 05. `AccountScopeMenu`'s closed-trigger icon (e.g. the "Marked for Later" scope dropdown) rendered `.primary` in Compact mode but accent-red in Detailed, same view instance both places — confirmed via owner screenshots. Root cause: a single `Label(text, systemImage:)`'s icon only inherits ambient `.tint` inside a `List` (Detailed's row context), not in Compact's plain `ScrollView`. Split icon/text so the icon is explicitly `.foregroundStyle(.tint)`, matching `CardMetaLabel`'s existing pattern for the same problem. Two prior code-only sweeps (no screenshot evidence) found nothing — this only resolved once given the owner's visual repro. | Claude | `claude/general-improvements-branch-87e11b` | `37ca23e` | 2026-07-17 |
| T-107 | **Shared top-right toolbar helper** — kudos-qol-prompts batch item 02. New `ToolbarActionCluster.swift` (`ActionToolbar`/`ToolbarIconButton`) formalizes the `ToolbarItem(.primaryAction) { HStack(spacing: 2) {...}.labelStyle(.iconOnly) }` pattern copy-pasted across ~12 files; migrated every top-right toolbar site (Account, Library, Home, Browse, Reader, Search, Collections, Reading Queues, Author/Series profiles, Work Detail) onto it, fixed a few accessibility gaps (bare `Image` ellipsis menus → real `Label`s) and merged two files' split `FilterButton`/overflow-menu toolbar items into one cluster. No hardcoded colors; every existing conditional/disabled-state/destructive-action/selection-mode toolbar left untouched. **Verified:** Debug build green; correctness + completeness review found zero regressions. | Claude | `claude/general-improvements-branch-87e11b` | `cc0f539` | 2026-07-17 |
| T-106 | **Remove duplicate Compact Overview button on Account page** — kudos-qol-prompts batch item 04. Compact mode's library-style root re-implemented the Overview tab as an extra tappable row directly below the segmented tab picker that already includes Overview; Detailed mode never had this duplicate. Dropped it. | Claude | `claude/general-improvements-branch-87e11b` | `07cb369` | 2026-07-17 |
| T-102 | **Comments audit fixes Part B — honest parsing + verification verdicts (CAA-6, CAA-4, CAA-5).** Comments/chapter parsers now accept empty only with AO3's authoritative landmarks, distinguish restricted-work login bounces, and fail closed on missing/nested/unexpected markup. Ambiguous-submit verification now uses canonical account identity and exact target level; moderation/Anonymous Creator form evidence, rich/entity transformation, and timing disagreement stay `.unknown` and blocked across guard recreation, with the attempt timestamp anchored at `begin`. Top-level matching is roots-only; replies remain direct-child matches. **Tests:** +18 from the branch baseline (**522 → 540 tests**, 51 suites), including a sanitized zero-comment fixture and parser/verdict/store regressions. **Adversarial review:** three lenses found two real gaps (non-comment rows could masquerade as empty; moderated replies lost hidden-post evidence) plus misleading unknown copy; all fixed and independently re-reviewed clean. **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint gate (pre-existing warnings only), iOS **540 tests / 51 suites** (iPhone 17/iOS 26.5), macOS Debug build, whitespace. **No live AO3 writes/authenticated requests. Manual remaining:** owner should exercise one ambiguous post/reply on a moderated or Anonymous Creator work and confirm Check Again remains blocked until the comment becomes visible. **Next:** Part C (CAA-3/CAA-11) is not started. | Codex | `codex/comments-honest-parsing-caa456` | `4a05556` | 2026-07-15 |
| T-100 | **Restore AO3/Cloudflare cookie continuity without reintroducing A5-F1** — regression from T-93: `AO3Client`'s anonymous session (`httpCookieStorage = nil`, `httpCookieAcceptPolicy = .never`) can no longer accept or resend Cloudflare's `cf_clearance`/`__cf_bm` cookies, so every request looks like a brand-new client and AO3 now serves a CAPTCHA/managed challenge. **Fix:** `makeAnonymousSessionConfiguration` (`AO3Client.swift`) switches the session to `URLSessionConfiguration.ephemeral` (a private, in-memory, per-process cookie jar automatically distinct from `HTTPCookieStorage.shared`) with `httpCookieAcceptPolicy = .always`/`httpShouldSetCookies = true`, so anonymous reads keep Cloudflare's `cf_clearance`/`__cf_bm` warm across requests. New `AO3Client.purgeSessionCookie` deletes any `AO3RequestDefaults.sessionCookieName` (`_otwarchive_session`, now single-sourced) cookie from that jar after every anonymous fetch, so the jar can never carry account identity even transiently — the actual A5-F1 invariant (auth cookie never in shared/ambient state) is unchanged; authenticated/write requests still build their `Cookie` header explicitly from `AO3AuthService`'s own persisted per-account `AO3Session`, never from this jar, so two accounts' requests can never race over one cookie value. New `AO3Client.challengeCookieHeader`/`AO3AuthService.mergedCookieHeader` let authenticated/write requests *read-only* append the jar's current Cloudflare cookies onto their explicit header too, so they aren't left un-cleared while anonymous traffic is. **Also fixes the CAA-2 redirect regression found by Part A's (T-99) adversarial review:** new `AO3RedirectCookieRelay` (`URLSessionTaskDelegate`, stateless, task-scoped) restores a same-host write redirect's fresh `_otwarchive_session` value (which carries otwarchive's success/error flash via Rails' `CookieStore`) onto the followed request, without ever touching shared/ambient cookie storage — wired into `performAuthenticatedFetch`/`submitWrite` via `session.data(for:delegate:)`. **Tests (+14):** private-ephemeral-jar-not-shared invariant (replaces T-93's now-obsolete `anonymousSessionConfigurationDisablesCookieHandling` pin), Cloudflare-cookie filtering excludes the auth cookie (present + auth-only-empty cases), purge removes only the auth cookie / tolerates nil storage, redirect-relay merge (replace/append/no-op/nil-header/nil-url/multi-cookie cases), auth+challenge header concatenation (present/absent/empty). **Verified:** `Scripts/verify.sh` ALL GREEN — invariants, lint, iOS **486 tests / 50 suites** (iPhone 17/iOS 26.5), macOS build, whitespace. **Not done:** no live AO3 request by an agent (project policy) — owner must confirm the CAPTCHA stops appearing and that a real edit/delete no longer shows "couldn't confirm." **Docs:** T-93's row annotated with this regression + fix; T-99's row and the audit report's CAA-2 status note reference this fix. **Adversarial review + follow-up fix (2026-07-14):** a review of this same commit found a genuine, CONFIRMED bug in `AO3RedirectCookieRelay`: it attached the merged Cookie header — including the freshly re-signed, live `_otwarchive_session` value — onto whatever host the redirect's `newRequest` targeted, with no check that the target was still an AO3 host. A cross-host redirect (open-redirect parameter, misconfigured route, or an intermediary domain such as a Cloudflare challenge subdomain that happens to echo a same-named cookie) could have carried the real account session cookie off-site — reopening A5-F1 through an explicit per-request header, outside every cookie-storage protection this task otherwise added. Fixed by a new `RedirectCookieAction` three-way decision (`.leaveUnchanged` / `.set(String)` / `.strip`) in `redirectCookieAction(currentHeader:responseHeaderFields:responseURL:newRequestURL:)`: it checks `AO3RequestDefaults.isTrustedURL(newRequestURL)` first and returns `.strip` for any non-AO3 (or non-HTTPS, or nil) target — actively removing the Cookie header rather than merely leaving it as Foundation built it. **Tests (+6):** cross-host strip, non-HTTPS strip, nil-target strip, same-host set (incl. an AO3 subdomain), same-host no-op when nothing needs updating. Re-verified: `Scripts/verify.sh` ALL GREEN, **492 tests / 50 suites**. (A second review finding — `AO3AuthService.swift` crossing SwiftLint's 700-line `file_length` *warning* threshold at 707 — is not a behavioral defect; matches the same non-blocking finding on T-99's own review and needs no action.) **Second follow-up review (2026-07-14):** re-checked the cross-host fix plus the previously-incomplete cross-account-isolation concern (its prior review pass hit a transient API error and never returned a verdict). Cross-host fix confirmed correct; no critic findings. Two confirmed-but-minor gaps in `performFetch`/`downloadEPUB`: `purgeSessionCookie` sat after the throwing `Self.check(response)`, so a non-2xx anonymous response skipped the purge, and `downloadEPUB` never called it at all. Traced impact: harmless in practice — `challengeCookieHeader` unconditionally filters the auth cookie by name regardless of what's in the jar, and authenticated/write requests never read this jar at all, so no leak was ever possible even with the gap — but it contradicted the doc's "structural invariant" claim. Fixed by moving the purge into a `defer` (runs regardless of `check`'s outcome) and adding the identical call to `downloadEPUB`. No new test (the purge's own removal logic is already unit-tested; this was a call-site/ordering fix only). Re-verified: `Scripts/verify.sh` ALL GREEN, **492 tests / 50 suites**. Review loop closed — ready for merge consideration. | Claude | `claude/ao3-cookie-policy-3886f9` | `dec87d6`,`8bcca45`,`7234cb2` | 2026-07-14 |
| T-99 | **Comments audit fixes Part A — write-path correctness (CAA-1 + CAA-2)** — implement `docs/COMMENTS_AO3_API_AUDIT.md` findings CAA-1 (comment POSTs must carry an AO3-authorized `comment[pseud_id]`; fetch pages that actually render the form; fail typed pre-POST when no pseud control exists) and CAA-2 (honest write-response reading: recognize `comment_error`/`caution` error flashes, scan delete bodies, require positive `comment_notice`/`notice` evidence before declaring a final-200 write successful; unconfirmed → ambiguous, never success). Per `claude_comments_audit_fixes_implementation_v1` Part A; owner-decided defaults encoded there. Writes stay fixture-tested; **no live AO3 write or authenticated request by an agent**; owner live test of one reply + one single-pseud top-level post is the release gate. **Adversarial review (2026-07-14):** one real-bug finding survived verification (`AO3AuthService.swift:955`'s new `requiredCommentPseudID` pushed `file_length` to a 705-line SwiftLint *warning*, not a defect — refuted, no action) plus a genuinely CONFIRMED bug: `commentWriteVerdict`'s positive-success requirement (`AO3Client.swift:571`) can never see the success flash on a redirect-based write, because `AO3Client`'s session (`httpCookieStorage = nil`/`.never`, T-93) discards the redirect's `Set-Cookie` — and otwarchive uses Rails' `CookieStore` for `_otwarchive_session` (confirmed against `config/initializers/session_store.rb`), so the flash rides that cookie, not server-side state. This made `editComment`/`deleteComment`/`performInboxBulkAction` misreport a fully successful write as `.unconfirmed`. **Not fixed on this branch** — the defect lives in shared `AO3Client` plumbing (`performAuthenticatedFetch`/`submitWrite`), not in this branch's own code, and the same commit that would fix it also had to fix a live, user-facing CAPTCHA regression from the identical root cause; both are closed by **T-100** (`AO3RedirectCookieRelay` restores the redirect's fresh session-cookie value onto the followed request without ever touching shared/ambient cookie storage). Part A's own `commentWriteVerdict`/CAA-1/CAA-2 logic is correct as implemented; it merges cleanly once T-100 lands on `release-fixes`. | Claude | `claude/comments-write-path-caa12-a85b62` | `257eb1d`,`3e8cdcc` | 2026-07-14 |
| T-96 | **Inbox account-transition isolation** — closed release-blocking T91-RF3 and T91-RF5. Immutable auth-scope/session-generation snapshots gate every Inbox read, write, enrichment, destination-cache, and private-cache continuation; an A write remains single-shot but cannot reload or mutate B, and a queued A tap cannot borrow B's form. Auth fences stale login/fallback/restore/verification/expiry flows and serializes WebKit cookie mutations. Review follow-ups clear stale login errors and extend generation-qualified cache scoping to Account list HTML, pagination snapshots, and counts. **Tests:** 22 task-specific transition/cache/auth regressions, including 11 deterministic Inbox A→B/C races and six follow-up cases over the production fetcher wiring and Account caches. **Verified at `df8207cc`:** `Scripts/verify.sh` ALL GREEN — invariants, lint warnings only, iOS **474 tests / 48 suites** (iPhone 17/iOS 26.5), macOS Debug build, whitespace. **Manual remaining:** owner-operated A→B mid-write check; no AO3 write was live-tested by an agent. | Codex | `claude/inbox-account-transition-isolation-9bf225` | `06031af`,`28f1f5ff`,`df8207cc` | 2026-07-14 |
| T-89 | **Adversarial review repairs for T-86 + Account hub** — all 15 confirmed issues corrected, including deterministic malformed-comment tombstones and paced/coalesced reply-avatar loads found during final re-review. **Verified:** `Scripts/verify.sh` (invariants; lint with pre-existing warnings; iOS **360 tests / 42 suites**; macOS; whitespace) plus a post-fix hand review. **Manual remaining:** owner Light/Dark/Sepia, Dynamic Type, iPad/macOS Account and threaded-comment screenshot pass; signed-in post/reply/edit/delete remains the existing live-write gate. | Codex | `claude/review-grok-changes-9373ca` | `9398da7` | 2026-07-12 |
| T-84 | **Comments UI visual-regression fix** (owner spec, screenshots as reference) — on `comments-ui-visual-regression-fix` from `comments-ui-polish`; **not a revert**: keeps every T-83 functional win (flattened lazy rows + stable IDs, cancellable loads, debounced drafts, avatars, unified entries, no Report Abuse, no legacy composer, submission guard, networking untouched) while restoring the mockup/Kudos card language. Core: **card-within-a-card threads** — all rows of a top-level thread share one continuous Library `cardSurface` card (`commentThreadGroupRow`, `UnevenRoundedRectangle` opens/closes via new tested `AO3CommentRow.isLastInThread`); replies = nested bubbles on new `ReaderTheme.nestedCardSurface` + quaternary connector (red rail gone); ReaderTheme card surfaces made internal so feature cards can't drift. Also: quiet "N comments · Oldest First" footnote line; floating capsule CTA (slab removed); composer field with focus ring/placeholder/auto-focus + short titles (no truncation); picker note → Section footer, fitted detents; neutral avatar placeholders. **Adversarial review** (workflow `wf_9748d785-6a7`; verify agents died on a usage limit → verified inline): FIXED connector collapsing to 10pt stub (List nil-height proposal), FIXED CTA shadow ignoring Dark's shadow-free language, corrected handoff-doc murk root-cause note (`carouselCardSurface` == `cardSurface` — murk was the fragmented treatment, not the fill); accepted+documented border/shadow tradeoff on multi-row cards. ⚠️ Incident: a review agent stashed the tree mid-pass — recovered via `git stash pop`, then committed before further agent runs. Verified: `Scripts/verify.sh` ALL GREEN ×2 (**274 tests / 33 suites**, iOS + macOS). **Follow-up round** (owner reviewed screenshots against these mockups, found 5 more gaps): (1) info card showed only the bare title — never wired to fandom/rating/chapters on any branch since the feature was first built; new `AO3CommentsWorkContext` (title/authors/fandoms/rating/chapters, **no cover** — most AO3 works don't have one) threads real data through every presentation call site, info card now reuses `WorkDetailView.overviewSection`'s exact layout instead of bare text. (2) Filter/funnel button: confirmed never implemented, declined. (3) No dismiss control when presented as a sheet — real gap, fixed: Close button whenever modal, plus an Expand button (new `CommentsSheetModifier`/`.commentsSheet(...)`) promoting the pop-up card to a `fullScreenCover` on iOS (macOS: Close only, no fullScreenCover concept there); all 4 modal call sites (actions menu, both readers, Library/Search context menus) now share one modifier instead of 4 near-identical `.sheet` blocks. (4) All/By Chapter control was a bespoke capsule — now the exact `Picker(...).pickerStyle(.segmented)` Reader Settings uses for Scrolled/Paged. (5) Chapter selector is its own Section/card, separate from the info card, matching the mockup's three distinct cards. Verified: `Scripts/verify.sh` ALL GREEN (**274 tests / 33 suites**, iOS + macOS). **Live-test round** (owner used the app; found 2 real functional bugs): (1) "Log in to comment" did nothing — the CTA either hid entirely when signed out or (original branch) was `.disabled(...)` behind its own login-prompting label, a dead end either way; now always shows once loaded, tapping while signed out presents `AO3LoginView()` (same sheet as Account/Bookmarks/Collections); fixed the identical dead "Log in to Reply" per-comment menu item too. (2) "Thread"/"Parent Thread" opened AO3's website instead of focusing natively — `CommentsView` now wraps its List in a `ScrollViewReader`; both menu items scroll to and briefly tint the target comment (self / immediate parent via new `AO3Comment.parentCommentID`, parsed from AO3's own "Parent Thread" link) — no extra request, since a comment and its thread relatives always come from the same page fetch. Verified: `Scripts/verify.sh` ALL GREEN (**274 tests / 33 suites**, iOS + macOS). **Diagnosed + fixed a related but app-wide bug found while live-testing:** the login sheet's ~10s freeze right after opening was WebKit's one-time WebContent/Networking process spin-up (worse on Simulator + ad-hoc "Sign to Run Locally" signing), triggered the moment `AO3LoginView` first mounted its hidden webview into a real window — the sheet's own presentation animation completes instantly, unrelated to WebKit. New `WebKitPrewarmView` (`kudos-ao3-reader/App/ContentView.swift`) mounted invisibly at the app root warms a throwaway `WKWebView` (loading local `about:blank`, no network) 500ms after first render, so cold launch never competes with it; it never touches `auth.loginWebView` itself so it can't race the real login flow — WebKit's process spin-up is shared process-wide, so this also warms the in-app Browse tab's webview for free. Verified: `Scripts/verify.sh` ALL GREEN (**274 tests / 33 suites**, iOS + macOS). **Thread-geometry round** (owner task doc: card-within-card, not timeline): the connector was a far-left rail detached from avatars with reply avatars inside the bubbles. New `CommentThreadGeometry` enforces `connectorX == parentAvatar.centerX == childAvatar.centerX` — one shared 38pt avatar column per thread (parent 38 / reply 30 avatars, all centers x=33), single straight quaternary line drawn by the group-row modifier as arrival (row top → avatar center, behind the opaque-backed circle) + continuation (below avatar → row bottom) segments that join flush across rows; depth ≥2 indents the bubble only, so no elbows exist; action row reordered Reply-left / overflow-right per the doc. One file (`CommentThreadRow.swift`), view-layer only; flat lazy rows + all functional behavior preserved. Verified: `Scripts/verify.sh` ALL GREEN (**274 tests / 33 suites**, iOS + macOS). **Avatar-badge round** (owner provided a closer mockup crop after the thread-geometry pass): a reply's avatar now overlaps its own bubble's top-left corner (half poking above/outside, half over the interior) instead of sitting inline inside the bubble — positioned via `.overlay(alignment: .topLeading) + .offset`, a pure visual overlap that doesn't affect bubble layout. Bubble gained real trailing/bottom margin (floating-card look). Hairline separator before the overflow menu shown in the mockup intentionally NOT implemented, per owner. **Adversarial review** (workflow `wf_799cad23-a82`, 4 lenses + independent verification) caught 2 real regressions before landing: (1) `continuationTop(forDepth: 0)` used a center-anchor formula (`parentAvatarSize/2`) for the top-anchored parent avatar (no offset trick, unlike the reply avatar) — line started 19pt early, bleeding through the parent avatar's lower half; fixed to the full `parentAvatarSize`, and restored the parent avatar's opaque backing (dropped when parent/reply code paths split). (2) reply avatar's corner offset was applied before the depth-based bubble indent, so at depth ≥ 2 it drifted right with the bubble instead of staying on the fixed centerline as documented, leaving the connector dangling; fixed by having the offset subtract `bubbleIndent(forDepth:)` so the avatar's absolute x is depth-invariant (documented residual: past depth 2 the avatar detaches from its own bubble's corner, not from the line). Verified: `Scripts/verify.sh` ALL GREEN (**274 tests / 33 suites**, iOS + macOS).  **Avatar-badge round** (owner mockup: corner-overlap avatar) landed as `96b1827`, then an adversarial review (workflow, 4 lenses) found 2 real high-severity bugs before any owner testing: the parent-avatar connector bled through the lower half of placeholder/guest avatars (continuationTop used half the avatar's radius instead of its full height), and the reply avatar drifted off the fixed connector line at nesting depth >= 2, leaving the arrival segment dangling in blank space — both confirmed via independent re-verification. **Before those fixes could land, the owner rejected the whole approach**: "profile picture must remain inside bubble." Reverted to a plain avatar-then-content row (matching the parent's own layout) inside a bubble with real margin on every side — a genuinely floating card, not the corner-overlap treatment. Carried forward the two real fixes since they're not overlap-specific (parent connector now reaches the avatar's true bottom edge; replies no longer thread the connector through their own avatar at all — the arrival segment only needs each bubble's own top edge, and a non-last reply's continuation just runs the row's full height, relying on the bubble's own opaque fill to hide whatever falls behind it, sidestepping variable AO3-comment-length bubble heights entirely). A second adversarial-review attempt partially failed on a usage-limit mid-run (2/5 agents completed, refuting its 2 findings; one review lens never ran) — rather than re-launching a third workflow, did a direct, own-hand arithmetic check and found one more real gap: `bubbleIndent`'s per-depth increment pushed a bubble's left edge past the connector's fixed x at nesting depth 3+ (46 > 36), leaving the continuation line floating unhidden beside a triply-nested reply; fixed by capping the indent at one step past the first reply (depths 1-2 — the pair explicitly required, first reply + reply-to-reply — were already safely within bounds; only the rare depth-3+ case changes). Verified: `Scripts/verify.sh` ALL GREEN (**274 tests / 33 suites**, iOS + macOS). **⛔ Owner screenshot/live-test pass still required** (light/dark list, picker, both composers, long thread, 1-chapter + many-chapter works, sheet-expand-to-fullscreen, login CTA, thread-focus scroll, and confirm the login-sheet freeze is gone). | Claude | `comments-ui-visual-regression-fix` | `324f348`,`48c50ef`,`57bdd2c`,`10cb0bc`,`4296a65`,`3d573c8`,`96b1827`,`96b1827`,`08446cc` | 2026-07-09 |
| T-82 | **Native AO3 Comments (Phase 4, items 13-14)** — view a work's comments (All / By Chapter), threaded conversations, reply, top-level compose, and per-comment actions, on `feature/ao3-comments` (from `release-hardening`). **Read:** `AO3Client+Comments` parses `show_comments` pages (threads = sibling `ol.thread` nesting), `/navigate` chapter index, pagination, totals — selectors grounded in **live AO3 markup captured 2026-07-09** (~8 polite recon GETs; ground truth + endpoints table in [`docs/ai/COMMENTS_HANDOFF.md`](docs/ai/COMMENTS_HANDOFF.md)). Fetches use the client's paced/coalesced/retried pipeline (authenticated when signed in, so AO3's own per-session Edit/Delete links surface and are the ONLY gate for those actions); 5-min session `CommentsPageCache`; every request maps to an explicit user action — no polling/prefetch/per-chapter count harvesting (mockup's per-chapter counts intentionally dropped as they'd require fetching every chapter). **Write:** `postCommentReply` (`POST /comments/<parent>/comments`, verified via AO3's no-JS reply form), `editComment`/`deleteComment` (Rails `_method` PUT/DELETE, parse-gated). **Never-double-post:** `CommentSubmissionGuard` — single-flight, whitespace-normalized duplicate window (5 min), ambiguous outcomes (timeout/connection-lost after send) verify against the work's newest comments page with three-way result (`found/absent/unknown`); only verified absence unlocks explicit retry; `unknown` keeps re-POST blocked with a "Check Again" (read-only) — plus per-context drafts cleared only on verified success. Covered by a 17-test suite (fixture-driven parsing + full guard state machine). **UI:** `CommentsView` (segmented All/By-Chapter, chapter sheet, local Oldest/Newest order — AO3 has no server sort, pagination row, skeletons, empty/failed/offline-stale states), recursive thread cells (Author/Guest badges, chapter chips), composer (parent quote, draft-as-you-type, honesty note that top-level posts land work-level), delete confirm, copy link/open thread/report abuse. Entries: Work Detail stats row (push), shared On-AO3 menu "View Comments" (sheet; covers macOS legacy reader), Readium reader toolbar bubble (sheet). **Adversarial self-review fixed 5 issues pre-commit** (double-fetch on scope switch; stale cross-scope page; verification-failure≠absence — the one real double-post path; chapter-scope verification mismatch; chapter-posting honesty note) — details in the handoff doc. Scope-outs documented: guest posting, comment kudos (AO3 has none), flat view, chapter-targeted top-level posting (needs live chapter-form capture). Verified: `Scripts/verify.sh` ALL GREEN ×2 — **265 tests / 33 suites**, iOS + macOS builds. **⛔ MERGE-GATED: owner live-session pass required** (checklist in handoff doc: post/reply/edit/delete + double-post simulations — Edit/Delete markup and reply endpoint semantics can only be confirmed signed-in). | Claude | `feature/ao3-comments` | `87ef7d0` | 2026-07-09 |
| T-81 | **Phase 3 item 9 — `SavedWork.ReadingState` lifecycle partition** (`agile-forging-moon.md` backlog). Shelf filters, statistics, and the history query each re-derived "unread / in progress / finished / freed" from raw booleans and had drifted twice already. New computed **`ReadingState`** (`.unread / .inProgress / .finished / .freedHistory`) folds `isFinished` / `hasEPUB` / `hasStartedReading` into one exhaustive partition — exactly one case per work; **finished wins even after the EPUB is freed**; no stored @Attribute, no schema change, backup format untouched; AO3's posted WIP-vs-complete status (`isComplete`, the search `Completion` filter) deliberately stays a separate axis. `isInProgress` redefined via the partition (provably identical); `LibrarySectionKind` readingNow/finished shelves filter on `readingState`; `ReadingStatistics.hasStarted` defers to the model's `hasStartedReading` — **bug fix:** its private field re-listing missed the Readium locator, undercounting works read only on iOS. History's `@Query` predicate unchanged (`#Predicate` can't call computed properties) + keep-in-sync comment. **Adversarial review** (workflow `wf_1b4eef1d-e56`; its verify agents died on a usage limit, findings re-verified inline): CONFIRMED+FIXED `MediaBrowserView.hasBeenRead` (`isFinished \|\| lastSpineIndex > 0` missed Readium-only reads → recent fandoms never surfaced; now `isFinished \|\| hasStartedReading`); CONFIRMED+FIXED `WorkDetailView` keep-status text checked `!hasEPUB` before `isFinished`, mislabeling unfinished freed works "Finished." (now switches on `readingState` with honest `.freedHistory` copy); REFUTED `ReadingStatistics.inProgressWorks` counting freed-unfinished works (intentional stats semantics, left as-is); REFUTED PersistenceSync keep-in-sync gap (already in sync). +4 tests (`readingStatePartitionsTheLifecycle`, `finishedWinsOverFreedEPUB`, `readingStateMatchesIsInProgress`, `readiumOnlyWorkCountsAsStarted`). Verified: `Scripts/verify.sh` full green ×2 (pre- and post-review-fixes; **248 tests / 31 suites**, iOS Simulator + macOS Debug builds). Cross-session handoff: implemented by Fable, verified/reviewed by a successor Claude instance after a usage limit, landed by Fable — via `plan_context.md`. **Manual remaining:** owner sim pass (Library shelves, Work Detail keep-status line, Browse recent fandoms). | Claude | `release-hardening` | `a4c844e` | 2026-07-08 |
| T-80 | **Phase 5 item 16 — on-demand AO3 session health** (`agile-forging-moon.md` backlog). The stored session was validated only once at launch (`restoreSession()` is single-shot by design); nothing let the user re-check it or see its state. `AO3AuthService` gains **`AO3SessionHealth`** (`unknown / verifying / healthy(Date) / expired / unreachable`) + **`verifySession()`**: on-demand re-validation mirroring `restore()`'s outcome handling — `.valid` refreshes + re-saves the session (`finishAccepting` now stamps `.healthy(now)`, so launch restore and login set it too), `.expired` clears it with the usual notice, and a **thrown (transient) failure keeps the session** and reports `.unreachable` instead of logging the user out. `logout()`/`clearStoredSession()` reset to `.unknown`; `sessionDidExpire()` sets `.expired`. Account's signed-in section shows a **Session row** (relative last-verified time / Expired / Couldn't verify) + a **Verify Session** button (disabled while checking). Landed the pre-existing uncommitted implementation found on the branch after verifying its correctness; added 4 regression tests (healthy-keeps, expired-clears, unreachable-keeps, signed-out no-op) via a new `ConfigurableAO3SessionValidator` mock (swappable result, can throw). *Test-authoring note:* the suite's `testSession` is a computed property minting a fresh `savedAt` per access — capture it once per test or equality assertions fail. Verified: `Scripts/verify.sh` full green (invariants, lint, **244 tests / 31 suites**, iOS Simulator + macOS Debug builds, whitespace). **Manual remaining:** owner sim pass of the new Account rows; live-AO3 verify of the actual network validation path. | Claude | `release-hardening` | `a80e517` | 2026-07-08 |
| T-79 | **Post-T-77 owner-reported UI bug fixes (simulator testing pass)** — a run of precise UI/behavior bugs the owner found while hands-on testing the T-77 work, each root-caused, fixed, `Scripts/verify.sh`-green, and committed. **(1) Detailed-card selection outline drawn inside the card** (`6d492ae`): `WorkRow`'s outline overlay sat inside `.listRowInsets` padding, not at the card's true edge; moved the accent stroke onto `.cardRow(isSelected:)`'s own `.listRowBackground` (`AppThemeSurface.swift`) and applied it per-row at the 5 selectable-list call sites. **(2) Blurred detailed-row expand button vanished + tap-to-reveal dead** (`6d492ae`): `WorkRow` gained an `externalExpanded` binding + static `isExpandable(for:)` + shared `WorkRowExpandButton`; `SensitiveWorkRow`'s blurred branch renders an unblurred `[expand][bubble]` cluster bound to the blurred content, and its tap targets became real `Button` wraps so the decorative stroke overlay (which is hit-testable across its bounds) no longer swallowed the reveal tap; `SensitiveWorkCoverCard`'s equivalent overlay got `allowsHitTesting(false)`. **(3) Dashboard selection lost on ">" chevron** (`4d8e904`): `HomeSectionListView`/`LibrarySectionListView` owned independent `isSelecting`/`selection` @State; seeded both from the dashboard via new `init(initialSelecting:initialSelection:)`. **(4) Misleading "Tap to reveal" in select mode** (`3db9862`): label hidden while `isSelecting` (a tap toggles selection there, not reveal). **(5) Search Back → blank page** (`f65b052` + `d204a6d`): `pushFilters` was pushing a non-searchable default filter set as a phantom history entry (fixed by only pushing when `isSearchable`); and `goBack()` re-fetched the prior level from AO3, which occasionally returned empty/rate-limited — `FilterHistoryEntry` now snapshots `results`/`totalPages` and `goBack` restores them verbatim (no network). All verified `Scripts/verify.sh` full green; **drag-reorder (Branch E) reported still broken by owner — pinned for later, untouched.** | Claude | `release-hardening` | `6d492ae`,`4d8e904`,`3db9862`,`f65b052`,`d204a6d` | 2026-07-08 |
| T-78 | **Phase 2 item 11 — add works to a collection from inside the collection** (`agile-forging-moon.md` "rest of the backlog"). `CollectionDetailView` (`Features/Library/Collections.swift`) had no in-page way to add existing library works — its empty state only pointed users to a work's own "Add to Collection" menu. Added an **"Add Works"** entry point (a "+" `primaryAction` toolbar button, always visible, plus an action button in the empty-state `ContentUnavailableView`) that presents a new **`AddWorksToCollectionView`** — a searchable (`.searchable`, default placement for macOS-build safety), multi-select list of eligible library works with an "Add (N)" confirm. Eligibility + the add mutation are extracted into a pure, unit-tested **`CollectionWorkPicker`** enum: `candidates(from:notIn:)` excludes queue-only EPUB-preservation records and existing members; `add(_:to:in:now:)` is idempotent, stamps both sides `markModified` for sync, and records **no** tombstone (only removals do), mirroring `AddToCollectionView`'s add branch. The view keeps the privacy filter (needs the live `PrivacyGate`) so a Hide-mode mature work is never offered. New `KudosTests/CollectionWorkPickerTests.swift` (5 tests: exclude-members, exclude-queue-only, both-sides membership, idempotency, modified-stamping). Verified: `Scripts/verify.sh` full green — invariants, lint, **240 tests / 31 suites**, iOS Simulator + macOS Debug builds, whitespace. **Manual remaining:** simulator screenshot pass of the new picker + entry points (the UI-consistency approval gate). | Claude | `release-hardening` | `e54402e` | 2026-07-08 |
| T-77 | **UI/Behavior Fix Pass (8 issues) + Drag-to-Reorder for Reading Queues** — a user-specified spec covering privacy-toggle spacing, imports auto-adding to Saved for Later, selection coverage, fandom-name tinting, carousel truncation, filter back-navigation, context-menu gaps, and detailed/compact layout, plus a separate drag-reorder system. Full plan (research, judge-panel design synthesis for the two genuinely architectural pieces, and per-item implementation notes) lives at `agile-forging-moon.md`. **D1** privacy toggle: `MatureRevealToggle` sat in its own `ToolbarItem` next to `WorkCardListControls` in a second one (wide system spacing) in `LibrarySectionListView.swift`/`HomeSectionListView.swift`/`AO3AccountWorksList.swift`; merged into one tight `HStack(spacing: 2)` `ToolbarItem`, matching the pattern already established in `LibraryView.swift:490-501`. **D2** import pollution: `WorkImporter.swift`'s `importUserEPUB` set `isSaved = true`, which `LibrarySectionKind.savedForLater`'s legacy pre-queue-save carve-out (`LibrarySectionKind.swift:77`) wrongly enrolled; removed, aligning with the sibling AO3-download `importEPUB` path which never set it. **D3** selection: extended Library's `EditMode`/`Set<UUID>` pattern to Home's dashboard and both category "See all" pages (new shared `WorkBulkActionBar`), and Browse's `isSelecting`/`Set<Int>` pattern to `TagWorksView` and Search results (new shared `RemoteWorkBulkActions.swift`, also refactored into `FandomWorksView` to remove a third divergent copy). Disclosed gap: Home's Subscriptions carousel (`CanonicalWorkCoverCard`) and the remote Marked-for-Later sub-list keep single-item actions only. **D4** fandom tint: carousel cover cards were already correct (`CardMetaLabel`, `HomeCards.swift:294-308`); `WorkRow.swift`/`AO3WorkRow.swift` split icon-tint from text-secondary to match. **D5** carousel truncation: the primary work-carousels already had a working uncapped See-All path; the real gap was `collectionsCarousel`/`readingQueuesCarousel` having no cap and no See-All page at all — added `LibraryEntityGridView.swift` (reusing existing card views) + `prefix(12)`/`onSeeAll` wiring. **D6** filter history: `SearchView.swift` gained a local `filterHistory: [FilterHistoryEntry]` stack — `applyTagSearch` pushes the prior filters before overwriting (`pushFilters`), `goBack()` pops one level first, cleared on every full-reset path. Zero other files touched — every tag tap already lands inside Search's own `NavigationStack`. **D7** context menu: added a direct `isInSavedForLaterQueue`-gated Save/Remove-for-Later item to both `WorkCardActions.swift` modifiers, using existing `ReadingQueueService` methods (no new service logic). **D8** layout toggle: new `WorkListDisplayMode`/`DisplayModeToggle` — "Compact" reuses `WorkCoverCard`/`AO3WorkCoverCard` in a two-column `LazyVGrid` instead of new card types, in `LibrarySectionListView.swift`/`HomeSectionListView.swift`. **Branch E** drag-reorder: scoped to Reading Queue works only (`ReadingQueueMembership.sortOrderInQueue` already existed, zero schema change; Collections explicitly deferred — no order field, real migration needed). New `ReorderHandle.swift` + `ReadingQueueService.reorder(_:in:context:)`; detailed rows use native `List` + `.onMove`; the new compact grid uses `onDrag`/`onDrop` scoped to the handle. **Adversarial review pass** (4-dimension parallel review + independent verification of each finding) caught and fixed 9 real issues before merge, most notably: D2's `isSaved` removal had left plain non-AO3 imports unprotected from permanent EPUB deletion on "Mark Finished" (fixed by making `SavedWork.isProtected` also cover `ao3WorkID == nil`); the detailed-list reorder's `.onMove` had no gate on macOS (`EditMode` is iOS-only) so rows were draggable outside Reorder mode with filtered/unfiltered index mismatch (fixed with `.moveDisabled`/an internal guard); the compact grid's drag was attached to the whole card instead of just the handle, leaving a blurred Mature card's reveal-tap gesture live underneath during reorder (fixed by hit-test-disabling the card and scoping the drag to the handle); D6's `filterHistory` wasn't cleared by `clearQuery()`/`runSaved()`; selection wasn't cleared across a pagination page change in three surfaces. Verified after every commit and after the fix pass: `Scripts/verify.sh` full green (invariants, lint, **235 tests / 30 suites**, iOS Simulator + macOS Debug builds, whitespace). Ten commits: `ba61d3f`…`905ad83`. **Manual remaining:** simulator screenshot pass of every changed surface (the project's own UI-consistency approval gate) — not performed this session (no interactive screen-control); the disclosed low-severity gap where the compact grid's drag saves on every card boundary crossed instead of debouncing to the drop (real sync churn, not a correctness bug). | Claude Sonnet 5 | `release-hardening` | `905ad83` | 2026-07-08 |
| T-76 | **Normalize AO3 EPUB Preface/Summary/Chapter/Afterword indexing in both readers** — the reader index and bottom progress pill treated every EPUB spine item as an equally-weighted numbered chapter, so an AO3/Calibre export's Preface counted as chapter 1, its Summary (which AO3/Calibre never gives a navigable TOC heading, confirmed by unzipping and inspecting a real reference export: `toc.ncx` has 103 navPoints against a 104-item OPF spine, with the untitled gap sitting exactly between the "Preface" navPoint and "Chapter 1") was entirely invisible, and the denominator was inflated by however much front/back matter existed. New platform-agnostic `kudos-ao3-reader/Reading/ReaderSection.swift`: `ReaderSectionKind` (`.preface`/`.summary`/`.chapter`/`.afterword`/`.other`) + `ReaderSection` (one per spine item, `id == spineIndex`) + `ReaderSectionBuilder.build(tocEntries:spineHrefs:)`, which reconciles a TOC (however incomplete) against the full spine: classifies by exact-match TOC title ("Preface"/"Summary"/"Afterword"; everything else — "Chapter N", "Epilogue", "Epilogue: Chapter N" — defaults to `.chapter`, satisfying the "treat epilogues as story chapters" requirement for free); synthesizes a `.summary` entry for an untitled spine gap, but **only** when a real `.preface`-classified TOC entry was already seen and no summary has been placed yet — this single gate is the entire AO3-specificity boundary, so an EPUB with no "Preface" TOC entry at all (i.e. every non-AO3 EPUB, verified against the bundled `sample.epub` fixture) can never enter the synthesis branch and is byte-for-byte unaffected. `ReaderSection.pillLabel(storyChapterTotal:)` renders "P"/"S"/"A" for front/back matter and "`<index>`/`<total>`" for real chapters, where `<total>` prefers `SavedWork.totalChapterCount(from:)` (new — parses AO3's own "Chapters: X/Y" stat's Y side, `nil` for "5/?" WIPs) over `[ReaderSection].storyChapterCount` (new — counts only `.chapter`-kind sections), matching the spec's stated preference order exactly. **iOS/Readium** (`Features/ReaderReadium/ReadiumReaderView.swift`): `ReadiumBook` gained `readingOrder`/`sections`, built once in `open()` by resolving `Publication.tableOfContents()`'s `Link.href` strings against `Publication.readingOrder` via a new fragment/path-insensitive `ReaderSectionBuilder.hrefKey` (verified `Link.href: String`/`Link.title: String?` directly against the vendored Readium source rather than inferring the API); `go(toSpineIndex:)` added so the synthesized Summary entry (which has no TOC `Link` of its own) is still navigable through Readium's real navigator. `progressPill` and the `chapterRows` TOC sheet now read `book.sections` instead of raw `book.toc`/`pos.chapter`; per-position page/percent tracking is untouched. **macOS legacy reader** (`Features/Reader/ReaderView.swift`): given full parity rather than being left behind again (the exact bug class T-73 already found once, in a different subsystem) — `EPUBDocument.chapters` already carries pre-resolved spine indices, so wiring here needed no href-matching at all; new `sections` `@State` built alongside `document` in `load()`, `positionLabel`/new `currentSectionLabel`/`longFormPositionLabel` replace the raw `Ch \(currentIndex+1)/\(chapterCount)` computation for both paged and scroll reading modes, `chapterRows` reads `sections` instead of `document?.chapters`. Both platforms filter `.kind != .other` out of their index/TOC sheet (per spec) while leaving `.other` items reachable by ordinary page-turning (page-turn/pagination mechanics were not touched on either platform). Added `KudosTests/ReaderSectionTests.swift` (16 tests) — the reference EPUB's exact shape reconstructed synthetically (Preface + untitled-gap Summary + 95 numbered chapters + 6 "Epilogue: Chapter N" + Afterword = 104 spine/103 TOC, matching the real unzipped file) rather than bundling the real, specific fanwork into the repo: every literal "Reference EPUB Assertions" bullet from the spec (index membership, Summary between Preface and Chapter 1, all four pill label formats, `1/101` for real Chapter 1, denominator excludes front/back matter), plus the full stated regression matrix (no Afterword, single chapter, multiple chapters, Summary missing from TOC vs. already present — not duplicated, non-AO3 EPUBs via the real bundled fixture, empty/missing TOC falls back to the old one-section-per-spine-item behavior, `hrefKey` normalization). Verified: `Scripts/verify.sh` — invariants OK, lint clean, **233 tests / 30 suites** (up from 217; the two progress-computation tests flagged as must-not-break, `SavedWorkProgressTests.progressUsesChapterPositionWhenChaptersKnown`/`readiumProgressTakesPrecedenceOverLegacyChapters`, pass unchanged since this branch never touched `SavedWork.readingProgress`), both iOS Simulator and macOS Debug builds succeed, whitespace clean. **Manual remaining:** an actual on-device read-through of a real AO3 multi-chapter work with Preface/Summary/Afterword, on both platforms — the reconciliation logic is fixture-tested exhaustively, but live Readium pagination behavior (chrome, page-turn animation, the sheet's `.presentationDetents`) wasn't and can't be from a unit test. | Claude Opus 4.8 | `release-hardening` | (this commit) | 2026-07-07 |
| T-75 | **Fix EPUB import silently failing for undownloaded iCloud Drive files** — Settings → Import EPUB let the user select files, but "Open" appeared to do nothing for any file shown with the cloud-download icon in Files. Root cause, confirmed by reading the full call chain (`SettingsView.importEPUBs` → `importUserEPUB` → `UserEPUBInspection.inspect` → `EPUBDocument.inspectPackage`, `kudos-ao3-reader/Reading/EPUB.swift:236`): the chain has zero iCloud-download awareness anywhere — `inspectPackage` does `try? Data(contentsOf: url)` on a `.fileImporter`-returned URL the instant the picker dismisses; a not-yet-materialized placeholder fails that read and `try?` silently converts it to the generic `EPUBError.unreadableFile`, with no wait and no retry, matching the user's own diagnosis exactly ("Files download before being imported and if the select view is dismissed before download is done then the import is cancelled"). Fix: new `waitForUbiquitousDownload(of:pollInterval:timeout:)` (`kudos-ao3-reader/Services/WorkImporter.swift`) — no-ops immediately for any non-ubiquitous (already-local) file via `URLResourceKey.isUbiquitousItemKey`, otherwise calls `FileManager.startDownloadingUbiquitousItem(at:)` and polls `.ubiquitousItemDownloadingStatusKey` (0.3s interval, 120s timeout) until `.current`, throwing a new typed `UserEPUBImportError.iCloudDownloadTimedOut` with an actionable message on timeout. `SettingsView.importEPUBs` now: starts security-scoped access for every selected URL up front and holds it for the whole pass (not just the read — materialization needs the scope too) via `accessedURLs = urls.filter { $0.startAccessingSecurityScopedResource() }`; kicks off `startDownloadingUbiquitousItem` for every selected file immediately (so files later in a multi-select are already downloading by the time their turn comes, instead of each one blocking the next); then per file, awaits the download with progress text ("Waiting for iCloud Drive… (N of M)") before importing. `WorkImporter.swift` picked up a `// swiftlint:disable file_length` (702 lines; matches the existing convention on `SettingsView.swift`/`WorkDetailView.swift`/`KudosBackup.swift` rather than splitting cohesive import-funnel code for a lint threshold). Added `EPUBTests.waitForUbiquitousDownloadNoOpsOnAPlainLocalFile` (asserts the overwhelmingly common case — a plain local file — returns in <1s, not the 5s `pollInterval` used in the test, proving the early-return path rather than an accidental fall-through to polling). Verified: `Scripts/check-invariants.sh` OK, `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (217 passed, up from 216), macOS Debug build succeeded. **Manual remaining:** an actual iCloud Drive file that hasn't been downloaded yet, on a real device — the code path is grounded in the documented `URLResourceKey`/`FileManager` iCloud APIs and compiles/type-checks against them, but the download-in-progress → `.current` transition itself can't be exercised in the simulator (there's no real iCloud container in CI). | Claude | `release-hardening` | (this commit) | 2026-07-07 |
| T-74 | **Agent-quality gap closure: mechanical invariant gate, one-command verification, missing test suites, process templates** — converts the operational docs' prose rules into enforcement. **New `Scripts/check-invariants.sh`** (8 grep-based gates, each from a real debugged bug, each failing with a why + doc link): exactly one `.fileImporter` in `SettingsView.swift` (T-73 shadowing); `Mozilla/5.0` UA literal only in `AO3AuthService.swift` (`AO3RequestDefaults.userAgent` single-sourcing); `URLSession(configuration` only in `AO3Client.swift`/`AO3AuthService.swift`; no `var isDeleted` in `Models.swift` (T-70 CoreData collision); no `searchText` in `KudosBackup.swift` and no `.markModified(` in `WorkSearchIndex.swift` (derived-index invariants); no `try! `/`as! ` in app code; no `devplaceholder` anywhere; `replaceItemAt` still present in `FolderSyncService.swift` (staged-write invariant). **New `Scripts/verify.sh`**: the whole definition-of-done as one command (invariants → `lint.sh` → full iOS suite → `build-macos.sh` → `git diff --check`); dogfooded end-to-end this commit (`verify: ALL GREEN`). **Coverage gaps closed** (both were named gaps in `REGRESSION_TEST_MATRIX.md`): new `KudosTests/LibraryFiltersTests.swift` (10 tests: newest-first default, categorized→flat tag fallback, excludeTags, `Tag`-relationship userTags, lenient rating text, Underage dual-spelling `matchNames`, completion split, case-insensitive language, word bounds skipping unknown counts, all four `LibrarySort` orders) and new `KudosTests/AO3ClientPolicyTests.swift` (8 tests) over two new testability seams in `AO3Client.swift`: `retryDelay(for:attempt:)` made internal (Retry-After ≥ backoff, exponential doubling, transient-vs-never-retry classification incl. `.forbidden`/`.cancelled`) and pure `paceStep(now:nextAllowed:minInterval:)` extracted from `pace()` (first-call no-wait slot claim, rapid callers spaced at the interval, no idle credit accumulation). **Process made transferable:** new `docs/ADVERSARIAL_REVIEW_TEMPLATE.md` (review-the-tree-not-the-report; per-concern reviewers + cross-cutting critic; refute-don't-confirm; findings need file+line+failure-scenario; check both `#if os` branches; grep shared-thing call sites; CONFIRMED/PLAUSIBLE/REFUTED discipline) and `docs/TASK_PROMPT_TEMPLATE.md` (the grounded per-task prompt structure that made T-73's implementation agents near-correct). **Doc updates:** `AGENT_ONBOARDING.md` definition-of-done now starts with `Scripts/verify.sh`, adds same-commit doc-freshness and reporting-discipline rules (every claim cites file/function; no unverified "currently does X"; numbers from command output — added at the human's instruction); `REGRESSION_TEST_MATRIX.md` marks gaps 1–2 closed; `AGENTS.md` checklist updated. Verified: `Scripts/verify.sh` ALL GREEN — invariants OK, lint clean, **216 tests / 29 suites** (up from 198), macOS build succeeded (pre-existing Swift-6 advisory warnings in `WorkImporter.swift`/`FandomCatalog.swift`/`AO3Client.swift:817,856` confirmed pre-existing: first two files unmodified in this diff, AO3Client hunks at `@@ -42,12` and `@@ -147,7` only), whitespace clean. **Not closable by tooling (still human/manual):** live AO3 write-action verification, device pass (BGTask/iCloud Drive/folder picker), UI screenshot gate. | Claude | `release-hardening` | (this commit) | 2026-07-07 |
| T-73 | **Release hardening — 8 consolidated fixes from the three adversarial reviews** — **(1) Dead "Import EPUB" button fixed:** SettingsView chained four sibling `.fileImporter`s on one Form node and SwiftUI honors only one file-dialog presenter per node, so only the last-attached (sync folder, added by T-68 *after* the EPUB importer — which is exactly when the button died) ever presented. Consolidated into one enum-driven importer (`FileImportKind`: fonts/backup/epub/syncFolder; derived `isPresented` binding; per-kind content types/multi-select/handlers preserved exactly, `.fileExporter` untouched); hardened so the completion falls back to a `lastPresentedImport` recorded via `.onChange` — whether dismissal nils the active kind before or after the completion runs is an OS implementation detail, and the result must never be dropped either way. **(2) Sync-up can no longer destroy the only cloud copy:** `coordinatedWriteContents` staged remove-then-write; now stages to a temp and swaps via `replaceItemAt` (moveItem on first write), original package surviving any failed write — staging in `itemReplacementDirectory` (same volume as the destination) so external-drive sync folders don't fail the swap with EXDEV. **(3) Unchanged sync-downs skipped:** `performSyncDown` early-returns (`FolderSyncResult.skippedUnchanged`) when the package's coordinated `contentModificationDate` matches the stamp stored after every successful restore *and* after this device's own sync-up writes — never skipping while unresolved `NSFileVersion` conflicts exist (they can arrive without the main file's date moving). Kills the every-launch full-package read (all EPUB blobs into RAM) for the common nothing-changed case. **(4) TOCTOU duplicate-insert race closed:** `importEPUB` now re-checks `existingWork(forSource:)` after the caller's multi-second download and merges (fill-only, same path as the Recently-Deleted revive, which is byte-identical) into ANY existing match instead of inserting a twin. **(5)** `WorkSearchIndex.rebuildIfNeeded` is async with `Task.yield()` every 200 works + a `modelContext != nil` liveness re-check per record (yields let a user delete a fetched work mid-rebuild; touching an invalidated model asserts — same guard as the migration service). **(6)** Authenticated GETs coalesce in-flight duplicates (`RequestCoalescer` keyed URL+Cookie so a mid-flight account switch can't leak a response across sessions; value carries `response.url?.path` since URLResponse isn't Sendable; login-redirect check unchanged; `submitWrite` untouched — writes never coalesce/retry). **(7)** Locked/failing AO3 works now wait out cooldowns: new `SavedWork.lastTagRefreshAttemptAt` (device-local, deliberately NOT in `.kudosbackup`) gives `needsAO3Refresh` a 24h attempt cooldown (nil/first attempts pass), stamped in `refreshFromAO3` before the fetch; `WorkUpdateChecker` now stamps `lastUpdateCheck` on *failed* checks too so the 6h throttle actually applies to failures. **(8)** Browse batch actions hold their `Task` in `@State` and cancel in `.onDisappear` — the bulk loops' existing CancellationError handling stops them cleanly instead of resolving works after the user leaves. Built via a 7-agent parallel workflow (disjoint file sets) + build stage; the review panel hit a rate limit so the adversarial pass was done by hand, finding 3 hardening gaps (the dismissal-ordering fallback in 1, the EXDEV staging in 2, the liveness guard in 5) — all applied. New tests: failed-sync-up-write preserves the remote package, sync-down skip/re-restore cycle (incl. own-write stamp), importEPUB merges into a non-pending existing match. Verified: `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (198 passed, up from 195), macOS Debug build. **Manual remaining:** simulator tap-through of all four Settings pickers presenting one after another (Fonts → Backup import → EPUB import → Sync Folder) + Export Backup still presenting — the consolidation is logic-verified but the release gate's interactive pass should include it. | Claude | `release-hardening` | (this commit) | 2026-07-07 |
| T-72 | **Library search indexing + AO3 network hardening + storage audit** — three-part scalability/correctness pass. **(1) Local search index:** new derived, rebuildable `SavedWork.searchText` + `searchIndexVersion` (additive fields with defaults — lightweight migration) built by the new `WorkSearchIndex` (normalize = lowercase + diacritic-fold under a fixed `en_US_POSIX` locale, so "Héroïne" matches "heroine" regardless of device locale; composition = title/author/categorized tags/un-categorized flat tags/rating/language/completion words/HTML-stripped summary capped at 600 chars). Explicit `reindex` at every content-mutation chokepoint (`importEPUB` fresh+revive, `importUserEPUB` fresh+duplicate, `WorkTags.refreshFromAO3` + `backfillFromEPUB`, `ReadingQueueService.applyRemoteMetadata`, `WorkMetadataRefresh.refresh`, the backup-restore merge loop) — grep-verified that no other code path writes indexed fields (only model initializers, whose insertion sites all reindex). `rebuildIfNeeded` sweeps `searchIndexVersion != current` at launch (`ContentView`, after migration/sweep) — covers pre-index libraries, older-schema records, and anything a restore missed. Deliberately **not** `markModified()`-driven: a reindex never bumps sync timestamps (it must not look like a user edit to backup/folder-sync merges), and the index never travels in `.kudosbackup` (test-asserted against the manifest JSON) — restore rebuilds it from real fields. Consumers: Global Search's "In Your Library" now matches per-keystroke against the precomputed text (AND-across-terms, so multi-word queries span fields — previously title/author-only substring, re-lowercased per keystroke and per work); `LibraryFilters.matches` now builds its per-work tag `Set`s only for facets that are actually set, so default filtering over a large library costs no per-work set construction and no `tags`-relationship faulting. **(2) AO3 hardening:** User-Agent now appends an honest product token + contact URL (`KudosReader/<CFBundleShortVersionString> (+github.com/cidy02/kudos-ao3-reader)`) to the browser-like base; new slot-claiming `pace()` in `AO3Client` spaces every request start ≥0.6s apart across all four network touchpoints (GET / auth-GET / write-POST / EPUB download, retries included) — the actor's old "one request at a time" header claim was false under actor reentrancy, so the pacing is now real rather than assumed; explicit `AO3Error.forbidden` for HTTP 403 (never retried — hammering a block only prolongs it); fixed a cancellation bug in `FandomWorksView`'s batch loops where a cancelled task burned through all remaining items, counting each instant `CancellationError` as a per-item failure. Pre-existing and verified still sound: `RequestCoalescer` (in-flight GET dedup), `AO3RequestCoordinator` (3-slot concurrency cap), exponential backoff honoring `Retry-After`, typed 429/404/5xx, transient-only retry policy, `WorkUpdateChecker`'s 6-hour per-work throttle. Audited: no `softDelete`/`hardDelete` sits in any network error path — a 404 marks `ao3Unavailable` and keeps the local copy. **(3) Storage audit:** identity/dedup (shared `WorkIdentityIndex`, revive-on-reacquire) and backup round-trip (v7, tombstones, `incomingWins` merges) confirmed sound from this session's earlier passes; EPUB metadata reads already run off-main via `Task.detached` (whole-file reads are inherent to `MiniZip`, acceptable at fanwork sizes); `workTags` flat-union duplication kept deliberately (backup compat + filtering for un-enriched works) but de-duplicated out of the search index at index time. **Known scalability ceiling flagged, not fixed:** every list surface `@Query`s the full `SavedWork` table into memory; fine into the hundreds-to-low-thousands, but the eventual fix (paged/sectioned fetches, or SwiftData `#Index` when the toolchain supports it here) is follow-up work, not this branch. Added `WorkSearchIndexTests` (6: normalization + terms, full-field composition with HTML stripping, AND-matching across fields, fill-path reindex via `applyRemoteMetadata`, launch rebuild once-and-only-once, backup round-trip excludes the index and restore rebuilds it). Verified: `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (195 passed, up from 189), macOS Debug build. | Claude | `indexing-implementation` | (this commit) | 2026-07-06 |
| T-71 | **Canonical Work Identity — one card per AO3 work across Home/Library/Account** — the same AO3 work no longer renders as two separate cards (once local, once remote) on any surface. New shared `WorkIdentityIndex` (the 3-tier matcher: AO3 work ID → canonical AO3 URL → record UUID) extracted from the three previously-independent implementations: `WorkRestoreIndex` (backup/sync restore) is now a thin wrapper over it, and `RemoteWorkContextMenuModifier.existingLocalWork` + `ReadingQueueService.existingWork(for:)` are one-line calls into it — every surface now answers "is this the same work?" identically (`KudosBackupTests` pass unchanged over the refactor, the pure-refactor gate). New `CanonicalWork` (query-time `{local, remote}` pairing with a stable ForEach id) and `CanonicalWorkMerge` with two deliberately distinct operations: **`remoteLed`** for remote-defined lists (Subscriptions, Bookmarks, History, My Works, collection pages — AO3's own order is kept, matched entries carry their local record, local-only works are deliberately absent since not being in the remote list means not being part of it) and **`remoteOnly`** for the hybrid Saved-for-Later surfaces (remote entries whose work exists *anywhere* in the local library drop out entirely — the work already renders as its single richer local card wherever that card lives, e.g. Reading Now). Surfaces updated: Home's Subscriptions carousel renders the new `CanonicalWorkCoverCard` (richer local cover card — reading progress, local context menu, straight-to-reader navigation — when a local record exists; the remote AO3 card otherwise); Library's Saved-for-Later carousel and its See-All `LibrarySectionListView` list dedupe their remote Marked-for-Later entries against the full library (this was the confirmed literal duplicate-rendering bug that motivated the feature); `AO3AccountWorksList` (shared by Bookmarks/History/Marked for Later/My Works/collections + Home's Subscriptions See-All) pairs matched works into `SensitiveWorkRow` local rows. **Privacy:** pairing matches only privacy-visible local works (Home + account lists), so a Hide-mode Mature work keeps its plain remote row instead of surfacing local state for it; in Blur mode the paired local row blurs itself (`SensitiveWorkRow`'s existing behavior). Added `CanonicalWorkMergeTests` (8: ID match; URL-variant match with no stored `ao3WorkID`; remote-only passthrough + local-only dropped; order preservation; duplicate remote mention paired once with unique ForEach ids; record-UUID tier; `CanonicalWork` id/accessor stability). Verified: `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (189 passed, up from 181; `KudosBackupTests`/`ReadingQueueTests` unchanged), macOS Debug build. **Manual remaining:** simulator screenshot pass of the four surfaces with a deliberately-duplicated work (the TASKS.md UI-consistency approval gate applies before merge); `RemoteWorkContextMenuModifier`'s Save/Delete label behavior on the shared matcher is logic-identical (same tiers, same inputs) but worth one interactive spot-check alongside it. | Claude | `feature/canonical-work-identity` | (this commit) | 2026-07-06 |
| T-70 | **Preserved Work / 90-day Recently Deleted recovery** — deleting a work, collection, or reading queue now moves it to a recoverable "Recently Deleted" state for 90 days instead of an instant, permanent delete. New `PreservedWorkService` (`softDelete`/`restore`/`hardDelete`/`sweepExpired`) reuses the existing (previously inert) `isDeleted`/`deletedAt` fields plus a new `permanentDeletionScheduledAt: Date?` on `SavedWork`/`WorkCollection`/`ReadingQueue` — deliberately **not** `ReadingQueueMembership` (no user-facing trigger ever soft-deletes a membership alone; removing a work from a queue has always been non-destructive, and a soft-deleted queue's memberships are left untouched so restoring brings the whole queue back intact). `WorkLifecycle.delete` renamed to `hardDelete` (permanent-only, called by `sweepExpired`/"Delete Permanently"); all ~11 call sites across `WorkCardActions`/`LibrarySectionListView`/`LibraryView`/`ReadingQueueService`/`Collections`/`ReadingQueues`/`AccountView`/`PrivacyDataView` now soft-delete instead. Added an AO3-availability check (`SavedWork.ao3Unavailable`, already cached from tag refresh) that escalates the delete-confirmation copy when a work is no longer on AO3 — restoring it becomes the only way to keep it. New `RecentlyDeletedView` (Works/Collections/Reading Queues sections, days-remaining label, swipe-to-restore/delete-permanently) with a Library dashboard entry row shown only when non-empty. Manifest bumped to v7: `permanentDeletionScheduledAt` added to works/collections/queues, and `isDeleted`/`deletedAt` now travel for reading queues for the first time (previously never carried at all); merged the same `incomingWins`-gated way as `isDeleted`. Every existing `@Query` that lists works/collections/queues (12 files) now excludes `isPendingDeletion` so a Recently Deleted record disappears from the normal Library/Home/Search/Account surfaces. `sweepExpired` runs at launch (independent of Auto Sync) and after a successful background sync; a migration pass backfills `permanentDeletionScheduledAt` for any pre-v7 backup that left a record soft-deleted with no recovery deadline. **Two real bugs found and fixed during verification, both worth flagging for future model work:** (1) a property literally named `isDeleted` on an `@Model` class is unreliable — it silently reverts to `false` on every `context.save()`, evidently colliding with CoreData's own reserved, read-only `NSManagedObject.isDeleted` bookkeeping (a same-shaped differently-named field survived a save fine; `isDeleted` never did) — fixed by renaming the *live* `@Model` property to `isPendingDeletion` on all three types (the `.kudosbackup` JSON field name and `KudosBackupWork/Collection/ReadingQueue` struct fields are unaffected — they're plain `Codable` structs, not `@Model`, so `isDeleted` stays as the on-disk key for backward compatibility); (2) the `.kudosbackup` encoder used plain `.iso8601` date encoding, which has no fractional-second support and silently truncates every timestamp to the whole second — two edits landing in the same wall-clock second (a real possibility with fast automated sync, and not impossible with real auto-sync either) became unorderable by every `lastModifiedAt`-based merge decision in the file; fixed with a custom formatter (`.withFractionalSeconds`) on encode, with a fallback to the old whole-second formatter on decode so older backups still read correctly. Also fixed: `PreservedWorkService.softDelete`/`restore` weren't calling `markModified()`, so the soft-delete state couldn't win real sync merge conflicts (found via the two-device convergence test). Added `PreservedWorkTests` (soft-delete field/EPUB-retention, restore/tombstone-retraction, queue soft-delete preserves memberships, sweep date-gating + gate no-op, sweep hard-deleting a queue tombstones memberships and updates `isQueuedForLater`, AO3-unavailable confirmation-copy escalation, manifest v7 round-trip, `incomingWins` gating, full two-device soft-delete→restore convergence). Verified: `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (178 passed, up from 168), iOS Simulator build, macOS Debug build. **Adversarial review + fix pass (Claude, pre-merge):** a line-by-line review of the branch diff found two real gaps, both fixed in the follow-up commit. **(1) Ghost rendering:** the `@Query` exclusions covered every list view, but relationship-derived listings weren't filtered — a soft-deleted work still rendered fully visible/readable inside `ReadingQueueDetailView`/`ReadingQueueCard` (via `queue.memberships`) and `CollectionDetailView`, and inflated `CollectionCard`/`AddToCollectionView` counts (via `collection.works`); the old hard-delete's cascade removed memberships so this was a real regression, not cosmetic. Memberships/links still survive for restore — they're now filtered at render time only. **(2) Re-acquiring a Recently Deleted work never restored it:** every acquisition path matched the hidden pending record — `DownloadQueue` marked a fresh Save "skipped" (user saves, nothing appears, the work stays scheduled for permanent deletion), `WorkDetailView` adopted the hidden record, `ReadingQueueService.addToSavedForLater`/`addAndPreserveSummary`/`preserveSeries` queued ghosts the sweep would later hard-delete out from under the queue, `importUserEPUB` reported "duplicate" while the only copy sat in Recently Deleted, and `importEPUB` (the shared AO3-download funnel) has **no dedup at all**, so paths that did proceed would insert a hidden-twin duplicate that collides on restore. Fixed with one consistent policy — *re-acquiring revives*: `importEPUB` now revives a pending-deletion source match in place (keeping reading progress, swapping in the fresh EPUB) instead of inserting a twin; `DownloadQueue` restores an EPUB-bearing pending match with no download at all (EPUB-less ones fall through to the download and revive via `importEPUB`); `WorkDetailView` deliberately does *not* adopt a pending match (page shows remote state; Save revives); the three queue-service acquisition paths and `WorkCardActions.resolveLocalWork` restore before use; `importUserEPUB` reports `.restored`. **(3) A crash class the new tests exposed:** the fire-and-forget `WorkTags.refreshFromAO3` task crashes with SwiftData's invalidated-model assertion if the work is deleted (or, in tests, the whole container torn down) before/while it runs — its very first line reads `work.ao3WorkID`. Added `modelContext != nil` liveness guards at entry, after the network await, and in the 404 catch (the same guard pattern `PersistenceMigrationService` already uses). **Minor:** `RecentlyDeletedView.daysRemaining` now rounds up (an hour after deleting it said "89 days left"); Settings' saved-works→queue migration now excludes Recently Deleted works. Added 3 regression tests (revive via `addToSavedForLater`; revive-not-duplicate via `importEPUB`; `.restored` via `importUserEPUB`). Re-verified after fixes: `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (181 passed, up from 178), macOS Debug build. **Manual remaining:** interactive tap-through of the Recently Deleted UI in a live simulator (screen-control permission was denied this session too) — logic is covered by `PreservedWorkTests`; the TASKS.md UI-consistency approval gate (screenshots + human approval) still applies before merge. | Claude | `feature/preserved-work-recovery` | (this commit) | 2026-07-05 |
| T-69 | **BGTaskScheduler background refresh for Library Sync Folder** — best-effort background sync freshness on top of the existing reliable launch/foreground/background/debounced sync (never a replacement for it). New `FolderSyncBackgroundTask` (iOS-only) registers a `BGAppRefreshTask` at `MyApp.init()` (before the scene attaches, per Apple's requirement — `MyApp` now builds an explicit shared `ModelContainer` instead of using the `.modelContainer(for:)` scene-modifier convenience), reuses `FolderSyncService.syncNow(in:)` unchanged (already gated by `PersistenceOperationGate`, so a rejection from contention completes quietly, no retry storm), and only submits a request when `FolderSyncSnapshot.isConnected && autoSyncEnabled` (`FolderSyncBackgroundTask.shouldSchedule(snapshot:)`, unit tested). `scheduleNext()` is called after launch's catch-up sync and again on `.background`/`.inactive` scene-phase transitions (re-arming, since a `BGAppRefreshTask` is one-shot per submission). **Info.plist blocker found and fixed:** `GENERATE_INFOPLIST_FILE=YES`'s `INFOPLIST_KEY_*` mechanism silently drops array-valued keys (`UIBackgroundModes`, `BGTaskSchedulerPermittedIdentifiers`) — confirmed via build + `plutil -p`; combining `INFOPLIST_FILE` with `GENERATE_INFOPLIST_FILE=YES` is a hard "Multiple commands produce Info.plist" error. Landed on a `PBXShellScriptBuildPhase` that injects both keys via `PlistBuddy` post-generation, iOS/iOS-Simulator only (`PLATFORM_NAME` guarded, no-op on macOS). Two further build-system issues surfaced and were fixed before this actually worked: (1) the phase had no declared `inputPaths`/`outputPaths`, so Xcode's build-graph scheduler had no ordering edge and could run it racing the internal Info.plist generation step — fixed by declaring `$(TARGET_BUILD_DIR)/$(INFOPLIST_PATH)` as both an input and an output; (2) `ENABLE_USER_SCRIPT_SANDBOXING=YES` (already on for this project) silently sandbox-denies a Run Script phase's writes to any path outside its declared outputs — `PlistBuddy` printed "Operation not permitted" but still exited 0, so the phase's own success echo was meaningless; the script now verifies the keys actually landed (`PlistBuddy -c Print`) and fails the build loudly if not, instead of trusting `PlistBuddy`'s exit code. A same-path input+output also requires a second, harmless dummy output (`BackgroundModesInjected.stamp`) since Xcode errors on a "mutable output but no other virtual output node" otherwise. Verified end-to-end: `plutil -p` on the built iOS Simulator `Info.plist` shows both keys present; macOS Debug build's `Info.plist` correctly has neither (script no-ops there); simulator install + launch clean (log shows `(BackgroundTasks) Handling app launch`, confirming registration succeeded). Added `FolderSyncBackgroundTaskTests` for the pure `shouldSchedule` gating logic (`BGTaskScheduler` itself isn't unit-testable). Verified: `Scripts/lint.sh` clean, `git diff --check` clean, full iOS suite green (168 passed, up from 167), iOS Simulator build + install + launch, macOS Debug build (`Scripts/build-macos.sh`). **Manual remaining:** real background-refresh firing (Xcode's "Simulate Background App Refresh" debug menu, and eventually real-device timing) — the pure gating logic is covered, but the OS's actual scheduling decision isn't something a unit test can exercise. | Claude | `feature/folder-sync-bg-refresh` | (this commit) | 2026-07-05 |
| T-68 | **No-entitlement Library Sync Folder** — replaced staged entitlement/CloudKit assumptions with a user-selected Library Sync Folder that stores `KudosLibrary.kudosbackup`, persists a folder bookmark, syncs down/up with `NSFileCoordinator`, folds file-provider conflicts through backup restore, gates folder sync against migration/import, and exposes Settings controls/disclosure. Backup manifest v6 now includes collections alongside works, queues, bookmarks, fonts, settings, and tombstones. Launch/scene-phase/debounced local changes trigger conservative folder sync when connected. Added deterministic temp-folder tests for package write/read, sync-down restore of works/queues/collections, two-device convergence, missing-file no-op, operation-gate reentrancy, conflict-content folding, and sync-up timestamp stability. Docs/README now describe folder-based iCloud Drive sync honestly; no entitlement/container/CloudKit APIs or `.entitlements` file were added. Verified `rg` forbidden API audit, `find . -name "*.entitlements"`, `git diff --check`, `Scripts/lint.sh`, focused `FolderSyncTests`, full iOS suite (`Scripts/test.sh 'platform=iOS Simulator,name=iPhone 17,OS=26.5'`), and macOS Debug build. **Manual remaining:** real-device iCloud Drive folder/provider behavior, quota/offline/conflict UX, and large-library timing. **Adversarial review + fix/completion pass (Claude):** a 6-lens review against both the transport prompt and the separate full-automatic-sync-and-onboarding prompt found this commit implemented transport well but left several required pieces of the second prompt missing or buggy; all fixed/added here. **Fixed:** (1) foreground `.active` sync-down had no throttle — added a 60s gate (bypassed by explicit dirty state); (2) background/inactive sync-up ran unconditionally on every transition even with zero changes, forcing a needless full-file rewrite/iCloud re-upload — now gated on a real durable `hasPendingFolderSyncChanges` flag (`FolderSyncService.markDirty()`/`isDirty`), cleared only after an actual write, checked again on next launch to catch anything a force-quit dropped mid-debounce; (3) `runGuarded()`'s gate-rejection throw bypassed `recordError()`, so a sync silently lost to contention left zero trace anywhere — fixed so it's always recorded; (4) reader close had zero folder-sync wiring at all — added a near-immediate (1.5s, not the full 7s debounce) best-effort sync-up on `onDisappear`; (5) collection membership merge was additive-only (pure union) with no way to represent an intentional removal, so an older sync file could silently re-add a work the user removed from a collection — added a `.workCollectionMembership` tombstone type (composite `(collectionID, workID)` ID) wired into both removal call sites in `Collections.swift`, consulted during restore with the same newer-wins escape hatch used elsewhere; (6) `WorkCollection.isDeleted` merged via unconditional boolean AND (currently dead code, but the same unguarded-boolean bug class fixed for `SavedWork` previously) — gated on `incomingWins` to match. **Added (previously entirely missing):** Auto Sync Settings toggle (default on once connected; automatic triggers respect it, manual Sync Now always works regardless); a first-install Library Sync Folder onboarding screen (`SyncFolderOnboardingView`, shown once after the existing `WelcomeView`, never in place of it) with Choose Sync Folder / Not Now / Don't Remind Me Again, gated by `hasConfiguredSyncFolder`/`hasPermanentlyDismissedSyncFolderOnboarding` (a session-only in-memory flag handles "remind me next launch" without touching either persisted flag); a lightweight Sync Details sheet in Settings (status + last result, kept out of the main list); collections/collection-conflict counts in `KudosBackupRestoreSummary` and the Settings import notice, matching the existing queue-conflict reporting. **Deferred, documented rather than silently skipped:** `BGTaskScheduler` background-refresh registration was not implemented this pass — foreground/lifecycle/debounced sync covers the primary reliable path, and background refresh would only improve freshness at the margin; worth adding later since it's a standard `UIBackgroundModes` capability and (unlike CloudKit) does not require a paid Apple Developer account. Added 8 regression tests (gate-rejection error visibility, dirty-flag clears only after a real write, collection-membership tombstone suppresses a stale resurrection, a demonstrably-newer snapshot still revives it, collection-level suppression is reported in the restore summary). **Also fixed a real, pre-existing test-suite flake found while verifying:** `PersistenceOperationGate` is a process-wide static lock (correctly global for the real app, since only one instance runs), but `xcodebuild`'s default parallel test execution runs `PersistenceSyncTests`/`KudosBackupTests`/`FolderSyncTests` concurrently in separate simulator clones, letting them spuriously contend the same gate and non-deterministically fail (`PersistenceMigrationService.run()` silently no-ops and returns `.notStarted` when the gate is already held) — reproduced consistently across repeated full-suite runs. Fixed by adding `-parallel-testing-enabled NO` to `Scripts/test.sh` (the project's own canonical verification command) plus `@Suite(.serialized)` on the two suites that weren't already marked. Verified clean across 3 consecutive full-suite runs post-fix. Verified: `Scripts/lint.sh` clean, iOS build clean, full iOS suite green (167 passed / 0 failed, up from 153; deterministic now, was previously flaky under the default parallel runner). | Codex, Claude | `codex-icloud-drive-folder-sync` | (this commit) | 2026-07-04 |
| T-67 | **Queue tombstone conflict fix + iCloud sync staging** — added default-safe queue deletion metadata, queue membership freshness, and backup v4 queue/member timestamps; `.kudosbackup` restore now computes `effectiveQueueModifiedAt`, suppresses only stale queue/member snapshots, revives newer queue metadata or membership activity over older tombstones, preserves/reports ambiguous conflicts, and never falls back suppressed memberships into Saved for Later. Settings import summaries now report suppressed/revived/ambiguous queue conflicts. iCloud remains honestly staged: no entitlement/container or real-device CloudKit verification is claimed; docs list the blockers and manual device checklist. Added deterministic queue conflict/migration tests. Verified `git diff --check`, `Scripts/lint.sh`, focused Persistence/Backup xcodebuild tests, full iOS suite (`Scripts/test.sh 'platform=iOS Simulator,name=iPhone 17,OS=26.5'`), and macOS build. **Adversarial review + fix pass (Claude):** found and fixed two real gaps this commit had — (1) `effectiveQueueModifiedAt`/`effectiveModifiedAt` folded the backup file's own `exportedAt` wall-clock time into the revival decision, so a content-stale queue could be revived purely because the file it shipped in was written later than the tombstone (fixed by dropping `exportedAt`/`restoreOrImportDate` from the calculation entirely — only the queue's/memberships' own timestamps count now). (2) `SyncTombstone` rows never traveled in the `.kudosbackup` manifest at all, so a fresh install/reinstall restoring a backup had zero local tombstone history and no protection against resurrecting anything deleted after that backup was made — the single most common real-world use of `.kudosbackup` (device migration/reinstall). Fixed via manifest v5: tombstones now export with the backup and merge into the target's local store on restore, before conflict resolution runs, so a fresh device inherits the source device's deletion history. Added 2 regression tests for both fixes. Verified: lint clean, iOS build clean, full iOS suite green. | Codex, Claude | `codex-icloud-sync-queue-conflict-fix` | (this commit) | 2026-07-04 |
| T-66 | **iCloud backup & persistence refinement** — documented the current SwiftData + Application Support/Caches + `.kudosbackup` architecture, chose a conservative metadata-first iCloud-ready design, and added stable sync metadata/tombstones/migration state without enabling unverified CloudKit entitlements. Backup schema v3 now preserves sync/progress metadata with timestamp-aware merge rules; EPUBs keep stable asset identifiers and remain local/manual-backup assets until a signed iCloud build is tested. Settings shows local persistence readiness and iCloud account availability. Added persistence migration/merge/tombstone tests and docs. Verified `git diff --check`, `Scripts/lint.sh`, iOS build (`iPhone 17`, OS 26.5), full iOS tests (`Scripts/test.sh 'platform=iOS Simulator,name=iPhone 17,OS=26.5'`), and macOS build. **Adversarial review + fix passes (Claude, `e8a6164` + follow-up):** backup import now consults tombstones (was write-only — older backups could resurrect explicitly-deleted works, including privacy-cleared history) with a newest-tombstone-wins index and a newer-snapshot escape for works; members of a suppressed queue are dropped, not re-homed into Saved for Later; isFavorite/isSaved/isFinished/isComplete no longer OR-merge (older backups resurrected explicitly-unset flags) while brand-new records still adopt archive flags; `WorkLifecycle.delete` tombstones cascade-deleted queue memberships; `PersistenceMigrationService` is async with yield points + a re-entrancy guard + deleted-model guards (Settings retry no longer blocks the main thread); stage-aware migration error logging; Settings disclosure states nothing syncs to iCloud in this build. Six regression tests added. **Known toolchain issue (pre-existing, tracked separately):** Release-config builds (macOS + iOS device archive) crash the beta Swift compiler inside vendored SwiftSoup (`Element.appendNormalisedText` SIL ownership verifier); Debug builds unaffected. | Codex, Claude | `backup-refinement-icloud` | (this commit) | 2026-07-04 |
| T-62 | **Reading queue review pass + reader page-turn correction** — Independent adversarial re-review of `feature/native-ao3-queue` (13-dimension fan-out): confirmed lifecycle protection, split non-destructive/destructive removal, series consent-before-enumeration + quick auto-preserve toggle, custom-queue optional series, full 7-way series result accounting, atomic EPUB replacement, backup v1/v2 + AO3-identity restore, and container/schema coverage are all correctly implemented (prior "P0 lifecycle" review was stale). **Fixed** confirmed data-safety leak: the Library "Saved for Later" section (`LibrarySectionListView`) exposed queue-only works to a generic destructive Delete swipe that called `WorkLifecycle.delete` (hard-deleting the preserved EPUB, single-swipe when confirm-off) — now routes queue-only works to non-destructive `removeFromAllQueues`. **Fixed** history-query leak: `PrivacyDataView` + `AccountView` reading-history `#Predicate<SavedWork> { !$0.hasEPUB }` now also excludes `isQueuedForLater` (a queued work with pending/failed preservation has `hasEPUB == false` but is protected). **Cleanups:** removed dead `addAndPreserveCancellable`; deduped `postedChapterCount` into shared `SavedWork.postedChapterCount(from:)`. **Reader:** removed T-61's wrapper page-turn transform + floating page-edge overlay — it fired post-hoc on `locationDidChange`, after Readium already animated its native paginated swipe, producing a whole-view jiggle that didn't match Apple Books; paged turns now defer to Readium's native horizontal slide (Apple Books-like). Swipe-down-to-dismiss retained. Verified `Scripts/lint.sh` (warnings only, pre-existing), iOS build (`iPhone 17`, OS 26.5), focused `ReadingQueueTests`+`KudosBackupTests` (32 passed). | Claude | `feature/native-ao3-queue` | (uncommitted) | 2026-07-01 |
| T-61 | **Reader UI refinements** — iOS Readium reader now supports a vertical-dominant swipe-down dismissal with distance/velocity thresholds, horizontal-swipe rejection, scroll-mode top-of-scroll gating, a progress flush before dismiss, and the existing close/navigation affordances intact. Added a lightweight wrapper-level Apple Books-inspired page/card shuffle animation for paged Readium locator changes, with direction inferred from Readium positions/progression and a reduced-motion minimal fade/slide path; chapter-list jumps are suppressed. **Limitation:** Readium owns the actual page renderer/gesture internals, so the shuffle is a wrapper-level transform around the navigator rather than a true per-page renderer transition. No Reading Queue data/model/backup logic touched. Verified `git diff --check`, `Scripts/lint.sh` (warnings only), iOS build (`iPhone 17`, OS 26.5), macOS build, and full iOS tests. | Codex | `feature/native-ao3-queue` | (this commit) | 2026-07-01 |
| T-60 | **Reading queue review follow-up + Browse category tint** — Browse category icons now use the active theme tint; queue removal is non-destructive by default with explicit destructive variants; series preservation prompts use one-page preview context before full enumeration, include the small-series auto-preserve toggle, and pace/cancel preservation; custom queues can explicitly add selected series works; result accounting separates preserved/already-preserved/unavailable/failed/skipped/cancelled; critical queue/preservation/lifecycle saves are throwing or explicitly logged; EPUB replacement paths audited around validated atomic replacement. Added focused queue/series/removal/download-failure tests. Verified `git diff --check`, `Scripts/lint.sh`, focused `ReadingQueueTests`, full iOS tests (`Scripts/test.sh 'platform=iOS Simulator,name=iPhone 17,OS=26.5'`), and macOS build. | Codex | `feature/native-ao3-queue` | (this commit) | 2026-07-01 |
| T-59 | **Native Reading Queues + Saved for Later preservation** — first-class ReadingQueue/ReadingQueueMembership state; queue-preserved EPUB lifecycle protection; default Saved for Later queue; backup v2/v1-compatible restore; queue-aware Library visibility; Work Detail queue actions; Settings queue storage/migration controls; safe small-series auto-preserve via one-page AO3 series preview; cancellable/slow-paced series preservation with progress; focused lifecycle/v1-backup/removal tests. Follow-up `89650ec` makes last-queue removal delete queue-only records, makes storage removal remove queue membership only, paces/cancels bulk preservation/migration work, and preserves saved/favorite records/files. Review-hardening `9fbd02e` fixes strict queue normalization, AO3-first/canonical backup restore identity, missing preserved EPUB repair, broken membership restore skipping, remote Save for Later failure semantics, canonical AO3 matching, and atomic EPUB replacement coverage. Verified `Scripts/lint.sh`, focused queue/backup/work-tag tests, `Scripts/test.sh 'platform=iOS Simulator,name=iPhone 17,OS=26.5'`, and macOS build. | Codex | `feature/native-ao3-queue` | `f0e7886`, `6cee074`, `c5659ca`, `16e2e18`, `89650ec`, `9fbd02e` | 2026-06-30 |
| — | **Browse category icon theming fix** — In `MediaBrowserView.categoryCard`, changed the main category icon from `.foregroundStyle(.primary)` to `.foregroundStyle(.tint)` (matching stat icons and other themed elements) so it respects the current app theme (AO3 red on Light/Dark, warm brown on Sepia). Small polish follow-up to native Browse on the queue branch. | Grok | `feature/native-ao3-queue` | (this commit) | 2026-06-30 |
| T-58 | **Import AO3 EPUB files** — Settings now has an Import EPUB row with native multi-file `.epub` picker; user EPUBs are validated, inspected, copied into existing `SavedWork.fileURL` storage, parsed for EPUB/AO3 metadata, deduped by AO3 work ID or title+author+file size, and enriched through the existing throttled AO3 tag refresh only when an AO3 work URL/ID is present. Imported EPUBs use the normal Library/Work Detail/Reader path and existing backup assets. Added focused EPUB/import tests. Verified lint, iOS build, macOS build, targeted EPUB tests, and full iOS simulator tests (`iPhone 17`, OS 26.5). | Codex | `feature/native-ao3-actions` | `3fec492` | 2026-06-28 |
| T-56 | **Expanded result tags + Library card depth follow-up** — expanded shared `AO3WorkRow` now shows de-duplicated Additional Tags after Characters and omits the section when empty; Library/Home work cards have stronger theme-aware elevation/border plus carousel padding so shadows are not clipped. UI-only; no AO3 behavior/data/navigation changes. Verified lint, iOS build, macOS build, and iOS simulator tests (`iPhone 17`, OS 26.5). | Codex | `feature/native-ao3-actions` | `154257a` | 2026-06-27 |
| T-55 | **Native iOS UI refinement pass** — Library/Home cards are self-contained AO3 work summaries (title/author/fandom/status/stats/state/progress inside the card, no fake cover placeholders); Browse category title-to-stats divider removed while lower dividers remain; category-detail fandom rows stack `|` names with one fandom icon and trailing work-count icon; canonical Work Detail gains a shared overview section. Preserved AO3 behavior/actions/navigation and avoided reader/network refactors. Verified lint, iOS build, macOS build, and iOS simulator tests (`iPhone 17`, OS 26.5). | Codex | `feature/native-ao3-actions` | `e4e1842` | 2026-06-27 |
| T-53 | Library **real Collections** (user-named shelves): `WorkCollection` SwiftData model (many-to-many w/ `SavedWork`; registered in the container); Collections dashboard section (leading "New Collection" card + per-collection cards); `CollectionDetailView` (list/remove works, rename/delete); `AddToCollectionView` sheet from a work's detail page (toggle membership, create). Builds iOS+macOS; section + New card sim-verified. | Claude | `test/home-tab-overhaul`→`main` | `2570f8e` | 2026-06-22 |
| T-52 | Library **top-fandom quick-filter chips** on the dashboard (data-driven `All` + most-common fandoms, reuse `TagChip`; tap → filter all sections incl. AO3 Marked-for-Later; trailing Reset; replaces the plain "Filters active" banner). Chip UI sim-verified. | Claude | `test/home-tab-overhaul`→`main` | `73d7e84` | 2026-06-21 |
| T-51 | **Master prompt Phase B** — networking politeness/local-first (`main` + `readium-migration`). (1) `AO3RequestCoordinator` — polite bounded-concurrency gate (3) complementing `AO3Client`; `FandomCatalog` loads Browse-by-category prefetches concurrently-but-bounded. (2) **Local-first disk cache** (`FandomCatalogCache`, stale-while-revalidate). (3) **Request coalescing** (`RequestCoalescer` in `AO3Client.fetchData`). 10 tests; sim-verified. **Proposed/deferred:** formalize `DownloadQueue`→DownloadCoordinator + `WorkImporter`→ImportCoordinator; wider cache/coalesce rollout. | Claude | both | `ae22b2f`, `aec3554`, `5e28a4c` | 2026-06-21 |
| T-50 | **Master prompt Phase A**: new app icon (red book + heart cutout + black bookmark, vector source `Design/AppIcon.svg`); first-launch **WelcomeView** (`hasCompletedOnboarding`, theme-aware, accessible); **shake-to-report** (`ShakeDetector` + `BugReportView` → prefilled GitHub issue) reachable by shake or About; `AppLinks`. Verified in sim. | Claude | `main` + `readium-migration` | `e56e97b`, `b1d23a2` | 2026-06-21 |
| T-49 | Add canonical **`docs/PROJECT_PHILOSOPHY.md`** (master prompt Part 3); linked from README + AGENTS. On all branches. | Claude | all | `7aa3d26` | 2026-06-21 |
| T-48 | **Security:** scrub the Apple `DEVELOPMENT_TEAM` ID from `project.pbxproj` → `""` (repo is public). All branches. Still in git **history** — purge needs a force-push rewrite (deferred, owner's call). | Claude | all | `e36dda5` | 2026-06-21 |
| T-46 | Layout overhaul — **Library** rebuilt as a 5-section carousel dashboard (Reading Now, Saved for Later, Finished, Collections, Downloaded) via shared `WorkCarouselSection`; collapsible, `>` See-all → full list, per-section empty states; Saved for Later merges AO3 Marked for Later; filters/insights/privacy/tag-routing/iOS bulk-select preserved. Verified in sim. | Claude | `test/home-tab-overhaul`→`main` | `3fc9c98` | 2026-06-21 |
| T-45 | Consolidate the `docs/Bugs.md` + `Feature_Ideas.md` + `UI_Polish_Todo.md` trackers into single BUG-N / FI-N / UI-N registries in `TASKS.md`; remove the three files; update README + AGENTS.md. | Claude | `test/home-tab-overhaul`→`main` | `bb0b224` | 2026-06-21 |
| T-44 | Layout overhaul — **Home** tab rebuilt as a Books-style dashboard: collapsible horizontal carousels (Reading Now, Recently Updated, Subscriptions, Favorites, Recently Opened) with `>` See-all, per-section empty states, reading-progress bars, and AO3 update detection (`WorkUpdateChecker`, `SavedWork.knownChapterCount`). Added the `Home` tab + `Kudos_Layout_Structure.md`. Verified in sim. | Claude | `test/home-tab-overhaul`→`main` | `5286dda` (+ `5ffa33f`, `306ac6f`) | 2026-06-21 |
| T-43 | Fix **BUG-4**: Library bulk-select `EditMode` is iOS-only → macOS build broke. Multi-select state guarded `#if os(iOS)`; macOS uses a plain `libraryList`. macOS + iOS both build, all tests pass. | Claude | both | _see git log_ | 2026-06-21 |
| T-42 | Portable `.kudosbackup` export/import for Library records, EPUBs, User Tags, bookmarks, custom fonts, and app/reader settings; merge-only restore through the system document picker (FI-19) | Codex | both | `6048684` / `5cd9394` | 2026-06-20 |
| T-41 | Local Reading Insights dashboard: works/words read, activity, completion, and top fandoms (FI-18) | Codex | both | `1cfe4b0` / `be74d8f` | 2026-06-20 |
| T-40 | Continue Reading shelf at the top of the Library (in-progress works, most-recently-read first → one-tap resume into the reader); added `SavedWork.lastReadDate` (FI-17) | Claude | both | _see git log_ | 2026-06-20 |
| T-39 | Settings → About / Sources & Licenses sheet (version, GPL-3.0, SwiftSoup/Readium/ao3_api credits, AO3/OTW disclaimer) (FI-16) | Claude | both | _see git log_ | 2026-06-20 |
| T-38 | Download queue (`DownloadQueue` + root progress banner) — "Download Whole Series" scrapes the series page (`AO3Client.seriesWorks`) and downloads/imports serially via the polite AO3Client; second half of "Download queue / bulk actions" (FI-15) | Claude | both | _see git log_ | 2026-06-20 |
| T-37 | Library bulk-select + bulk actions (delete / save / favorite) via EditMode + `List(selection:)`; first half of "Download queue / bulk actions" (FI-15) | Claude | both | _see git log_ | 2026-06-20 |
| T-36 | Phase-2: native AO3 work Subscriptions (4th "AO3" sub-tab) — reuses worksPage/parseSearchPage; only `li.work.blurb` items surface, so work subs only (FI-14). Completes the Phase-2 read backlog. | Claude | both | _see git log_ | 2026-06-20 |
| T-35 | Phase-2: native AO3 reading History + consolidate the account lists into one "AO3" segment with a sub-picker (`AO3AccountSection`) to avoid section-bar overflow (FI-13) | Claude | both | _see git log_ | 2026-06-20 |
| T-34 | Phase-2: native AO3 Bookmarks list — generalized the MfL view into `AO3AccountWorksList(kind:)`; `parseBookmarksPage` (`li.bookmark.blurb`, work id from `/works/` link, skips series/external) (FI-12) | Claude | both | _see git log_ | 2026-06-20 |
| T-33 | Phase-2 first authenticated feature: native "Marked for Later" reading list — Bookmarks "Later" segment, authenticated reads via AO3AuthService, reuses parseSearchPage + AO3WorkRow + pagination (FI-11) | Claude | both | _see git log_ | 2026-06-20 |
| T-21 | Calibrate Readium theme colors, typography units, margins, weight, built-in fallbacks, and imported custom-font rendering | Codex | `readium-migration` | — / `6fb3322` | 2026-06-20 |
| T-17 | Document EPUB ZIP/OPF/spine/TOC/metadata assumptions, import failures, security boundaries, tests, and Readium platform differences | Codex | both | `208df0c` / `a3f70ba` | 2026-06-20 |
| T-29 | Readium reader routes EPUB HTTP/HTTPS links to the in-app Browse tab while preserving system handling for non-web schemes | Codex | `readium-migration` | — / `6cb7525` | 2026-06-20 |
| T-32 | AO3 auth review follow-ups: off-screen login WebView gets a window, one silent hidden-login retry, calmer fallback copy, sign-up/reset links, AO3 HTML-fixture parser tests, doc + code clarifications | Claude | both | _see git log_ | 2026-06-20 |
| T-31 | Preserve successful AO3 login when Keychain is unavailable by recovering from WebKit's persistent app-scoped cookie store (BUG-3) | Codex | both | `3a3363d` / `39556be` | 2026-06-20 |
| T-30 | AO3 authentication foundation: native login, hidden WebView session capture, automatic visible fallback, Keychain persistence, session lifecycle, authenticated requests (FI-10) | Codex | both | `a5775d5` / `811a784` | 2026-06-20 |
| T-28 | EPUB web links (AO3 work/author/tag) open in the Browse tab, not inside the legacy reader's web view — verified in simulator (BUG) | Claude | both | _see git log_ | 2026-06-19 |
| T-15 | Sync in-app AO3 browser with app theme (FI-5) | Codex | both | `58663da` / `2f48e95` | 2026-06-19 |
| T-27 | Search Back returns to Browse (then the previous tab) after a fandom/typed search, instead of skipping straight to the tab (BUG) | Claude | both | _see git log_ | 2026-06-19 |
| T-26 | Toolbar "expand/collapse all" toggle for Search result cards | Claude | both | _see git log_ | 2026-06-19 |
| T-25 | Calm Search pagination layout (UI-1 follow-up) | Codex | both | `9374053` / `491b195` | 2026-06-19 |
| T-14 | Refine the Search pagination card (UI-1) | Codex | both | `024af77` / `1ab6781` | 2026-06-19 |
| T-24 | Enrich Browse-by-fandom cards: fandom/work counts, saved count, recently-read chips, regular text weight, section dividers (+ Search/Library card dividers) (FI-9) | Claude | both | _see git log_ | 2026-06-19 |
| T-16 | P2 AO3-red default accent + accent color picker (FI-6) | Claude | both | _see git log_ | 2026-06-19 |
| T-23 | Extend Include → Exclude → Clear cycling to Warnings/Categories (FI-3) | Codex | both | `ff4f93a` / `8373068` | 2026-06-19 |
| T-22 | Fix T-09 tag cycling UI + restore top picker search field (BUG-2) | Codex | both | _see git log_ | 2026-06-18 |
| T-09 | P2 Advanced rating + cycling include/exclude Search tags (FI-2, FI-3) | Codex | both | _see git log_ | 2026-06-18 |
| T-10 | P2 Expandable search result cards (FI-4) | Codex | both | _see git log_ | 2026-06-18 |
| T-11 | P2 Tap a tag (work/My) → filter the Library (FI-8) | Claude | both | _see git log_ | 2026-06-18 |
| T-12 | P2 Long-press Filters → Clear All Filters (FI-1) | Claude | both | _see git log_ | 2026-06-18 |
| T-13 | P2 Hide privacy eye button when no hidden works (FI-7) — *already implemented; verified* | Claude | n/a | — | 2026-06-18 |
| —    | docs: stable IDs + status across trackers | Claude | both | `2a8696d` / `1c3c8a4` | 2026-06-18 |
| —    | Collaboration system (`AGENTS.md` + `TASKS.md`) | Claude | both | `458dfd4` / `170d381` | 2026-06-18 |
| T-08 | P1 Sepia consistency verified (Settings); bug closed | Claude | both | `30f3e9a` / `746273a` | 2026-06-18 |
| T-06 | P1 Structured OSLog logging | Claude | both | `23392f0` / `d6b8c9f`+`b0ea6ff` | 2026-06-18 |
| T-05 | P1 Split `EPUB.swift` into focused files | Claude | both | `daa8422` / `edc07f4` | 2026-06-18 |
| —    | GitHub repo setup (GPL-3.0 LICENSE, README, docs/, topics) | Claude | both | `22ce718` / `20d7550` | 2026-06-18 |
| —    | P0-1 `KudosTests` target + 20 pure-logic tests | Claude | both | `7eac358`+`1012d53` / `8f6c7cc`+`aebe4ba` | 2026-06-18 |
| —    | P0-3 Typed `EPUBError`, AO3Client retry/backoff, surfaced failures | Claude | both | `362d5c0` / `cea2eb1` | 2026-06-18 |
| —    | P0-2 SwiftLint + SwiftFormat + pre-commit hook + CI | Claude | both | `f59a1bb` / `c5d9c1e` | 2026-06-18 |

_Older UI / reader / Library work predates this board — see `git log`._

---

## 🐛 Bugs (BUG-N registry)

_Consolidated from the former `docs/Bugs.md`._ **Status:** Open · In Progress · Fixed. Detail for each is in the Completed table / `git log` under its board task.

**Active:**
- **BUG-7** — (P2; found live in T-115's macOS pass, 2026-07-18) The legacy macOS reader lays its WKWebView content out **under the overlaid sidebar**: with the sidebar visible, the leading edge of every preface/metadata line is occluded ("~~Post~~ed originally on…", "~~Teen A~~nd Up Audiences", "~~Categor~~y:") and content centers relative to the full window rather than the detail column. Reproduced on "The Queen's Mercy". `Features/Reader/ReaderView.swift`/`ReaderController.swift` (macOS-only path) — the web view needs to respect the split-view detail column / sidebar safe area. Evidence: T-115 report §3.9.
- **BUG-6** — (pre-existing, low) Newest-first comments: after posting, the refresh sizes "last page" from the cached page count (`CommentsModel.knownTotalPages`), so a comment that rolls AO3 over onto a new page (every 20th top-level comment) isn't shown until a manual refresh. Found during the 2026-07-10 comment-ui-refinement review; not introduced by that branch. Fix idea: skip the cached count when `forceRefresh` is set.

**Closed — not a bug (owner decision):**
- **BUG-8** — (found live in T-115's macOS pass, 2026-07-18; closed 2026-07-19, Wave 0) macOS Library cards opening the reader directly is **working as intended** — the owner confirmed iOS does the same live on-device, and the code agrees: every Library card call site passes `openMode: .reader` (or navigates straight to `LocalWorkDestination.reader`) on both platforms — `LibraryView.swift:692,699`, `LibrarySectionListView.swift:313,327`, `Collections.swift:133`, `ReadingQueues.swift:285`. The "Library cards open detail" note under Key Decisions was stale (predated this behavior or was never accurate for the carousel/dashboard cards) and has been corrected there. No code change.

**Fixed & verified:**
- **BUG-5** — Opening Browse ballooned CPU + memory until iOS killed the app (owner-reported on iPhone 17 Pro). **Two root causes, fixed separately.** (1) View-side: `MediaBrowserView.stats(for:)` built a `Set` of each big media category's full (tens-of-thousands) fandom-name list and scanned the whole library, synchronously in the view body, per card, re-run on every render as `@Observable` fandom lists streamed in (~O(categories²×fandoms) on main). Moved off render+main: cards read a precomputed `statsByCategory`; a debounced `.task(id:)` snapshots works to `Sendable` structs and computes each name-`Set` once in `Task.detached`. (`685c8a3`.) (2) **Parser-side (the on-device killer, still present after fix 1):** `AO3Client.fandoms(atPath:)` SwiftSoup-parsed each multi-MB `/media/<name>/fandoms` page into a full DOM, then ran `li.select("a.tag")` **per `<li>`** (14k+ for the big categories) — each select triggering the vendored SwiftSoup's `rebuildQueryIndexes…`/`traverseElementsDepthFirst` over a several-hundred-thousand-node tree, ×3 concurrent categories. **Reproduced empirically in the sim** (cold fandom cache): RSS 330MB→**1.33GB** in ~10s, CPU pegged ~100% — the sim survives the transient churn, a phone jetsams mid-load. And because `FandomCatalog` persisted only after the whole task group, a mid-load kill lost every fetched list → the next open repeated the full burst (kill **loop**). Fixed: new `AO3Client.parseFandomIndex(_:)` — single linear cursor scan (no DOM), CSS-faithful class matching (`classList(in:)`), entity-decoded names, digits-only counts, deduped — **byte-identical results to the DOM parse on real AO3 pages** (Movies 14,496 / Anime & Manga 5,079 in back-to-back runs of both parsers); `FandomCatalog.loadMissing/refresh` now persist per landed category. Measured after: same cold-cache open peaks at **427MB RSS** (~340MB baseline) with sub-second CPU blips; Browse cards fill normally. +3 parser fixture tests. (`9ab14c2` on `merge-test`, `Scripts/verify.sh` green 282/33 iOS+macOS, 2026-07-10. **Owner: rebuild/reinstall on device to pick this up.**)
- **BUG-4** — Library bulk-select `EditMode` broke the macOS build; guarded `#if os(iOS)`, macOS uses a plain list (T-43, 2026-06-21).
- **BUG-3** — AO3 login was discarded when Keychain was unavailable; now falls back to WebKit's app-scoped cookie store (T-31, 2026-06-20).
- **BUG-2** — T-09 tag-cycling UI + tag-picker search-field placement regression (T-22, 2026-06-18).
- **BUG-1** — Sepia theme not applying app-wide; fixed via `.appThemedScroll()`/`.appThemedRows()` (T-08, 2026-06-18).

---

## 💡 Feature Ideas (FI-N registry)

_Consolidated from the former `docs/Feature_Ideas.md`._ **Status:** Idea · Planned · In Progress · Done · Parked. All current FI items are **Done** (board task in parens).

- **Search & Filters:** FI-1 long-press clear filters (T-12) · FI-2 advanced rating (T-09) · FI-3 cycling include/exclude multi-select (T-09/T-23) · FI-4 expandable result cards (T-10).
- **Browse / Web:** FI-5 sync browser theme (T-15) · FI-9 enrich browse-by-fandom cards (T-24).
- **AO3 account (auth + Phase-2 reads):** FI-10 auth foundation (T-30) · FI-11 Marked for Later (T-33) · FI-12 AO3 Bookmarks (T-34) · FI-13 Reading History + grouped AO3 section (T-35) · FI-14 work Subscriptions (T-36).
- **Library:** FI-7 hide privacy eye when nothing hidden (T-13) · FI-8 tap tag → filter Library (T-11) · FI-15 download queue & bulk actions (T-37/T-38) · FI-17 Continue Reading shelf (T-40) · FI-18 reading statistics (T-41).
- **App:** FI-16 About / Sources & Licenses (T-39) · FI-19 portable Library backup (T-42).
- **Theming:** FI-6 AO3-red accent + color picker (T-16).

---

## ✨ UI Polish (UI-N registry)

_Consolidated from the former `docs/UI_Polish_Todo.md`._
- **UI-1** — Refined the Search pagination pill: elevated card, tightly grouped page pills, long-press arrows jump to first/last, nearby-page fallback on narrow cards (Done; T-14/T-25).

_UI-2 … UI-9 are T-115's confirmed HIG findings (all adversarially verified against source; **0 P1 / 24 P2 / 28 P3**). Per-finding file/line evidence, HIG citations, and verifier notes: [`docs/audits/HIG_ADHERENCE_REVIEW_T115.md`](docs/audits/HIG_ADHERENCE_REVIEW_T115.md) §3. Status: all Open._

- **UI-2** — (P2) **VoiceOver labeling & grouping sweep.** The canonical patterns exist in-repo (OnboardingPointRow, SensitiveWorkRow, TextSizeSlider); these sites missed them: `WorkStatLabel`/`CardMetaLabel` raw HStacks (every card's stats read as fragments); comment-thread reply structure invisible (spine chrome hidden, no substitute label); Inbox rows = 5 overlapping buttons; `RecentlyDeletedRow` ungrouped; CustomizeTheme sliders unlabeled + swatch selection color-only (no `.isSelected`); `selectableRow`/`cyclingFacetRow` missing traits; MatureContent blurred branches use bare `onTapGesture` + swallow the nested expand button; `CardNavigationModifier`'s opacity-0 NavigationLink likely adds a stray stop to every card (needs VO runtime check); TagChip/BugReport/AuthorProfile/WorkDetail-tile smaller items. Umbrella: 126/160 files have zero a11y modifiers (16 control-heavy). Report §3.1.
- **UI-3** — (P2) **Sub-44pt hit targets.** Inbox bulk-bar icons (32pt), SearchPaginationBar buttons (31pt, arrows fixed-frame), `WorkRowExpandButton`, WorkDetail Remove-tag icon (tag chips in the same file got the 44pt fix), tappable author bylines app-wide (glyph-sized), `ReorderHandle` (28pt = absolute floor, sole reorder path, no hint), carousel-section collapse + see-all chevron, Comments pagination/sort. Report §3.2.
- **UI-4** — (P2) **Dynamic Type fixed frames.** Fixed 164×228pt cover cards truncate nearly every field at AX sizes (**runtime-confirmed**; `WorkStatLabel` also chains `lineLimit(1)`+`fixedSize()`); Work Detail's segmented control has no AX fallback (the quick-action grid in the same file shows the pattern); hero fandoms `lineLimit(3)` no expand; reader progress pill fixed 40pt; see-all grid hard-codes 2 columns (never widens on iPad/macOS); Account shortcut grid scales text down instead of reflowing. Report §3.3 + §5.1.
- **UI-5** — (P2) **Reduce Motion coverage.** Only 3 files gate on `accessibilityReduceMotion` (all correctly) vs 15 files animating. Ungated spatial sites: comment expand/highlight + animated scrollTo + Browse toast `.move`, carousel collapse `withAnimation(.snappy)`, iOS reader-chrome fade (file already reads the env var for a sibling), CustomizeTheme, stats `numericText`. Suggested: small `withReducedMotion` helper + sweep. Report §3.4 + §5.2.
- **UI-6** — (P2) **Theming/color-role hygiene.** Sepia segmented-control theming = global UIKit appearance proxy with hardcoded RGB (can't adapt to Increase Contrast/Reduce Transparency); excluded-tag state hardcodes `.red` ×3 (Sepia contrast unverified, no semantic role); `BugReportView` is the sole grouped-Form outlier missing `.appThemedRows()`/`.appThemedScroll()` (two-line fix); destructive-role misuse (Reset Filters, collection "Remove", unconfirmed context-menu-only Clear All Filters). Report §3.5.
- **UI-7** — (P2) **Consistency.** Bulk-select implemented twice, incompatibly — macOS dashboard has no select mode while the section list one level down has a cross-platform one (port the Bool-based pattern up); selection mode hides the tab bar non-modally to mask a Liquid Glass bleed (fix the bleed, not the chrome); hub chrome tokens diverge (Account 20pt everywhere vs Work Detail 16pt + stray 14pt, despite a sibling-parity comment; Author Profile picker = third shell); `chevron.right` on rows that don't push (segmented-tab jump, sheet-opening rows); pagination bar rendered twice with identical a11y labels; sheet-inside-inspector on iPhone. Report §3.6.
- **UI-8** — (P2/P3) **Timing magic numbers as completion signals.** 350ms `Task.sleep` sequences modal teardown → next push at 3 sites (CommentsView ×2, AuthorProfileView); 700ms sleep suppresses same-touch author-nav double-push in AppRouter. Replace with onDismiss continuations / deterministic signals. Report §3.7.
- **UI-9** — (P3) **Platform-split code health.** The two globally-`#if os(macOS)` reader files contain nested `#if os(iOS)` blocks that can never compile — and the dead copy already diverged (`.presentationContentInteraction(.scrolls)` exists only there, not in the live iOS sheet); shared `letterSpacingRange`'s negative half renders on macOS but is clamped on iOS. Report §3.8.

---

## 🧭 Key decisions & open questions

- **macOS reader — decided:** iOS/iPadOS use Readium; **macOS keeps the legacy
  reader** (Readium navigator is UIKit-only). Readium SPM is scoped `platformFilter = ios;`.
- **Workflow — decided:** **single `main` branch** (the `main`/`readium-migration`
  split was consolidated 2026-06-24). Just commit to `main`; no cross-branch porting.
- **AO3 authentication — decided:** native account UI drives AO3's real form in
  a hidden WebView; mechanism failures reveal the same WebView as a fallback.
  Sessions, never passwords, are stored device-only in Keychain.
- **Team ID in `project.pbxproj` — decided (2026-07-19, Wave 0):** keep
  `DEVELOPMENT_TEAM = NQH85H7343` as-is (2 of 4 build configs; the other 2 are
  already `""`). Owner call: the exposure is low-value (identifies the Apple
  Developer team, doesn't grant signing) and not worth a git-history rewrite. No
  scrub, no history purge. Closes A1-F2 / T-48 / KD-TEAMID — no action needed.
- **Library metadata — decided (2026-07-19, Wave 0):** split by platform. iOS
  consolidates onto **Readium's** metadata handling (Readium is iOS-only,
  `platformFilter = ios`). **macOS keeps the custom OPF layer** (legacy reader —
  no Readium navigator there to source metadata from). Closes KD-MIGRATION-META.
- **`KudosBackupManifest.supportedVersions` — decided (2026-07-19, Wave 0):** raise
  the floor to the latest version only — no pre-July `.kudosbackup` exports are
  still in use. `Services/KudosBackup.swift:174-175` changes from
  `[1, 2, 3, 4, 5, 6, currentVersion]` to `[currentVersion]` (currently `7`).
  Closes PONY-6.7. **Not yet implemented** — one-line change, unscheduled.
- **Compact/Detailed default — decided (2026-07-19, Wave 0):** keep `@AppStorage`
  semantics as-is; no forced migration for users who already launched under the
  old `.detailed` default (SHORT-R5). **New scope item:** the compact/detailed
  toggle should become **one app-wide setting**, not per-screen. Today it's **5
  independent stores** that don't share state: `AccountView.swift:33`
  (`@AppStorage("account.displayMode")`, default `.compact`),
  `HomeSectionListView.swift:36` (`@AppStorage("home.\(kind).displayMode")`,
  **keyed per Home section**, default `.detailed`), `LibrarySectionListView.swift:44`
  (same pattern, **keyed per Library section**), `ReadingQueues.swift:107` (plain
  `@State`, **not persisted at all** — resets every visit), plus
  `AccountComponents.swift:606` / `AuthorProfileContentSections.swift:194,364`
  (externally-driven parameters, no storage of their own). Fix: one shared
  `@AppStorage` key (or an observable settings object) read by all of them,
  default `.compact`. Unscheduled — natural fit for Wave 1 (shared foundation),
  same "one helper, many call sites" shape as X1–X6.
- **Cleanup — done:** `test/card-lists` (abandoned/polluted) was deleted in the
  2026-06-24 single-branch consolidation, along with all other non-`main` branches.
- **Layout overhaul (`test/home-tab-overhaul`) — decided:** Home & Library both use
  the shared `WorkCarouselSection`. **Library cards open the reader directly**,
  same as Home — confirmed live on iOS and macOS 2026-07-19 (Wave 0); this
  supersedes the earlier "Library cards open detail" note, which was stale (see
  BUG-8 in the Bugs registry). **Saved for Later = local saved + AO3 Marked for
  Later** merged; **Collections = placeholder** (no model yet); **no Synced/Local
  badges yet**. (Branch long since merged to `main` and deleted in the
  single-branch consolidation.)
- **Open — layout follow-ups (deferred):** (1) light filter *quick-chips* on the
  Library dashboard — only the active-filter banner + full Filters inspector exist
  so far; (2) a `Collection` model to make Collections real.

---

## ↩️ Context for the next session

- **Done & pushed (`main`, in sync with `origin`):** the full AO3 auth
  foundation + **Phase-2 reads** (Marked for Later, Bookmarks, History,
  Subscriptions — all under the Bookmarks tab's "AO3" segment), plus the
  missing-features batch — **download queue & bulk actions, About page, Continue
  Reading shelf, Reading Insights, and portable Library backups** (T-30…T-42,
  FI-10…19).
- **Natural next pickup:** fix **BUG-4** to restore the promised macOS build,
  then start **Highlights / notes / annotations**. Library Sync Folder still needs
  real-device iCloud Drive verification. The **live AO3 verification** debt still
  needs a real signed-in session.
- **Single branch:** the project is now just `main` (consolidated 2026-06-24) — no
  porting, no worktrees. Build **both** iOS (Readium) and macOS (legacy reader).
- Quick commands — Build/Test: `xcodebuild … CODE_SIGNING_ALLOWED=NO` ·
  `Scripts/test.sh` · Lint: `Scripts/lint.sh`.

## UI Consistency & Density Audit (Required Before Merge)

### Purpose
Ensure new UI work remains consistent with the established Kudos design language and does not regress information density, scanability, or theme behavior.

### Human Verification Required
The following items require screenshots and human review before completion:

- AO3 account sub-picker
- Library bulk-select mode
- Download queue banner
- About page
- Continue Reading shelf
- Search result cards
- Library cards

### Information Density Review
For every new or modified card:

- Verify title visibility is unchanged or improved
- Verify author visibility is unchanged or improved
- Verify fandom visibility is unchanged or improved
- Verify reading progress visibility is unchanged or improved
- Verify chapter/word count visibility is unchanged or improved
- Verify download status visibility is unchanged or improved
- Verify no critical metadata has been hidden behind additional taps

### Theme Consistency Review
Verify all new UI components:

- Use ThemeManager/AppThemeSurface
- Support Light, Dark, and Sepia themes
- Avoid hardcoded colors
- Avoid hardcoded corner radius values
- Avoid hardcoded shadows
- Follow existing spacing scale

### Card Family Consistency
Compare against existing Library/Search cards:

- Typography hierarchy
- Metadata presentation
- Badge styling
- Progress indicators
- Padding and spacing
- Corner radius treatment

### Approval Gate
Before merging UI changes:

1. Screenshot review completed
2. Density review completed
3. Theme review completed
4. Human approval received

Do not mark UI tasks complete until all four requirements are satisfied.
  
